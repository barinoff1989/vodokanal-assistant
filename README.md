# vodokanal-assistant

Прототип AI-помощника абонента водоканала: RAG-ассистент в личном кабинете (Этап 1) с последующим
переходом к агенту с операциями записи через подтверждение абонента (Этап 2).

> **Статус: разработка начата.** Закрыт шаг 0.5 плана — локальная тестовая модель как backend шлюза.
> Полноценный README с установкой и запуском пишется на шаге 11, после того как код появится целиком:
> раньше он был бы выдумкой.

## Что уже работает

Тестам не нужны ни LiteLLM, ни Ollama, ни сеть, ни установка пакета — только pytest:

```bash
python -m pip install --timeout 120 --retries 10 pytest pytest-asyncio
```

```bash
python -m pytest -q
```

Ожидаемо: `21 passed, 3 deselected`. Три отложенных — живые, им нужна модель.

Остальное ставится по мере надобности, не разом. `pydantic-settings` понадобится на шаге 1,
тяжёлый `litellm` — только для живой проверки и шага 4:

```bash
python -m pip install --timeout 120 --retries 10 litellm
```

Живая проверка на реальной модели — после установки Ollama:

```bash
ollama pull qwen2.5:7b-instruct-q4_K_M && python -m pytest -q -m live
```

Без Ollama живые тесты пропускаются с объяснением, а не падают.

> `Makefile` дублирует эти команды короткими целями, но `make` в Windows по умолчанию нет —
> основным считается путь через `python -m` выше.

## Ориентиры

| Документ | Где |
|---|---|
| План разработки — 14 шагов, ПО, доступы, данные | `AI_docs/Dev_Plan.html` |
| План тестирования — девять уровней | `AI_docs/Test_Plan.html` |
| Полный контекст проекта | `AI_docs/контекст_4.md` |
| Решения | `AI_docs/ADR/` |
| Выбор локальной модели | `docs/model-choice.md` |

## Правила, которые нельзя нарушать

- Персональные данные фильтруются в шлюзе **до** обращения к провайдеру.
- Ответ доходит до абонента потоком.
- Любая операция записи — только после подтверждения абонента, с идемпотентностью и аудитом.
- Тип обращения — единое машиночитаемое значение во всех компонентах.
- Зарубежные провайдеры общего назначения в конфигурацию шлюза не добавляются (152-ФЗ).
