# vodokanal-assistant

Прототип AI-помощника абонента водоканала: RAG-ассистент в личном кабинете (Этап 1) с последующим
переходом к агенту с операциями записи через подтверждение абонента (Этап 2). Пакет документации для
приёмки проекта — [AI_docs/](AI_docs/README.md).

## Материалы для сдачи

| Что | Где |
|---|---|
| **Репозиторий: /infra** — поднятие всего стека (Postgres, Redis, Qdrant, Backend) | [infra_instruction.md](infra_instruction.md); файлы стека — `docker-compose.yml`, `Dockerfile`, `docker/postgres/init/` |
| **Репозиторий: /backend** — код агентов и API (Python) | [backend_description.md](backend_description.md); код — папка `app/` |
| **Репозиторий: /docs** — архитектурная документация (ADR в Markdown, схемы, таблицы, PDF) | [docs_description.md](docs_description.md); документы — папка `AI_docs/` |
| **Видео для презентации**: работа с прототипом на демо-стенде, шесть вопросов (67 секунд) | [AI_docs/Demo_prototype.mp4](AI_docs/Demo_prototype.mp4) |
| **Презентация проекта** (PDF, 9 страниц) | [AI_docs/Сдача проекта AI-помощник абонента водоканала.pdf](AI_docs/Сдача%20проекта%20AI-помощник%20абонента%20водоканала.pdf) |
| **Нагрузочный отчёт** — сколько запросов в секунду держит система и какая задержка | [AI_docs/Load_Report.md](AI_docs/Load_Report.md) |

## Что уже работает

Шлюз к модели с лимитами, обезличиванием и охранителями; Backend с выбором пути ответа; поиск по базе
знаний; классификация темы; HTTP-интерфейс на двух адресах; демо-стенд; конечный автомат обращения;
адаптеры записи; метрики; доступ к данным Биллинга и личного кабинета.

Сквозной путь проверен на настоящем YandexGPT: страница → API → Backend → шлюз → модель → охранители
→ поток обратно. Два вопроса из четырёх до модели не доходят: об отключении отвечает точный поиск по
графику, а при пустом поиске ассистент отказывается отвечать наугад и называет контакт-центр.

Данные прототипа настоящие, а не выдуманные: FAQ воронежского филиала (20 пар вопрос-ответ), график
плановых отключений (1939 записей по 1821 адресу), выгрузка обращений (136 текстов, персональные
данные замаскированы), пример Биллинга и личного кабинета (200 счетов, 203 счётчика, 1323 начисления).

Качество поиска измерено на эталонном наборе из 66 вопросов (63 настоящих, разметка ручная):
**Recall@3 = 87%**, поиск 33,7 мс при бюджете 100 мс. Тот же замер показал, что порог отсечения
контекста вопросы с ответом и без **не разделяет** — открытый пункт, требует отдельной калибровки.

## Установка и прогон

Тестам не нужны ни Ollama, ни сеть, ни тяжёлый LiteLLM — но нужны те пакеты, которые код
импортирует на уровне модуля:

```bash
python -m pip install --timeout 120 --retries 10 pytest pytest-asyncio ruff mypy pydantic pydantic-settings httpx fastapi prometheus-client pyyaml
```

```bash
python -m pytest -q
```

Ожидаемо: `916 passed, 3 skipped, 10 deselected`. Десять отложенных — живые, им нужна модель; три пропущены по условию среды.

Весь пакет целиком, включая LiteLLM и клиент Redis:

```bash
python -m pip install --timeout 120 --retries 10 .
```

Слой защиты персональных данных ставится отдельно — русская модель spaCy идёт не с PyPI:

```bash
python -m pip install ".[pii]" && python -m spacy download ru_core_news_md
```

Живая проверка на реальной модели — после установки Ollama:

```bash
ollama pull qwen2.5:7b-instruct-q4_K_M && python -m pytest -q -m live
```

Без Ollama живые тесты пропускаются с объяснением, а не падают.

### Запуск сервиса и демо-стенда

**Основной путь — Docker Compose**, поднимает весь стек: Postgres/Redis/Qdrant, одноразовую
переиндексацию базы знаний в Qdrant и сам Backend (multi-stage `Dockerfile`, `--reload` живёт через
volume-монтирование `app/`/`web/`, пересборка образа не нужна на каждую правку кода):

```bash
docker compose up -d
```

Демо-стенд — `http://localhost:8000`, Swagger — `http://localhost:8000/docs`. Секреты (`YANDEX_API_KEY`
и т.п.) Compose подставляет из переменных окружения хоста — см. раздел ниже, файла `.env` не заводится.
Ollama (провайдер `local-test`) не контейнеризирована, остаётся сервисом хоста — из контейнера Backend
доступна по `http://host.docker.internal:11434` (уже задано в `docker-compose.yml`).

Пересобрать образ после изменения зависимостей (`pyproject.toml`) или системных файлов:

```bash
docker compose build backend reindex
```

**Альтернатива — голый `uvicorn` на хосте**, без Docker вообще (быстрее для точечной отладки, но нужно
поднять Postgres/Redis самому и держать зависимости Python в системе):

```bash
python -m uvicorn app.main:app --reload
```

### Настройки и ключи провайдеров

Приложение читает настройки **только из переменных окружения процесса** — файл `.env` не
используется и не читается (сознательно: секрет на диске в открытом виде — риск, которого нет у
переменной окружения). Список всех переменных с описанием — [env.example.sh](env.example.sh); это
справочник команд `export`, а не файл для копирования в `.env`.

Задать ключ YandexGPT можно двумя способами:

```bash
# на одну сессию терминала
export YANDEX_API_KEY="реальное-значение"
export YANDEX_FOLDER_ID="реальное-значение"
```

```powershell
# насовсем (Windows, применяется к НОВЫМ сессиям терминала)
setx YANDEX_API_KEY "реальное-значение"
setx YANDEX_FOLDER_ID "реальное-значение"
```

Провайдер по умолчанию — `local-test` (Ollama, без сети и трат); чтобы переключиться на реальный
YandexGPT, дополнительно задайте `LLM_PROVIDER=yandexgpt` тем же способом. Диагностика подключения —
`python scripts/check_yandex.py` (тоже без `.env`, только переменные окружения).

> `Makefile` дублирует эти команды короткими целями, но `make` в Windows по умолчанию нет —
> основным считается путь через `python -m` выше.

## Скрипты

| Скрипт | Зачем |
|---|---|
| `scripts/fetch_faq.py` | Сборка корпуса базы знаний с сайта филиала; `--check` сверяет сохранённое с сайтом |
| `scripts/sanitize_inquiries.py` | Очистка выгрузки обращений, приведение к одному филиалу, проверка нашим же обезличивателем |
| `scripts/generate_charges.py` | Начисления по счетам примера: услуги, основания, разнесение долга |
| `scripts/normalize_addresses.py` | Приведение адресов примера к воронежским улицам из графика отключений |
| `scripts/build_golden_set.py` | Сборка эталонного набора поиска из разметки и настоящих источников |
| `scripts/measure_retrieval.py` | Замер поиска на эталонном наборе: попадание в тройку и цена порога |
| `scripts/run_quality_eval.py` | Ночной прогон судьи качества по эталонному набору: faithfulness и answer relevancy |
| `scripts/measure_reranking.py` | Кросс-энкодер против косинуса вне пути абонента: расхождение троек и разделяющая способность (`--model` выбирает модель) |
| `scripts/measure_reranker_latency.py` | Задержка кросс-энкодеров на CPU: 20 пар «вопрос — фрагмент», варианты длины, числа кандидатов и INT8 |
| `scripts/load_server.py` | Сервер для нагрузочного замера: Backend с заглушкой вместо провайдера модели |
| `scripts/load_test.py` | Нагрузочный замер: RPS, задержки, загрузка процессора и памяти (отчёт — `AI_docs/Load_Report.md`) |
| `scripts/measure_search_models.py` | Замер моделей поиска: вес, память, задержка против бюджета |
| `scripts/measure_classifier.py` | Замер запасного режима классификации на настоящих обращениях |
| `scripts/measure_local_model.py` | Характеристики локальной модели: вес, память, скорость, время до первого токена |
| `scripts/check_yandex.py` | Диагностика подключения к YandexGPT |
| `scripts/build_openapi.py` | Пересборка `AI_docs/API_Spec.yaml` из тех же классов, по которым работает сервис |

## Ориентиры

| Документ | Где |
|---|---|
| **Пакет документации для приёмки** — архитектурные решения, стек, безопасность, ресурсы, дорожная карта | `AI_docs/ADR/`, индекс — `AI_docs/README.md` |
| Диаграммы (C1 контекст, C3 контейнеры, деплоймент, последовательности) | `AI_docs/C1/`, `AI_docs/C3/`, `AI_docs/` (корень, `*.html`) |
| Спецификация API — собирается из кода, не правится руками | `AI_docs/API_Spec.yaml` |
| Нагрузочный отчёт и его исходные числа | `AI_docs/Load_Report.md`, `golden_set/load_test*.json` |
| Видео работы с прототипом | `AI_docs/Demo_prototype.mp4` |
| Корпус базы знаний прототипа | `kb/faq_voronezh.json` |
| Эталонный набор поиска — разметка и последний замер | `golden_set/` |
| Пример данных Биллинга и ЛК | `data_example/` |

## Правила, которые нельзя нарушать

- Персональные данные фильтруются в шлюзе **до** обращения к провайдеру.
- Ответ доходит до абонента потоком.
- Любая операция записи — только после подтверждения абонента, с идемпотентностью и аудитом.
- У каждой оси классификации один справочник и одно назначение; значения разных осей никогда не
  сравниваются между собой.
- Что проверяется по факту, отвечается точным поиском, а не генерацией по найденному.
- Зарубежные провайдеры общего назначения в конфигурацию шлюза не добавляются (152-ФЗ).
