---
status: accepted
date: 2026-09-14
---

# ADR-500: Ресурсы и стоимость эксплуатации по контурам развёртывания

## Статус

**Accepted** как сводка. Все числа ниже — прогнозная модель, не официальная смета (P0 №1 проекта:
CAPEX/OPEX не утверждены владельцем). Здесь — только итог.

## Прототип — что работает сейчас

**Оборудование:** одна машина разработки, без видеоускорителя (в проекте измерено на Ryzen 5 5500U,
6 ядер, 15,3 ГБ RAM). Работает полный стек: FastAPI-сервис, `docker compose` (postgres, redis),
локальная тестовая модель через Ollama (Qwen2.5-7B, ~4,7 ГБ весов), эмбеддинги на CPU
(`multilingual-e5-small`, 0,46 ГБ).

**Денежные затраты — только токены YandexGPT.** Замер на реальном трафике этой машины (19 вызовов YandexGPT,
таблица `usage_events`, 24 сентября 2026): средний запрос ≈401 токен (322 на входе, 79 на выходе) ≈ 0,32 ₽. В этих вызовах
контекст короткий (фрагменты FAQ до 300 знаков); на базе из нескольких тысяч документов длина запроса может вырасти — не измерялось. При нагрузке одного тестового пользователя — расходы
на уровне единиц рублей в месяц.

**Проверено и отклонено:** облачная среда разработки (GitHub Codespaces) — базовая машина (32 ГБ диска)
не выдержала полного стека при установке зависимостей поиска (torch с GPU-пакетами, не нужными без
видеокарты, забил диск). Дешевле и надёжнее остаться на локальной машине, которая уже полностью
рабочая и мощнее базового тира облака.

## MVP — 8 000 абонентов

**Инфраструктура:** Kubernetes на своём железе, собственный GPU-пул под эмбеддинги и
переранжирование (BGE-M3 + BGE-reranker-v2-m3 — нужен видеоускоритель, на CPU превышают бюджет
задержки в 3–70 раз, замер 24 сентября 2026: BGE-M3 — 300 мс на запрос, реранкер — 7,2 с на 20 кандидатах при бюджете 100 мс; на GPU не измерялись). Qdrant вместо поиска в памяти; база знаний — несколько тысяч документов
(30 000–100 000 векторов, ≈0,3–1,0 ГБ, ADR-200). Телеметрия остаётся в Postgres: объём до ≈0,8 млн строк в год
на тираже (ADR-200).

**Стоимость токенов генерации (YandexGPT Pro, 0,8 ₽/1000 токенов):**

Допущения: 8 000 абонентов, 70% обращаются раз в месяц (5 600), 3 вопроса за визит в чате, доля визитов через
чат 10–50% — **допущения о спросе, данными водоканала не подтверждены**. Доля вопросов, реально доходящих до модели,
измерена на двух реальных наборах текстов: 30% (регистр обращений по телефону/почте) и 70% (форма вопросов golden set —
ближе к тому, что придёт в виджет). Токенов на запрос — 401 (замер, 19 вызовов).

| Доля визитов через AI-чат | Пользователей чата/мес | Вопросов/мес | При 30% до модели | При 70% до модели |
|---|---:|---:|---:|---:|
| 10% (базовый сценарий) | 560 | 1 680 | ≈162 ₽/мес | ≈377 ₽/мес |
| 50% (максимальное вовлечение) | 2 800 | 8 400 | ≈808 ₽/мес | ≈1 886 ₽/мес |

**Вывод: при измеренных 401 токене на запрос и принятых допущениях о спросе токены генерации — ≈160–1 900 ₽/мес.**
Это зависит от двух неподтверждённых чисел, и диапазон широкий:

| Допущение | Стоимость в месяц (8 000 абонентов) |
|---|---:|
| Таблица выше, 401 токен на запрос | ≈160–1 900 ₽ |
| Та же таблица, если в запросе 3 500 токенов (допущение A5 из `Capacity_Report.html`, замером не проверено) | ≈1 400–16 500 ₽ |
| Спрос из `Capacity_Report.html`, базовый сценарий (30% абонентов × 1,5 диалога × 4 запроса = 14 400 запросов), все запросы до модели, 401 токен | ≈4 600 ₽ |
| То же, 3 500 токенов на запрос (50,4 млн токенов) | ≈40 000 ₽ |

Два набора допущений о спросе в пакете расходятся: здесь — 0,2–1,05 вопроса на абонента в месяц, в
`Capacity_Report.html` — 0,3 / 1,8 / 7,5 (низкий / базовый / высокий). Ни один не подтверждён данными водоканала.
Фраза «на порядки меньше стоимости инфраструктуры», стоявшая здесь раньше, не подтверждена: стоимость инфраструктуры
не оценена (см. «Открытые пункты»), сравнивать не с чем.

GPU-пул под эмбеддинги/переранжирование содержится независимо от объёма трафика чата. Что онлайн-запросы загружают его
лишь на ≈1%, — **расчёт** по допущениям `Capacity_Report.html` (скорость на GPU не измерялась ни разу), не замер.
Основная нагрузка на него — индексация базы знаний.

**Ресурсы GPU-пула:** отдельно не оценены как единая смета (собственная позиция расходов —
сервер-хост, электричество, размещение, обслуживание — не входят в прогноз выше).

## Тираж — 90 000 абонентов

**Пересчёт нагрузки под масштаб** (по допущениям A1–A4 из `Capacity_Report.html`, данных водоканала нет; вывод — что
конкурентность в 20 раз ниже исходного проектного допущения — расчёт, не замер):

```
λ(90 000) ≈ 0,225 RPS (среднее)
L = λ × W ≈ 0,675 конкурентных запроса (среднее)
При пиковой неравномерности ×5…×10: L_пик = 3,4…6,75
```

**Вывод: 1× карта A100 80GB достаточно с большим запасом** — пиковая конкурентность втрое ниже
исходного расчёта, под который заложен объём VRAM (34 ГБ весов Qwen2.5-32B INT4 + запас
KV-cache; на GPU не проверялось, а 34 ГБ по общим сведениям соответствуют скорее 8-битному представлению — для INT4
ожидается около 17–20 ГБ; расхождение снимается при развёртывании). Наращивать число карт под масштаб 90 000 абонентов не требуется.

**Ориентировочная стоимость GPU-ресурса** (рыночная цена на момент прогноза, не тариф конкретного
поставщика; **источник цен в проекте не зафиксирован, коммерческих предложений нет** — `Capacity_Report.html` цен
сознательно не содержит; перед использованием цены нужно подтвердить):

| Вариант | Цена |
|---|---:|
| Карта A100 80GB, OEM | от ≈927 000 ₽ |
| Карта A100 80GB, оригинал/сертифицирована | ≈2 368 080 ₽ |
| Аренда карты у стороннего провайдера | ≈35 000–50 000 ₽/мес |

**Аренда исключена не по цене, а по триггеру.** Развёртывание собственной модели происходит именно
тогда, когда триггер — отказ от внешних API по требованию ИБ; облачная аренда GPU в этом состоянии
тоже считается внешним сервисом, поэтому единственный вариант — покупка карты в собственный контур.
Сервер-хост, электричество, размещение, обслуживание без выделенного ML-инженера в штате — отдельные
позиции, в прогнозе не оценены.

## Свод по контурам

| | Прототип (сейчас) | MVP (8 000 абонентов) | Тираж (90 000 абонентов) |
|---|---|---|---|
| Вычисления | 1 машина разработки, CPU | Kubernetes-кластер + GPU-пул (эмбеддинги/реранжинг) | То же + 1× A100 80GB (генерация) |
| Хранилища | Postgres, Redis (docker compose) | + Qdrant (телеметрия остаётся в Postgres) | Без изменений относительно MVP |
| Генерация | YandexGPT (внешний API) | GigaChat Pro (внешний API) + резерв YandexGPT — предварительно, решение по итогам теста на живых данных (ADR-600) | Self-hosted Qwen2.5-32B — по триггеру, внешний API остаётся до него |
| Токены/мес | единицы ₽ | ≈160–1 900 ₽ (до ≈16 500 ₽ при 3 500 токенов на запрос) | не пересчитано — заменяется CAPEX на карту |
| Разовые затраты | нет | GPU-сервер под эмбеддинги/реранжинг (не оценено отдельно) | ≈930 тыс.–2,4 млн ₽ на карту (по триггеру) |

## Объём и нагрузка по контейнерам

Что заложено по каждому контейнеру и на чём это основано. Нагрузка из `Capacity_Report.html` (допущения A1–A5, данных водоканала нет): MVP (10 тыс. абонентов) — базовый сценарий 18 000 запросов в месяц, пик 0,025 запроса/с, одновременно 0,08; высокий — 75 000, 0,10 запроса/с, 0,31. Тираж (100 тыс.) — базовый 180 000, 0,25 запроса/с, 0,75; высокий 750 000, 1,04 запроса/с, 3,12. Сценарии рассчитаны под 10 и 100 тысяч абонентов, контуры
этого документа — 8 000 и 90 000; разница масштаба — около 20%.

| № | Контейнер | Что заложено | Статус |
|---:|---|---|---|
| 1 | AI-виджет | Отдельного объёма нет: статические файлы, нагрузка — общая (см. Backend). Одновременно до 0,3 запроса на MVP и до 3 на тираже (расчёт). | Расчёт по допущениям |
| 2 | Backend | Общая нагрузка (см. выше). Замер: путь без модели — 117 запросов/с на один процесс (8,5 мс CPU на запрос), при 4 параллельных запросах медиана 36 мс. Выбрано: 2 реплики на MVP и 3 на тираже по 0,5 и 1 vCPU, ОЗУ 0,5 и 1 ГиБ (лист «Sizing»). | Замер пропускной способности; реплики выбраны |
| 3 | Классификатор обращений (Triage) | Один проход на вопрос (22–37 мс на CPU, замер). Набор примеров сейчас — 136 обращений; для MVP требуется не менее 100 на тип (около 1 500–2 000 текстов). | Замер задержки; объём набора — план |
| 4 | LLM Gateway | Общая нагрузка. Лимиты по умолчанию в `app/config.py`: 20 запросов в минуту на абонента, 10 на сессию, 300 на сервис (5 запросов/с); расчётом не обоснованы. Сессия живёт 30 минут. | Настройки прототипа |
| 5 | PII Filter | Один проход на вопрос и один на ответ. Время проверки ответа ≈800 знаков — 30–45 мс (замер), на ≈2 000 знаков — 60–70 мс. Память обезличивателя (Presidio + spaCy) — 769 МиБ (замер). Работает внутри шлюза, отдельного пода нет. | Замер |
| 6 | Guardrails и судья качества | Судья: 39 млн токенов в месяц на MVP и 120 млн на тираже (30 млн — ночной прогон Golden Set, остальное — 5% выборка трафика); допущение: 5 вызовов судьи по ≈2 000 токенов на образец. | Расчёт по допущениям |
| 7 | LLM Provider | Токенов на запрос: измерено 401 (19 вызовов, фрагменты FAQ до 300 знаков), допущение отчёта — 3 500. Стоимость токенов на MVP — ≈160–16 500 ₽/мес по ADR-500, до ≈40 000 ₽/мес при спросе из отчёта. Агент Этапа 2 добавляет около +20% токенов. | Замер (401) и допущение (3 500) |
| 8 | Эмбеддинги (модель поиска) | Один запрос на вопрос; при индексации 30 000–100 000 частей. Скорость на CPU измерена, на GPU — нет. | Замер на CPU; GPU не измерялся |
| 9 | Реранкер | Одно переранжирование 20 кандидатов на вопрос. На CPU 7,2 с (замер); загрузка GPU ≈0,1% на MVP и ≈1% на тираже — расчёт, скорость на GPU не измерялась. | Расчёт; GPU не измерялся |
| 10 | Векторная база (Qdrant) | Несколько тысяч документов (3 000–5 000), 30 000–100 000 векторов размерности 1 024, ≈0,3–1,0 ГБ. От числа абонентов не зависит. Поиск на 100 000 векторов: P99 65 мс без фильтра, 37 мс с фильтром (замер). | Допущение объёма; замер поиска |
| 11 | Кэш профиля абонента (Postgres) | 8 000 профилей на MVP, 90 000 на тираже. Строка профиля с начислениями — 3 КБ (допущение): ≈24 МБ на MVP, ≈270 МБ на тираже. По примеру прототипа (200 счетов, 1 323 начисления) ≈6,6 начисления на счёт. Хранится в Postgres (см. ниже). | Допущение; выбрано и посчитано |
| 12 | Redis (квоты, состояние диалога) | Сессия живёт 30 минут, одновременно до 0,3 запроса на MVP и до 3 на тираже; замер: ключ квоты ≈0,5 КБ, сессия ≈0,8 КБ на ход; до 1 800 одновременных сессий ≈5 МБ. Выбрано: 1 экземпляр на MVP, 2 на тираже, 0,25 ГиБ (лист «Sizing»). | Замер; экземпляры выбраны |
| 13 | Postgres (обращения, слепки, события, телеметрия) | Телеметрия: 6 000–70 600 строк в год на MVP, 68 000–794 000 на тираже (≈2–245 МБ в год; замер 2 млн строк — 589 МБ). Журнал диалогов при сроке хранения 90 суток (выбрано) — до 0,15 ГБ на MVP и 1,5 ГБ на тираже (4 КБ на диалог, высокий сценарий). Итого до 0,5 ГБ на MVP и до 5 ГБ на тираже, том 20 и 50 ГиБ (лист «Sizing»). Число обращений в месяц принято равным диалогам с записью (допущение A6). | Расчёт и замер; срок хранения выбран |
| 14 | Индексация базы знаний (ETL) | 30 000–100 000 частей. На CPU e5-small ≈14–46 мин, BGE-M3 ≈2,7–9,6 ч; на GPU не измерялось. | Расчёт по замеру на CPU |
| 15 | Агент записи, Billing Adapter, Сервис обращений, Document Generator (Этап 2) | Допущение A6: 25% диалогов ведут к записи — 1 125 диалогов в месяц на MVP, 11 250 на тираже (базовый сценарий); надбавка токенов ≈12 и ≈124 млн в месяц. Хранилище готовых документов (MinIO): ≈200 КБ на бланк (допущение) × 1 125 в месяц — 2,7 ГБ в год на MVP, 27 ГБ в год на тираже; том MinIO 100 и 200 ГиБ с копиями и документами базы знаний (лист «Sizing»). | Допущение; выбрано и посчитано |
| 16 | Оркестрация (Kubernetes), узлы | Расчёт по выбранным допущениям (лист «Sizing»): MVP — 6 узлов (3 общих 8 vCPU/16 ГиБ, 2 данных 8 vCPU/32 ГиБ, 1 GPU-узел с видеокартой 24 ГБ), тираж — 8 (4 общих, 2 данных, 1 GPU, 1 узел под собственную модель с A100 80 ГБ). Диск под тома ≈176 ГиБ на MVP и ≈467 ГиБ на тираже. Стоимость серверов не оценена. | Расчёт по допущениям |
| 17 | Наблюдаемость | Loki: ≈0,2 ГБ за 30 дней на MVP, ≈1,7 ГБ на тираже (допущение: 10 записей на запрос по ≈1 КБ). Prometheus: 84 ряда на процесс приложения (замер), до 20 000 рядов с метриками кластера (допущение), хранение 15 суток ≈2,6 ГБ; том 10 ГиБ (лист «Sizing»). | Допущение и замер |
| 18 | Секреты (Vault) | Объёма нет. | Не применимо |

**Вывод.** Объём по-настоящему задан для базы знаний (несколько тысяч документов) и телеметрии. По остальным контейнерам нагрузка взята из
допущений о спросе, а размеры подов, число узлов и объёмы хранилищ выбраны и посчитаны в разделе «Расчётный sizing по компонентам» ниже
(допущения, не замеры). Полная таблица — листы «Объём по контейнерам» и «Sizing» файла `Tech_Stack_Matrix.xlsx`.

## Расчётный sizing по компонентам

Ниже — выбранные допущения и расчёт для каждого компонента: число реплик, запросы на под (CPU, память, диск, видеопамять) и число узлов.
Это **расчёт по допущениям**, а не результат нагрузочного теста: замерено только то, что указано в колонке «Основание».
Числа можно менять в листе «Sizing» файла `Tech_Stack_Matrix.xlsx` — итоги и число узлов пересчитываются формулами.

**Выбранные допущения**

- **Расчётная нагрузка.** Пик MVP — 0,1 запроса/с, тираж — 1 запрос/с (высокий сценарий `Capacity_Report.html`, пересчитанный на 8 000 и 90 000 абонентов и округлённый вверх); одновременно 0,3 запроса на MVP и 3 на тираже (время ответа 3 с). До модели доходит 70% запросов (верхняя оценка ADR-500).
- **CPU на запрос.** Путь без модели — 8,5 мс на запрос (замер: один процесс отдаёт 117 запросов/с). Путь с моделью — 0,9 с на ответ в 800 знаков (эмбеддинг 36 мс + обезличивание + проверка ответа на потоке 0,3–0,9 с; на ответе в 2 000 знаков 1,4–4,5 с, замер). При 0,7 запроса/с к модели это ≈0,63 ядра на тираже; на длинных ответах — до 3,2 ядра.
- **Память.** Замер: обезличиватель (Presidio + spaCy `ru_core_news_md`) — 769 МиБ, эмбеддер e5-small — ещё 300 МиБ, контейнер Backend целиком — 1,32 ГиБ. На MVP эмбеддер уходит в отдельный сервис, обезличиватель остаётся в шлюзе.
- **Данные.** Ключ квоты в Redis — 72 байта, с накладными ≈0,5 КБ (замер). Сессия диалога — 1,6 КБ на два хода, ≈0,8 КБ на ход (замер), живёт 30 минут. Диалог — 4 хода ≈ 4 КБ с метаданными (допущение A3 = 4). Срок хранения журнала диалогов принят 90 суток (требование ИБ неизвестно). Строка телеметрии — 308 байт с индексами (замер).
- **Отказоустойчивость.** Минимум 2 реплики у stateless-сервисов; у хранилищ — 1 экземпляр на MVP с ежедневной копией и 2 экземпляра на тираже. Узлов на один больше, чем нужно по запросам (N+1); GPU-узел один, потеря узла = отказ поиска до восстановления.
- **Узлы.** Загрузка узла по запросам не выше 70%. Класс узлов: общий — 8 vCPU / 16 ГиБ; данных — 8 vCPU / 32 ГиБ с NVMe; GPU — 8 vCPU / 32 ГиБ и видеокарта 24 ГБ; LLM (тираж) — 16 vCPU / 128 ГиБ и A100 80 ГБ.

| Компонент | MVP (8 000 абонентов) | Тираж (90 000 абонентов) | Основание |
|---|---|---|---|
| Ingress (nginx) | 2 × (0,25 vCPU, 0,25 ГиБ) | 2 × (0,25 vCPU, 0,25 ГиБ) | Допущение. Внешний трафик — единицы запросов в секунду. |
| Vault | 1 × (0,25 vCPU, 0,25 ГиБ, диск 1 ГиБ) | 1 × (0,25 vCPU, 0,25 ГиБ, диск 1 ГиБ) | Допущение. Инжектор секретов и cert-manager — отдельной строкой не выделены (≈0,1 vCPU / 0,1 ГиБ). |
| AI-виджет (статические файлы) | 2 × (0,1 vCPU, 0,1 ГиБ) | 2 × (0,1 vCPU, 0,1 ГиБ) | Допущение. На стенде три статических файла. |
| Backend (API, классификация, поиск, сборка ответа) | 2 × (0,5 vCPU, 0,5 ГиБ) | 3 × (1 vCPU, 1 ГиБ) | Замер: путь без модели — 117 запросов/с на процесс (8,5 мс CPU). 2 реплики нужны для отказоустойчивости, а не для пропускной способности; ОЗУ — допущение (эмбеддер и обезличиватель вынесены). |
| LLM Gateway (лимиты, обезличивание, проверка ответа) | 2 × (0,5 vCPU, 1,5 ГиБ) | 3 × (1 vCPU, 1,5 ГиБ) | Замер ОЗУ: обезличиватель 769 МиБ. CPU: 0,9 с на ответ в 800 знаков — 0,06 ядра на MVP, 0,63 на тираже; на ответах в 2 000 знаков до 3,2 ядра (3 реплики × 1 vCPU + предел 2). После оптимизации проверки на потоке запас вырастет. |
| Billing Adapter, оркестратор записи (Этап 2) | 2 × (0,25 vCPU, 0,25 ГиБ) | 2 × (0,25 vCPU, 0,25 ГиБ) | Допущение. 1 125 диалогов с записью в месяц на MVP, 11 250 на тираже (A6 = 25%) — нагрузка ничтожна. |
| Document Generator (PDF, Этап 2) | 1 × (0,5 vCPU, 1 ГиБ) | 2 × (0,5 vCPU, 1 ГиБ) | Допущение. Библиотека PDF не выбрана; ≈1 бланк на диалог с записью. |
| ETL индексации (CronJob, окно 02:00–05:00) | 1 × (1 vCPU, 2 ГиБ, диск 5 ГиБ) | 1 × (1 vCPU, 2 ГиБ, диск 5 ГиБ) | Допущение. Эмбеддинги считает GPU-сервис; 5 ГиБ — временные файлы документов. Время индексации на GPU не измерялось. |
| Оценка качества (CronJob, ночной прогон) | 1 × (0,5 vCPU, 1 ГиБ) | 1 × (0,5 vCPU, 1 ГиБ) | Допущение. Ходит во внешний API судьи; локальной нагрузки почти нет. |
| Prometheus | 1 × (0,5 vCPU, 1 ГиБ, диск 10 ГиБ) | 1 × (0,5 vCPU, 1 ГиБ, диск 10 ГиБ) | Замер: 84 ряда на процесс приложения; допущение: до 20 000 рядов с метриками кластера, хранение 15 суток ≈2,6 ГБ. |
| Grafana | 1 × (0,25 vCPU, 0,25 ГиБ, диск 1 ГиБ) | 1 × (0,25 vCPU, 0,25 ГиБ, диск 1 ГиБ) | Допущение. |
| Loki | 1 × (0,5 vCPU, 1 ГиБ, диск 20 ГиБ) | 1 × (0,5 vCPU, 1 ГиБ, диск 20 ГиБ) | Расчёт из `Capacity_Report.html`: 0,2 ГБ / 30 суток на MVP, 1,7 ГБ на тираже (допущение 10 записей на запрос по 1 КБ); 20 ГиБ с запасом. |
| Alertmanager | 1 × (0,1 vCPU, 0,1 ГиБ) | 1 × (0,1 vCPU, 0,1 ГиБ) | Допущение. |
| Qdrant | 1 × (1 vCPU, 2 ГиБ, диск 10 ГиБ) | 2 × (1 vCPU, 2 ГиБ, диск 10 ГиБ) | Расчёт: 30–100 тыс. векторов × 1 024 × 4 байта = 123–410 МБ, с индексом ≈1 ГБ; во время blue-green две коллекции и копия — 10 ГиБ. Замер: 100 000 векторов, P99 65 мс. От числа абонентов не зависит; вторая реплика на тираже — для отказоустойчивости. |
| Postgres (обращения, профиль, телеметрия, журнал диалогов) | 1 × (1 vCPU, 2 ГиБ, диск 20 ГиБ) | 2 × (1 vCPU, 2 ГиБ, диск 50 ГиБ) | Расчёт данных за 90 суток по высокому сценарию: журнал диалогов 4 КБ × 12 500 диалогов в месяц = 150 МБ (MVP) и × 125 000 = 1,5 ГБ (тираж); профиль 24 МБ и 270 МБ (3 КБ на абонента — допущение); телеметрия ≤ 0,25 ГБ в год (2,4 ГБ с запасом ×10); обращения ≈30–300 МБ в год. Итого до 0,5 ГБ (MVP) и до 5 ГБ (тираж); диск с запасом на журнал и копии. |
| Redis (квоты и сессии) | 1 × (0,1 vCPU, 0,25 ГиБ, диск 1 ГиБ) | 2 × (0,1 vCPU, 0,25 ГиБ, диск 1 ГиБ) | Замер: ключ квоты ≈0,5 КБ, сессия ≈0,8 КБ на ход, срок жизни 30 минут. До 1 800 одновременных сессий (1 запрос/с × 30 мин) × 3 КБ ≈ 5 МБ: объём ничтожен. Отказ Redis отклоняет запросы (fail-closed), поэтому на тираже 2 экземпляра. |
| MinIO (документы, копии, снимки) | 1 × (0,25 vCPU, 0,5 ГиБ, диск 100 ГиБ) | 1 × (0,5 vCPU, 1 ГиБ, диск 200 ГиБ) | Расчёт: готовые бланки ≈200 КБ (допущение) × 1 125 в месяц = 2,7 ГБ в год (MVP) и × 11 250 = 27 ГБ в год (тираж), хранение 3 года; копии Postgres за 7 суток до 35 ГБ; документы базы знаний ≈1 ГБ; снимки Qdrant. |
| Эмбеддинги (BGE-M3, inference) | 1 × (1 vCPU, 6 ГиБ, диск 5 ГиБ, видеопамять 3 ГиБ) | 1 × (1 vCPU, 6 ГиБ, диск 5 ГиБ, видеопамять 3 ГиБ) | Замер: веса 4,25 ГБ (fp32). Допущение: в fp16 ≈2,3 ГБ + запас — ≈3 ГБ видеопамяти. Скорость на GPU не измерялась. |
| Реранкер (BGE-reranker-v2-m3, inference) | 1 × (1 vCPU, 3 ГиБ, диск 3 ГиБ, видеопамять 1,5 ГиБ) | 1 × (1 vCPU, 3 ГиБ, диск 3 ГиБ, видеопамять 1,5 ГиБ) | Замер: веса 2,14 ГБ. Допущение: ≈1,5 ГБ видеопамяти. Скорость на GPU не измерялась; на CPU 7,2 с на 20 кандидатов. |
| Собственная модель (Qwen2.5-32B INT4, vLLM) — по триггеру | нет | 1 × (8 vCPU, 64 ГиБ, диск 100 ГиБ, видеопамять 40 ГиБ) | Только на тираже по триггеру. Видеопамять 40 ГБ — допущение (веса INT4 ожидаются 17–20 ГБ, заложено 34 ГБ, плюс кэш); проверка на карте не проводилась. |

**Итог по пулам узлов** (запросы CPU / память ГиБ / видеопамять ГиБ по всем подам пула):

| Пул | Класс узла | MVP: запросы | MVP: узлов | Тираж: запросы | Тираж: узлов |
|---|---|---|---:|---|---:|
| Общий пул (платформа, приложение, наблюдаемость) | 8 vCPU, 16 ГиБ | 6,8 / 11,8 / 0 | **3** | 11,3 / 16,3 / 0 | **4** |
| Пул данных (NVMe) | 8 vCPU, 32 ГиБ | 2,35 / 4,75 / 0 | **2** | 4,7 / 9,5 / 0 | **2** |
| GPU-пул (эмбеддинги, реранкер) | 8 vCPU, 32 ГиБ, видеокарта 24 ГБ | 2 / 9 / 4,5 | **1** | 2 / 9 / 4,5 | **1** |
| GPU-пул генерации (тираж) | 16 vCPU, 128 ГиБ, видеокарта 80 ГБ | 0 / 0 / 0 | **0** | 8 / 64 / 40 | **1** |

**Итого узлов: 6 на MVP и 8 на тираже.** Диск под тома подов — ≈176 ГиБ на MVP и ≈467 ГиБ на тираже. Общий и
данных пулы взяты с запасом на потерю одного узла; GPU-узел один и является единой точкой отказа (при его потере поиск недоступен до
восстановления — риск, принятый на MVP).

**Что этот расчёт не покрывает.** Скорость моделей на GPU (объём видеопамяти и время индексации — допущения). Стоимость серверов,
электричества, размещения и обслуживания (в ADR не оценивались). Накладные расходы системных подов Kubernetes (примерно 1 vCPU и 2 ГиБ
на узел). Нагрузочный тест не проводился: пропускная способность пути с моделью посчитана по замеру CPU на ответ, а не измерена под
параллельной нагрузкой. Срок хранения диалогов 90 суток и класс узлов выбраны мной, требований ИБ и поставщика нет.

## Открытые пункты

- Официальная смета CAPEX/OPEX не утверждена владельцем — P0 №1 проекта, каждое число выше нужно
  защищать перед финальным утверждением, не принимать как готовую цифру.
- Стоимость GPU-сервера под эмбеддинги/переранжинг на MVP не оценена отдельной строкой.
- Сервер-хост, электричество, размещение и обслуживание для GPU тиража — не оценены.
- Скорость эмбеддингов, реранкера и генерации на GPU не измерялась ни разу: все числа «на GPU» — допущения, замеры
  сделаны на CPU (ADR-300).
- Допущения о спросе (доля абонентов, диалогов на абонента, запросов на диалог) не подтверждены данными водоканала;
  в пакете два несовместимых набора (здесь и в `Capacity_Report.html`), стоимость токенов зависит от них на порядок.
- Длина запроса к модели на базе из нескольких тысяч документов не измерялась: замер 401 токен получен на фрагментах FAQ
  до 300 знаков, допущение отчёта о мощности — 3 500.
- Цены на карту A100 не подтверждены коммерческими предложениями, источник в проекте не зафиксирован.
- Объём весов Qwen2.5-32B в INT4 (заложено 34 ГБ, ожидается 17–20 ГБ) не проверен.

## Источники

Приложение к пакету — [Capacity_Report.html](../Capacity_Report.html).
