---
status: accepted
date: 2026-09-14
---

# ADR-200: Технологический стек по контурам развёртывания

## Статус

**Accepted** как сводка: вывод и таблица «выбор против альтернатив» по каждому слою стека. Подробные сравнения
вариантов — в разделах «Почему Python, а не Go или Node.js», «Почему FastAPI…», «Почему uvicorn», «Как устроен
шлюз к модели» и «Сравнение вариантов по слоям» (хранилища, виджет, протокол потока, данные абонента, индексация,
наблюдаемость, документы Этапа 2).

## Как читать таблицу

Колонка «Контур» показывает, где компонент реально разворачивается: **прототип** — уже работает,
**MVP** — целевая production-инфраструктура (разворачивается после stage-gating, ADR-100), **тираж** —
масштаб на порядок больше. Это ось развёртывания, а не функциональный этап (Этап 1/Этап 2 — про
возможности системы, см. ADR-100) — строка про FSM-оркестрацию ниже единственная, где важна именно
функция, а не контур, это отмечено отдельно.

## Backend и оркестрация

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Веб-фреймворк | Python / FastAPI | прототип и далее | Go, Node.js (сравнение языков); Flask, Django REST Framework, Litestar (сравнение фреймворков) — оба сравнения ниже | разделы ниже |
| Сервер запуска Backend | uvicorn (ASGI) | прототип и далее | Hypercorn, Daphne, Granian; gunicorn — сравнение ниже | раздел ниже |
| Оркестрация контейнеров | Kubernetes в том же кластере, где работает личный кабинет (решение заказчика проекта, 24 сентября 2026; GPU-узлов в нём нет; эксплуатирует тот же отдел) | MVP | Docker Compose на нескольких хостах (не переживает отказ узла); Docker Swarm; Nomad — реальная альтернатива, если некому эксплуатировать Kubernetes | раздел «Почему Kubernetes» ниже |
| Оркестрация на прототипе | `docker compose`, один хост | прототип | — (единственный разработчик, одна машина — K8s избыточен) | — |
| Запуск самого Backend | Сервис `docker compose` (`Dockerfile`, multi-stage) — `docker compose up -d` поднимает весь стек, включая Backend | прототип | Изначально Backend запускался на хосте (`python -m uvicorn`) в обход Compose, который держал только Postgres/Redis/Qdrant — несогласованность самим оркестрации не была, просто так сложилось по шагам разработки; голый `uvicorn` остаётся быстрой альтернативой для точечной отладки без Docker | — |
| Оркестрация агента (функциональный Этап 2, любой контур) | Свой FSM (`app/fsm.py`) | написан на прототипе, включается с функционального Этапа 2 | LangGraph — кандидат по триггеру, не выбран: возможности, ради которых он нужен, на текущем сценарии проверить нечем; `transitions` отклонена (строковые ключи) | раздел «Оркестрация агента» ниже |

### Почему Python, а не Go или Node.js — Backend / API Gateway

Команда полиглотная (Go Middle, Python Middle, Node.js Upper) — все три языка были реальными вариантами,
не гипотетическими. Решающее ограничение — архитектурное: RAG-логика
(эмбеддинг запроса, поиск, сборка промпта) обязана жить **внутри** контейнера Backend, не выноситься в
отдельный сервис.

| Критерий | Python / FastAPI | Go | Node.js |
|---|---|---|---|
| Экосистема RAG/AI (эмбеддинги, PII-обезличивание, LLM-SDK) | Родная — LiteLLM, sentence-transformers, Presidio написаны на Python или имеют полноценные Python-биндинги | Зрелых библиотек почти нет — пришлось бы переписывать их или звать Python-подпроцесс | Тоньше, чем у Python: SDK для LLM-провайдеров есть, но нет инструментов PII-обезличивания и локального инференса эмбеддингов уровня Presidio/sentence-transformers |
| Совместимость с ограничением «RAG внутри одного контейнера» | Прямая — тулинг уже на этом языке | Нарушается или требует второго языка/процесса внутри контейнера | Та же проблема, что у Go |
| Производительность и конкурентность | Достаточно — нагрузка I/O-bound (вызовы LLM Gateway, Vector DB, Биллинга) | Объективно сильнее всего (goroutines), но выигрыш не используется — узкое место не здесь (см. ниже) | Сопоставимо с Python — тоже событийный цикл, тоже под I/O-bound |
| Contract-first / OpenAPI | FastAPI генерирует OpenAPI-схему из кода — совпадает с принципом проекта | Нужна отдельная библиотека и дисциплина | Нужна отдельная библиотека (например, NestJS + swagger) |
| Итог | **Выбран** | Отклонён | Отклонён |

**Go отклонён**, несмотря на объективное преимущество в конкурентности: этот выигрыш не используется,
потому что узкое место системы — сетевая задержка вызова внешнего LLM-провайдера (сотни миллисекунд —
секунды), а не накладные расходы веб-фреймворка (единицы миллисекунд). Замер 24 сентября 2026: время до первого куска у YandexGPT — медиана 0,78 с,
P90 5,6 с (19 вызовов); простой запрос `/v1/healthz` к Backend в docker — медиана 5,9 мс, P95 8,0 мс (200 запросов, включая запуск curl и
пересылку порта — это верхняя оценка накладных расходов). **Node.js отклонён** по той же
причине, что и Go — нехватка зрелого RAG/PII-инструментария; использование одного языка для фронтенда и
бэкенда не решает эту проблему, ограничение именно в инструментарии, а не в знании команды JavaScript.

LLM Gateway и ETL Worker на других языках даже не сравнивались — они с самого начала Python-only, потому
что весь нужный тулинг (LiteLLM, sentence-transformers) существует только в этой экосистеме.

### Почему FastAPI, а не другой Python-фреймворк

Выбор языка оставляет вопрос фреймворка. Сравнение сделано по документации фреймворков и по требованиям
проекта; замер скорости не проводился и не нужен — узкое место системы задержка внешней модели (см. выше),
а не накладные расходы фреймворка.

Требования, которые следуют из самого проекта:

1. **Асинхронность и потоковая выдача.** Ответ абонента идёт потоком, а сервис ждёт внешние системы
   (модель, Qdrant, Redis, Биллинг).
2. **Схемы и валидация на pydantic.** Модели проекта (`app/models.py`) уже написаны на pydantic; второй
   слой описаний рядом с ними — источник расхождений.
3. **OpenAPI из тех же моделей.** Спецификация `API_Spec.yaml` собирается из кода
   (`scripts/build_openapi.py`), а не пишется руками.
4. **Тесты без сети.** Тестовый клиент фреймворка (`fastapi.testclient`) гоняет контрактные тесты без
   запуска сервера.
5. **Небольшой вес.** Проекту нужен API, а не ORM, админка и шаблоны.

| Критерий | FastAPI | Flask | Django REST Framework | Litestar |
|---|---|---|---|---|
| Асинхронность и потоковый ответ | Родные (ASGI), потоковый ответ из коробки | По умолчанию синхронный (WSGI); async-обработчики возможны, но параллельные ожидания и потоковая выдача сложнее | По умолчанию синхронный, поддержка async ограничена | Родные (ASGI) |
| Схемы и валидация на pydantic | Встроены, тот же pydantic, что в моделях проекта | Отдельные библиотеки (marshmallow и т. п.) — дублируют модели | Собственные сериализаторы — дублируют модели | Поддерживает pydantic и msgspec |
| OpenAPI из кода | Из коробки | Через расширения | Через дополнение | Из коробки |
| Вес и лишние части | Лёгкий: только то, что нужно API | Лёгкий | Тяжёлый: ORM, админка, настройки Django, которые проекту не нужны | Лёгкий |
| Сообщество и зрелость | Самое широкое среди async-фреймворков Python | Самое широкое в целом | Широкое | Моложе, сообщество заметно меньше |
| Итог | **Выбран** | Отклонён | Отклонён | Отклонён |

**Flask отклонён**: всё, что FastAPI даёт из коробки (схемы, валидация, OpenAPI, async), пришлось бы
собирать из расширений. **Django REST Framework отклонён**: тянет за собой части Django, которые проекту не
нужны, и ведёт второй слой описаний рядом с моделями. **Litestar** близок по возможностям и не отвергнут по
качеству: выигрыша для проекта он не даёт (FastAPI уже закрывает все требования), а сообщество у него
меньше, что важнее для поддержки командой. Голый **Starlette** отдельно не рассматривался: FastAPI построен
на нём, а схемы, валидацию и OpenAPI пришлось бы дописывать самим.

Решение обратимо частично: модели pydantic и логика переживают смену фреймворка, переписываются только
маршруты (`app/api.py`).

### Почему uvicorn

FastAPI сам по себе сеть не слушает — ему нужен ASGI-сервер, который принимает HTTP-соединения и вызывает
код приложения. Требования: асинхронная работа и потоковая отдача; запуск в контейнере одной командой с
режимом перезагрузки на время разработки; минимум лишних зависимостей; масштабирование на MVP репликами
подов Kubernetes, а не менеджером процессов внутри контейнера.

| Критерий | uvicorn | Hypercorn | Daphne | Granian | gunicorn |
|---|---|---|---|---|---|
| Работает с ASGI и потоковой отдачей | Да | Да | Да | Да | Сам — нет (синхронный, WSGI); работает только как надстройка над воркерами uvicorn |
| Место в экосистеме FastAPI | Сервер по умолчанию в документации FastAPI | Поддерживается, встречается реже | Создан для Django Channels | Новее, сообщество меньше | Менеджер процессов, а не замена серверу |
| Чем отличается | Минимум настроек, режим `--reload` для разработки | Поддержка HTTP/2 и HTTP/3 — проекту не нужна: внешний трафик и TLS закрывает Ingress (nginx) | Ориентирован на Django | Ядро на Rust, быстрее — выигрыш не используется, узкое место в задержке внешней модели | Следит за процессами и перезапускает упавшие; в Kubernetes это делает сам оркестратор |
| Итог | **Выбран** | Отклонён | Отклонён | Отклонён | Не нужен на MVP; может добавиться, если появится причина держать несколько процессов в одном контейнере |

Как запускается: на прототипе — `python -m uvicorn app.main:app` с режимом перезагрузки и подключёнными
каталогами (только для разработки); на MVP — без `--reload` и без подключённых каталогов, число процессов
задаётся репликами подов. Если нагрузка потребует несколько процессов в одном поде, у uvicorn для этого есть
параметр `--workers`; замера под такую нагрузку в проекте нет.

### Почему Kubernetes, а не другая оркестрация

Роль: платформа, на которой на MVP работают приложение, хранилища, модели поиска (на CPU, без GPU), задания по расписанию и наблюдаемость (17 компонентов,
25 подов в расчёте ADR-500 для базового варианта без GPU). Прототип работает на `docker compose` на одной машине; для MVP платформа **выбрана предварительно и
владельцем инфраструктуры не согласована**. Известно и решено со слов заказчика проекта (24 сентября 2026, документально не подтверждено): личный кабинет водоканала уже работает на
Kubernetes; наш контур **размещается в том же кластере**; **эксплуатирует тот же отдел**; **GPU-узлов в кластере нет**. Не известно: дистрибутив и
версия, квоты и правила размещения, классы хранилищ и быстрые диски, как настроено шифрование Kubernetes Secrets (etcd) и права на них, какие источники данных подключены к Grafana владельца (Prometheus, Zabbix, Loki), сетевые правила.

**Требования проекта, из которых следует выбор**

1. **Отказоустойчивость.** Потеря одного сервера не останавливает ответы: не менее двух реплик Backend и шлюза, запас на потерю одного узла (ADR-500, «Расчётный sizing»).
2. **Обновление без остановки** и возврат на предыдущую версию при сбое.
3. **Закрытый контур.** Кластер внутри сети водоканала, без управляемых облачных сервисов (ADR-400, C4_Deployment): требование владельца, мной не перепроверялось.
4. **Разные типы узлов.** Пул без ускорителей и пул данных с быстрыми дисками; GPU-узел под эмбеддинги и реранкер в кластере отсутствует (см. «GPU» ниже), если
   по итогам замеров и оценки MVP будет принято решение о собственной модели, понадобился бы ещё узел под неё (ADR-500).
5. **Задания по расписанию** с ограничением по времени: ночная индексация в окне 02:00–05:00, ночной прогон судьи качества.
6. **Секреты вне образа и вне репозитория.** Kubernetes Secret, который создаёт pipeline из GitLab CI variables (ADR-400). Это выполнимо и без Kubernetes (переменные окружения контейнера), поэтому требование выбор платформы не обосновывает.
7. **Хранилища с состоянием** (Qdrant, Postgres, Redis, MinIO) на постоянных томах и наблюдаемость: Zabbix и Grafana владельца (Jaeger — по необходимости); Prometheus и Loki — верхняя оценка до ответа владельца (ADR-200, «Наблюдаемость: Grafana владельца и источники данных»).

**Что нагрузка не требует.** Расчётная нагрузка невелика: одновременно обрабатывается не более 0,17 запроса на MVP и 2 на тираже (`Capacity_Report.html`),
а запросы всех 25 подов базового варианта без GPU — 14,55 vCPU и 22,95 ГиБ (лист «Sizing»). Это помещается на один сервер. Kubernetes выбирается **не ради пропускной способности,
а ради требований 1–5 и 7 (требование 6 выполнимо и без Kubernetes)**; если бы требований 1–2 не было, хватило бы `docker compose` на одном сервере.

| Критерий | `docker compose` на одном сервере (прототип) | `docker compose` на нескольких хостах без оркестратора | Docker Swarm | Nomad (HashiCorp) | Kubernetes на своём железе (выбрано предварительно) |
|---|---|---|---|---|---|
| Переживает отказ сервера | Нет | Нет: сервисы не переезжают сами | Да (по документации) | Да (по документации) | Да (по документации) |
| Обновление без остановки | Нет | Нет | Есть скользящее обновление (по документации) | Есть (по документации) | Есть (по документации) |
| Пулы узлов и планирование GPU | Один сервер | Вручную | Ограниченно: ресурсы описываются вручную (по документации) | Плагины устройств, GPU поддерживаются (по документации) | Плагин устройств и метки узлов — штатный путь для GPU (по документации) |
| Задания по расписанию с лимитом времени | Внешний cron | Внешний cron | Нет отдельного объекта, нужен внешний планировщик (по документации) | Периодические и пакетные задания есть (по документации) | `CronJob` |
| Секреты вне образа и репозитория (целевой механизм — GitLab CI variables, ADR-400) | Переменные окружения из pipeline | То же | Docker secrets (по документации) | Переменные Nomad или интеграция с Vault (по документации) | Kubernetes Secret, создаваемый pipeline |
| Наблюдаемость | Отдельно | Отдельно | Отдельно | Отдельно | Готовые связки и метрики кластера (по документации) |
| Сложность эксплуатации | Минимальная | Высокая: всё вручную | Ниже, чем у Kubernetes | Ниже, чем у Kubernetes | **Высокая**: отдельная эксплуатационная компетенция |
| Экосистема и кадры | — | — | Развитие замедлено (по общим сведениям, не проверялось) | Меньше, чем у Kubernetes; условия лицензии HashiCorp изменились в 2023 (по общим сведениям, для РФ не проверено) | Самая широкая |
| Итог | Только прототип | Отклонён | Не выбран: слабее по GPU и заданиям, меньше сообщество | **Реальная альтернатива**, если эксплуатационной команды нет: проще Kubernetes; в проекте не рассматривалась подробно | **Предварительно выбран** |

Сравнение сделано по документации проектов и требованиям проекта; **ни один из вариантов, кроме `docker compose`, в проекте не разворачивался**.

**Цена решения**

- **Накладные расходы кластера.** Системные поды Kubernetes (планировщик, сеть, DNS, метрики) занимают порядка 1 vCPU и 2 ГиБ на узел — допущение, не замер.
  На шести узлах MVP это ≈6 vCPU и ≈12 ГиБ против 14,55 vCPU и 22,95 ГиБ приложения (базовый вариант без GPU), то есть ≈41% по процессору и ≈52% по памяти к нагрузке приложения.
- **Управляющая часть кластера.** Для отказоустойчивости нужны три узла с `etcd`. В расчёте узлов (ADR-500) отдельные узлы под неё не заложены:
  принято, что она размещена на трёх узлах общего пула (допущение); при отдельных узлах их станет на три больше. Так как контур размещается **в
  кластере владельца**, управляющая часть и системные поды общие, число заказываемых узлов меньше расчёта (расчёт для отдельного кластера — верхняя
  оценка), а вопрос переходит в договорённость: квоты, отдельное пространство имён, тома, сетевые правила.
- **Эксплуатация.** Кластер эксплуатирует отдел владельца, который уже ведёт личный кабинет: отдельной команды и обучения под Kubernetes не нужно, это
  снимает главный риск решения. Остаются: правила размещения (квоты, пространство имён, сеть), порядок обновлений и заявок, нагрузка на отдел; GPU
  им эксплуатировать пока не приходилось.
- **GPU.** В кластере GPU-узлов нет, и это меняет план моделей. **Решение о GPU-узле принимается владельцем только после проверки на реальных данных** (решение заказчика проекта, 24 сентября 2026); порядок и критерий — ADR-600, «GPU-узел в кластере владельца — решение после проверки на реальных данных». До этого решения план — вариант 1; вариант 2 остаётся открытым. Аренда GPU на месяц берётся, только если не подойдёт «CPU + YandexGPT» (ADR-600); покупка — по итогам аренды:
  1. **Без GPU (базовый расчёт).** Эмбеддинги `multilingual-e5-small` на CPU (36–37 мс на запрос, замер), реранкер выключен, индексация считает
     эмбеддинги на CPU. Ограничение: BGE-M3 (300 мс) и реранкер (7,2 с на 20 кандидатов) в бюджет 100 мс на CPU не укладываются; допустимы только
     модели, укладывающиеся в бюджет на CPU (e5-base и e5-large не измерялись).
  2. **С GPU-узлом.** Покупка сервера с видеокартой и подключение к общему кластеру: драйверы, плагин устройств, метки и запреты размещения, квоты,
     согласование с отделом. Цена и сроки не оценены; работа — 3–5 чел-дн по оценке автора (ADR-600, вне итога). Тогда возможны BGE-M3 и реранкер.
- **Подготовка платформы.** Работа на разворачивание кластера, Ingress, сертификатов и хранилищ заложена в матрицу как оценка автора (10–15 чел-дн, строка
  «Оркестрация контейнеров»); разбивки под неё нет.

**Дистрибутив и версию задаёт кластер владельца** — какой именно, неизвестно; выбирать его нам не нужно, но манифесты и требования (Ingress,
классы хранилищ, `CronJob`, `Secret`) должны с ним совпасть. Требования по отечественному программному обеспечению неизвестны.

**План Б почти неактуален:** платформа у владельца уже есть и общая. Возвращаться к Nomad или `docker compose` имеет смысл, только если владелец
откажет в размещении в своём кластере.

**Когда пересмотреть.** До начала работ MVP: владелец ответил на вопросы о квотах, сети, шифровании Secrets и наблюдаемости или отказал в размещении; решил вопрос
о GPU-узле. После MVP: стоимость эксплуатации оказалась выше ожидаемой либо нагрузка на отдел владельца недопустима.

**Что здесь не подтверждено.** Все возможности платформ описаны по документации; накладные расходы на узел — допущение; то, что кабинет работает на
Kubernetes, что мы размещаемся в его кластере и что GPU там нет, известно со слов и документом не подтверждено; дистрибутив, квоты и
наблюдаемость кластера неизвестны (секреты: целевой механизм — GitLab CI variables, тоже со слов, ADR-400; настройки GitLab, шифрование Kubernetes Secrets
и права на них в кластере неизвестны); отечественные ограничения на программное обеспечение неизвестны.

### Агент записи или самописный модуль: решение после анализа обращений (не принято)

**Решение заказчика проекта (25 сентября 2026):** после анализа всех обращений абонентов делается вывод, сколько разных типов запросов на запись существует, и только тогда принимается решение —
нужен ли отдельный агент (оркестратор записи) или достаточно отдельного самописного модуля. До этого форма компонента записи не выбрана.

**Как сейчас.** Путь записи (регистрация обращения) — самописный модуль внутри Backend (`app/backend/registration.py`) на автомате `app/fsm.py`: черновик, явное подтверждение, запись, номер, аудит отказа.
В схемах Этапа 2 он показан как отдельный контейнер `agent-orchestrator`; **сравнения «внутри Backend или отдельно» в проекте не проводилось.**

| Вариант | Что это | Изоляция прав записи | Что стоит | Когда подходит |
|---|---|---|---|---|
| А. Самописный модуль в Backend | Продолжение `registration.py`: адаптеры Биллинга, Диспетчерской и микросервиса обращений как модули; автомат `app/fsm.py` | Слабее: права записи в одном процессе с контуром вопросов; защита — проверки в коде и подтверждение абонента | Меньше всего: нет нового контейнера и межсервисных вызовов | Мало типов записи, сценарии линейные и похожи друг на друга |
| Б. Отдельный контейнер `agent-orchestrator` (текущий дизайн схем) | Собственный автомат, Draft Validator, Audit Writer, адаптеры; вызывается Backend по внутреннему API | Сильнее: права записи и проверка привязки сессии — в отдельном контейнере, вне пути, который общается с моделью | Контейнер, межсервисный вызов, свой выпуск; оценка Этапа 2 «4–6 недель» посчитана для этого варианта | Типов записи много или они различаются; ИБ требует отделить права записи |
| В. Агент на LangGraph | Модель выбирает шаги и инструменты; контрольные точки, ветвления | Как Б, плюс зависимость | Оценка 5–24 чел-дн сверх Этапа 2 (ADR-600) | Срабатывает любой из четырёх триггеров LangGraph (раздел ниже) |

**Что считается при анализе обращений.** Для каждого типа запроса на запись — а это обращения из закрытого перечня регистрируемых (`REGISTRABLE`, сегодня 7 типов), передача показаний, аварийная заявка и то, что найдёт анализ:
целевая система и протокол; число шагов; обязательные условия и ветвления; ожидание внешнего события дольше 30 минут; нужна ли параллельность; доля диалогов с записью (допущение A6: 15–40%); цена ошибки. Итог — число типов записи и их сценарии.

**Как принимается решение.** В — если срабатывает любой из четырёх триггеров LangGraph (ниже). Б — если ИБ требует изоляции прав записи от контура вопросов либо типов записи много и сценарии различаются; иначе А.
**Пороги («сколько — много») не заданы**: они выбираются при анализе вместе с владельцем — по данным, которых у проекта пока нет. Анализ — ADR-600, «Анализ типов обращений на живых данных».

**До решения.** Блок «Агент операций записи» на схемах и в расчёте узлов остаётся как верхняя оценка (вариант Б: 2 пода по 0,25 vCPU); оценка «Основная разработка Этапа 2» (4–6 недель) относится к варианту Б,
при варианте А ожидается ниже (не оценено). Решение об агенте не блокирует Этап 1.

### Оркестрация агента: собственная таблица переходов, LangGraph — по триггеру

Роль: конечный автомат обращения на пути записи (Этап 2): подтверждение абонента, идемпотентность, аудит, отказ абонента.
Главное требование — **запись возможна ровно из одного состояния «ожидает подтверждения»**, и механизм подтверждения один.

**Что есть.** Собственная таблица переходов на перечислениях (`app/fsm.py`, 200 строк): 7 состояний, 10 переходов, 7 событий;
тесты (`tests/unit/test_fsm.py`, 251 строка) перебирают **все** остальные состояния и проверяют, что из них запись невозможна.
Внешних зависимостей у модуля нет. LangGraph рассматривался как кандидат с самого начала проекта, но ни разу не подключался и не
пробовался; сравнение ниже сделано по документации и по требованиям проекта, кроме замеров, отмеченных отдельно.

| Критерий | Собственная таблица (выбрано) | Библиотека `transitions` (была на первом прототипе) | LangGraph (`StateGraph`, `interrupt_before`) |
|---|---|---|---|
| Запись только из состояния подтверждения, механизм один | Да: таблица и перебор всех состояний в тестах | Да, но переходы настраиваются **строками**: пробел в ключе (`"trigger "`) давал молчаливое несрабатывание — дефект, на котором первый прототип уже ломался | Второй механизм: `interrupt_before` — ещё одно место, где решается, можно ли писать; либо заменяет проверку состояния, либо не используется (по документации) |
| Ветвление по выбору инструмента самим агентом | Нужно писать самим | Нет | Из коробки (по документации, не пробовалось) |
| Контрольные точки состояния, переживающие перезапуск | Нет; состояние диалога хранит `SessionStore` в Redis, на прототипе Redis без сохранения (`--save "" --appendonly no`) | Нет | Есть (по документации, не пробовалось) |
| Параллельные ветви | Нет | Нет | Есть (по документации) |
| Вес зависимости | 0 | Небольшой | **Замер:** импорт `langgraph.graph` — +34 МиБ ОЗУ и 2,3 с (при контейнере Backend 1,32 ГиБ — 2,6%); 36,5 МиБ на диске со всеми зависимостями, из них ≈18 МиБ не входят в зависимости проекта |
| Итог | **Выбрано для Этапа 2** | Отклонено: возвращает дефект строковых ключей, ничего не даёт сверх таблицы | **Кандидат по триггеру:** не выбран, потому что возможности, ради которых он нужен, на текущем сценарии проверить нечем |

Другие движки (например, движки надёжного исполнения вроде Temporal) не рассматривались и не сравнивались.

**Триггеры перехода на LangGraph — точные условия.** Переход выполняется, когда наступает **одно из четырёх событий** ниже. Три первых —
свойства утверждённого сценария Этапа 2, которые можно показать документом; четвёртое — измеренная доля реплик абонентов. Ощущение сложности
не является событием. Решение о переходе
принимает владелец архитектуры проекта (тот, кто утверждает ADR); ответ и доказательство записываются в журнал ниже.

| № | Что должно случиться | Как установить (доказательство) | Порог | Что триггером **не** считается |
|---|---|---|---|---|
| 1 | В сценарии **следующий шаг после вызова инструмента выбирает модель**, а не таблица: модель получает результат инструмента и сама решает, вызывать ли другой инструмент и какой (function-calling) | Описание сценария: диаграмма шагов с развилкой «решает модель» и не менее чем двумя инструментами на выбор (Биллинг, обращения, Диспетчерская) | Развилка есть. Дополнительное условие запуска такого сценария: тест function-calling у выбранного провайдера на ≥30 размеченных запросах — верный инструмент и аргументы не менее чем в 90% случаев, неверных вызовов не более 5% (пороги — предложение автора, не расчёт; если тест не пройден, агентный сценарий не запускается, а упрощается до фиксированного) | Модель только формулирует текст ответа; выбор ветки по правилам (тип обращения, признак нажатой кнопки); фиксированная последовательность вопросов |
| 2 | В сценарии есть шаг, который **ждёт внешнее событие дольше срока жизни сессии** (сейчас 30 минут, `session_ttl_seconds = 1800`), и после ожидания диалог должен продолжиться **с того же шага и с тем же состоянием** | Требование в описании сценария: максимальное время ожидания шага (ответ микросервиса обращений, оператора, абонента) или явное требование «продолжить после закрытия вкладки или перезапуска» | Ожидание дольше 30 минут либо требование возобновления с шага | Перезапуск пода в пределах сессии — состояние хранит Redis (на MVP с сохранением); ожидание подтверждения абонентом в пределах 30 минут |
| 3 | В сценарии **два и более шага выполняются одновременно, у каждого свои повторы, таймауты и правило при ошибке, а результаты сливаются** в одно решение | Описание сценария: ветви, правило слияния и что делать, если одна ветвь упала | Не менее двух ветвей с разными правилами повтора или ошибки | Параллельные чтения для одного ответа без общего решения (Биллинг и график отключений через `asyncio.gather`) |
| 4 | **Доля реплик абонентов, в которых две и более задачи сразу, превысила 5%** (решение владельца архитектуры) | Разметка выборки реплик (правила и способ подсчёта ниже) | Доля > 5% по точечной оценке | Одна задача с уточнениями; несколько частей одного вопроса про одно и то же |

**Триггер 4 — как считать долю.** Задача — отдельный запрос из разных путей ответа или типов обращений (например, «долг» и «поверка»;
«отключение» и «тариф»; «заказать поверку» и «справку»). Реплика с двумя и более такими задачами считается многозадачной, в том числе с
условием («если долга нет, закажите поверку»). Считается **доля многозадачных реплик от всех содержательных реплик выборки** (приветствия и
реплики без вопроса не входят). Размечают двое, спорные случаи решает третий; каждая размеченная реплика получает число задач.

| Что | Как |
|---|---|
| Первое измерение | На выгрузке живых обращений (ADR-600, анализ типов на живых данных, не менее 1 500 текстов): при доле 5% погрешность около ±1,1 п.п. (95%, расчёт по формуле, не замер) |
| Повторные измерения | После запуска чата — раз в месяц по выборке не менее 400 реплик (±2,1 п.п.) либо по метрике детектора намерений, когда он появится |
| Решение | Точечная оценка выше 5% — триггер сработал, запись в журнал. Если выгрузка обращений (регистр по телефону и почте) и чат дают разные доли, решение принимается по каждому измерению отдельно и фиксируется |

Оговорки. Регистр обращений — письменные тексты, они длиннее и чаще многозадачны, чем реплики чата; поэтому первое измерение ориентировочное, а
измерение по чату после запуска обязательно. Ключевые слова дают верхнюю оценку: 31 из 136 настоящих обращений (23%) подходят под несколько
типов сразу, но пересечение слов не означает несколько задач; детектор намерений на тех же текстах находит их в 0,7% (ниже). LangGraph сам реплику на задачи не разбирает: разбор (детектор намерений) нужен в
любом случае, граф даёт цикл по очереди задач и условные ветви. Потеря части ответа и ложное «сделал» исправлены в таблице независимо от
перехода (см. «Как реализовано» ниже); доработка после прототипа — отдельный блок ADR-600.

**Как реализовано: несколько задач в одной реплике** (прототип, 24 сентября 2026; код — `app/agents/intents.py`, `app/backend/orchestrator.py`,
`app/gateway/guardrails.py`). Решено тремя частями в текущей таблице, без LangGraph.

1. **Детектор намерений** (`split_intents`) — правила, без модели. Реплика режется по жёстким границам (конец предложения, `;`, «а также»,
   «потом») и по мягким («и», запятая, «, а»); разрез принимается, только если каждая сторона несёт свою задачу: узнаётся тема ответа,
   тип обращения или операция, которую помощник не выполняет. Тип обращения считается отдельной задачей, только если фрагмент — просьба
   или вопрос либо просьба прозвучала раньше в том же предложении («Хочу заказать поверку и справку»). Реплики длиннее 300 знаков не
   разбираются: это письма, а не вопросы чата. Факты счёта различаются (`долг`, `поверка`, `показания`, `начисления`): ответчик счёта
   отвечает на один факт за раз, поэтому «долг и поверка» — две задачи. Условная часть («Если нет — закажите поверку») привязывается к
   предыдущей; проверяется единственное условие, которое можно проверить по Биллингу, — наличие долга.
2. **Ответ по каждой части** (`Orchestrator._stream_parts`): часть идёт тем же путём, что и отдельная реплика (ответчики, затем база знаний,
   затем модель), ответ выводится по очереди под заголовком «1. …», «2. …». Что не сделано, говорится прямо: операция без выполнения
   («передача показаний», «оплата», «изменение данных») — отказ с телефоном контакт-центра и фразой «я ничего не передавал»; условие не
   выполнено — «условие не выполнено, часть не выполняю»; условие не проверяется — «проверить не могу». Регистрация предлагается по
   **первой** подходящей части (в состоянии диалога один черновик), об остальных абонент предупреждён. Вопрос «Как передать показания?» —
   вопрос о порядке, а не просьба, и идёт в базу знаний. Метрика `assistant_multi_intent_total{tasks}` считает такие реплики.
3. **Запрет фраз о выполненном действии** — два рубежа. Первый: к запросу, идущему к модели, добавляется правило «ты только консультируешь,
   никогда не пиши, что выполнил операцию» (`NO_ACTIONS_RULE`). Второй: синхронная проверка ответа (`unperformed_action`) блокирует
   предложение, начинающееся с глагола первого лица в форме выполненного или выполняемого действия («Передал…», «Проверил…»,
   «Оформляю…»), и подставляет текст «Я не выполняю операции… это можно сделать в личном кабинете», а не нейтральную заглушку: абоненту
   важно узнать, что действие не выполнено. Третье лицо («Специалист проверил»), обращение к абоненту («Если вы передали»), приказ и
   инфинитив не блокируются.

**Результат на стенде** (те же реплики, что до реализации).

| Реплика | Было | Стало |
|---|---|---|
| «Какой у меня долг и когда поверка счётчика?» | Только про поверку | Два ответа: «задолженности нет» и «поверка до 15.06.2029» |
| «Когда отключат воду на моей улице и сколько стоит кубометр воды?» | Только про тариф | Два ответа: график отключений и тариф |
| «Хочу заказать поверку счётчика и справку об отсутствии задолженности» | Кнопка только на поверку, справка потеряна | Ответ по обеим частям; регистрация предложена по первой |
| «Передайте показания 1234 и скажите, нет ли долга. Если нет — закажите поверку» | Модель: «Передал показания 1234. Проверил наличие долга — его нет» | Отказ по показаниям, ответ про долг, условная часть по данным Биллинга |
| То же, счёт с долгом 1 250 ₽ | Условие проигнорировано | «Условие не выполнено: по счёту есть задолженность» |
| «Хочу передать показания 1234» | Ответ модели | Прямой отказ без вызова модели |

**Тесты:** 22 модульных случая на детектор (в том числе нерезаемые реплики и «как передать показания»), 16 на охранитель, 8 контрактных на
оркестратор (модель не вызывается там, где не нужна; условие про долг; правило в промпте).

**Измерение на выгрузке обращений** (`scripts/measure_intents.py`, 136 текстов, разметки числа задач людьми нет). Первая версия детектора
находила несколько задач в 14% текстов, но это были ложные срабатывания: длинные письма («Прошу вернуть переплату. Реквизиты прикрепляю»)
несут одну просьбу, а признак типа есть в каждой фразе. После ограничения длины, требования «просьба или вопрос» и исключения прошедшего
времени («оплатил») детектор находит две задачи в **1 из 136 текстов (0,7%)**. Оба числа — срабатывание детектора, не доля
многозадачных обращений: порог подбирался на этих же текстах, а полнота не измерена. Важно: 23% текстов, где ключевые слова подходят под
несколько типов сразу, — это пересечение слов, а не несколько задач; ожидание, что доля превысит 5%, по этим данным не подтверждается. Решение по триггеру 4 принимается по разметке людьми (правила выше).

**Что не сделано и что не подтверждено.**
- Разбор идёт только на пути абонента (`stream`); `generate` для служебных вызовов остался прежним.
- Регистрация предлагается по одной задаче; несколько черновиков в состоянии диалога не хранятся.
- Из условий проверяется только наличие долга; остальные («если скоро», «если дорого») не выполняются, и об этом сказано.
- Предел 300 знаков, перечни глаголов и операций выбраны автором и подобраны на 136 обращениях регистра, не на репликах чата. Точность и полнота
  детектора на размеченных репликах чата не измерены.
- Охранитель ловит только начало предложения с глаголом первого лица; «Уже всё передано» или фраза в середине предложения не ловится.
  Частота ложных блокировок на живых ответах неизвестна (ADR-600, блок Guardrails).
- Ответчик бланка ведёт диалог по шагам в состоянии сессии: если бланк запрашивают сразу в двух частях, второй перезапишет первый.
- Каждая часть, идущая к модели, считается в лимитах шлюза отдельным запросом; части отвечаются последовательно, задержка складывается.

**Сигналы — не триггеры.** Они не ведут к автоматическому переходу, а вызывают пересмотр решения владельцем архитектуры: автомат вырос
заметно выше 15–20 состояний и ветвление разошлось по коду (порог — ориентир автора); нужно воспроизводить шаги диалога для разбора инцидента.

**Когда проверяется — четыре момента, по календарю проверка не проводится.**

1. **При постановке сценария или блока Этапа 2 в разработку, до проектирования.** Владелец отвечает «да» или «нет» по каждому из трёх
   триггеров по описанию сценария и записывает ответ с доказательством в журнал.
2. **При изменении сценария** (добавлен шаг, изменено ожидание или ветвление): ответ даётся заново.
3. **При пересмотре архитектуры по метрикам Этапа 1** — условие перехода к Этапу 2 (ADR-600).
4. **После каждого измерения доли многозадачных реплик** (триггер 4): первое — при анализе живых обращений, дальше — ежемесячно после запуска чата.

**Что должно произойти после ответа «да».**

1. Запись в журнал: дата, сценарий, номер триггера, доказательство.
2. Решение владельца: переходить или упростить сценарий так, чтобы триггер не наступал. Для триггера 1 предварительно проведён тест
   function-calling; если он не пройден, сценарий не запускается на агенте.
3. **До написания кода сценария** выполняется миграция (ADR-600, «Переход агента на LangGraph — по триггеру»): граф с тем же составом переходов.
4. Миграция считается выполненной, когда одновременно верно: тесты, перебирающие все состояния, проходят на графе; из любого состояния,
   кроме подтверждения, запись невозможна; при включённой библиотеке нет исходящих соединений на внешние адреса (проверка сетевым
   наблюдением, для 152-ФЗ); ИБ согласовала зависимость.
5. Только после этого сценарий реализуется на графе. Если триггер обнаружен, когда сценарий уже написан на таблице, миграция
   становится критическим путём — это риск позднего обнаружения, потому проверка стоит до проектирования.

**Журнал проверки триггеров.**

| Дата | Сценарий Этапа 2 или измерение | Триггер 1 | Триггер 2 | Триггер 3 | Триггер 4 (доля > 5%) | Доказательство | Решение |
|---|---|---|---|---|---|---|---|
| — | Ни один сценарий Этапа 2 не поставлен в разработку, доля многозадачных реплик не измерена | — | — | — | — | — | Остаётся собственная таблица |

**В каком случае LangGraph нужен — подробно.** LangGraph — библиотека для сценариев, где путь по шагам не задан заранее, а состояние
надо хранить между шагами. На пути записи Этапа 2 проекта сейчас всё наоборот: сценарий фиксирован (черновик → подтверждение →
запись), состояние живёт в рамках одной сессии (30 минут), и вся сложность — в одном правиле подтверждения, которое таблица
закрывает лучше. Поэтому решение принимается по признакам ниже, а не по дате; проверяется при планировании каждого блока Этапа 2.

| Ситуация в проекте | Нужен ли LangGraph | Почему |
|---|---|---|
| Абонент заполняет заявку **фиксированными вопросами** (сейчас так: сбор без модели), подтверждает, запись | **Нет** | Путь известен заранее; таблица переходов с 7 состояниями справляется |
| Одно подтверждение, одна запись, отказ абонента пишется в аудит | **Нет** | Это и есть таблица: запись из одного состояния, перебор всех остальных в тестах |
| Модель сама решает, что делать дальше: в одной реплике абонента «передайте показания и проверьте долг» → агент выбирает инструменты (показания, задолженность, регистрация обращения, аварийная заявка) и может вызвать несколько подряд | **Да — триггер 1** | Нужен цикл «модель → выбор инструмента → результат → модель», ограничение числа шагов, обработка неверного вызова инструмента. Без библиотеки это пишется вручную, и результат — собственный, менее проверенный LangGraph. Решение зависит от качества function-calling у провайдеров РФ: оценка (≈1 неделя) стоит в плане перехода к тиражу и не проведена |
| Шаг **ждёт внешнее событие** дольше сессии: ответ микросервиса обращений, подтверждение оператором, срок в часы или сутки; за это время под перезапускается или сессия истекла | **Да — триггер 2** | Нужно вернуться на тот же шаг с тем же состоянием (контрольные точки). Для простого сохранения диалога после перезапуска достаточно Redis с сохранением, который уже заложен на MVP |
| Несколько шагов идут **одновременно и сходятся**: запрос к Биллингу, Диспетчерской и графику отключений, каждый со своими повторами и ошибками, результат объединяется | **Только если ветви имеют своё состояние**; простые параллельные запросы делаются средствами языка без библиотеки | Параллельные вызовы без общего состояния решает `asyncio.gather`; LangGraph нужен, когда у каждой ветви свои повторы, ошибки и правила слияния (триггер 3) |
| Автомат разросся: заметно больше 15–20 состояний, ветвление размазано по коду, переходы трудно показать аудитору | **Сигнал, не триггер** | Ориентир автора, не расчёт: граф даёт визуализацию и единое описание переходов; таблица при таком размере читается хуже |
| Нужно **воспроизвести шаги** прошедшего диалога для разбора инцидента или аудита | **Сигнал, не триггер** | Контрольные точки хранят историю шагов; аудит-лог проекта уже пишет кто, что, когда и ключ идемпотентности |
| Нужен человек в цикле для **записи** (HITL) | **Нет** | Подтверждение записи уже есть в таблице; `interrupt_before` создал бы второй механизм подтверждения |

**Что нужно сделать до решения.** 1) Для агентного сценария провести тест function-calling у GigaChat Pro и YandexGPT (порог — в
триггере 1). В плане ADR-600 оценка function-calling (≈1 неделя) стоит в таблице перехода к тиражу, а для триггера 1 она нужна раньше —
перед запуском агентного сценария Этапа 2: несоответствие момента не устранено, перенос строки решает владелец плана. 2) Для каждого
сценария Этапа 2 ответить на три вопроса триггеров и записать ответ в журнал. 3) Проверить, что библиотека не отправляет
телеметрию за периметр: в составе LangGraph идёт `langsmith` (трассировка во внешний сервис), по документации она включается
настройкой, но в проекте это не проверялось, а для 152-ФЗ это условие миграции.

**Чего стоит переход.** Дополнительные работы — в ADR-600 («Переход агента на LangGraph — по триггеру», от 7 до 24 чел-дн в зависимости от того, какие триггеры сработали, оценка автора) и в
матрице (строка «Переход агента на LangGraph (по триггеру)»; в итог сметы не входит).

**Что переход не отменяет.** Таблица переходов остаётся формулировкой правила подтверждения, меняется только исполнитель;
тесты, перебирающие все состояния, сохраняются; второй механизм подтверждения не заводится.

**Что здесь не подтверждено.** Довод «усложнение ради возможностей, которые пока не нужны» проверить нечем, пока нет агентного
сценария. Возможности LangGraph описаны по документации и в проекте не пробовались. Вес зависимости замерен и невелик: аргументом
против перехода он не является (в прежней редакции решения он назывался «тяжёлым» без замера). Триггер 1 в плане Этапа 2 (ADR-600)
может наступить уже при выборе сценария; оценка перехода (7–24 чел-дн по триггерам) заложена в ADR-600 отдельно и в итог не входит.

## Хранилища

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Векторный поиск (целевое) | Qdrant, self-hosted, в Kubernetes | MVP | pgvector (вполне подошёл бы на малом объёме; Qdrant выбран из-за алиасов коллекций и отдельной нагрузки от основной базы, см. раздел «Почему Qdrant»); Milvus (набор компонентов тяжелее при том же результате, по документации) | — |
| Векторный поиск (прототип, по умолчанию) | Корпус в памяти процесса | прототип | Qdrant с первого дня — избыточен на 20 парах FAQ | — |
| Векторный поиск (прототип, опционально) | Qdrant через `docker compose`, та же модель эмбеддингов | прототип | Реализовано и проверено на реальном сервере (`app/kb/qdrant_store.py`) — переключение одной переменной; payload-фильтр по `inquiry_type` и blue-green переиндексация (алиас, атомарное переключение) тоже реализованы и проверены. От целевого контура отличает только K8s (docker compose вместо кластера) | — |
| Состояние сессии, квоты | Redis | прототип и далее | In-memory без Redis (fail-open) — отклонено: теряет квоты при рестарте | — |
| Телеметрия | Postgres, отдельная база `telemetry` (`usage_events`, `quality_reports`) | прототип, MVP и тираж | ClickHouse (объём не оправдывает при хранении событий до 12 месяцев: до 4 млн строк в год на тираже в максимуме, замер до 12 млн строк — раздел «Телеметрия: почему Postgres, а не ClickHouse»); TimescaleDB, OpenSearch | — |
| Слепки внешних систем (прототип) | Postgres (три базы: Биллинг, ЛК, обращения) | прототип | — | `docker-compose.yml` |

## LLM-хостинг

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Стратегия хостинга генерации | Гибрид: управляемый API по умолчанию + единый шлюз (LiteLLM) + self-hosted — вариант тиража, решение после замеров и оценки MVP | прототип → MVP → тираж | Только SaaS весь жизненный цикл (риск вендор-лока и роста цены без права уйти); только self-hosted с начала (CAPEX на GPU до подтверждения ценности продукта) | — |
| Шлюз к модели | LiteLLM (по документации библиотеки — 100+ адаптеров, единый OpenAI-совместимый интерфейс; в проекте настроены повторы `num_retries: 2` и цепочки запасных провайдеров, circuit breaker — возможность библиотеки, не настроена) | прототип и далее | Свой клиент на каждого провайдера — дублирование ради контроля, который LiteLLM уже даёт | — |

### Как устроен шлюз к модели

Шлюз — модуль Python внутри контейнера Backend (`app/gateway/`). Порядок работы: проверка лимитов (Redis) →
обезличивание (Presidio, русская модель spaCy и собственные распознаватели — ADR-400) → вызов модели через
LiteLLM → проверка ответа на потоке → запись события использования (Postgres)
и метрики.

**LiteLLM подключён библиотекой (`Router`) внутри процесса, а не отдельным прокси-сервером.** Прокси — второй
процесс со своим портом, конфигурацией и жизненным циклом; на прототипе с 15 ГБ памяти ему тесно, а библиотека
принимает тот же список моделей, цепочки запасных провайдеров, число повторов и таймаут из
`litellm_config.yaml`. Формат файла совместим с прокси, поэтому вынос шлюза в отдельный контейнер на MVP — смена
способа запуска, а не переписывание кода. Провайдер выбирается переменной окружения `LLM_PROVIDER`
(`local-test`, `yandexgpt`, `gigachat`); YandexGPT вызывается через OpenAI-совместимый адрес.

**Почему Python и LiteLLM, а не собственный код.** Весь нужный инструментарий (LiteLLM, Presidio,
sentence-transformers) существует в Python — том же языке, что у Backend. Свой клиент на каждого провайдера
означал бы дублирование ради контроля, который LiteLLM уже даёт: единый интерфейс, повторы, переключение на запасного провайдера при сбое (circuit breaker библиотека тоже умеет, в проекте не настроен).
Переключение на запасного проверено на живом трафике: в таблице `usage_events` шесть вызовов ушли на локальную модель, когда ключ YandexGPT был пуст.

## Секреты и конфигурация

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Секреты на разработке | Переменные окружения оболочки (`env.example.sh` — справочник, не файл для чтения приложением), опционально Vault dev-mode | прототип | Файл `.env` на диске — отклонено: секрет в открытом виде на диске лишний, когда есть переменные окружения; GitHub Codespaces — опробован и отклонён отдельно: базовая машина (32 ГБ диска) не выдержала полного стека (Ollama + эмбеддинги + torch); локальная машина мощнее и уже рабочая | — |
| Секреты в проде (целевое) | GitLab CI/CD variables (masked, protected), деплой через pipeline, Kubernetes Secret (решение заказчика проекта, 24 сентября 2026) | MVP | Vault Agent Injector, External Secrets Operator / Vault CSI, SOPS / Sealed Secrets, Kubernetes Secret вручную — сравнение ниже; Vault остаётся вариантом по условиям пересмотра (ADR-400) | ADR-200, «Секреты в проде: GitLab CI variables и альтернативы»; ADR-400 |

## Сравнение вариантов по слоям

Здесь собраны сравнения выбора по слоям, для которых в таблицах выше указана только строка «выбор — отклонённые
альтернативы». Как и в разделах про FastAPI и uvicorn, сравнение сделано **по документации проектов и требованиям
проекта**; замеров нагрузки в нём нет, кроме тех, что отмечены в ADR-300 и `golden_set/`. Строка «Итог» в каждой
таблице — решение проекта. Где решение ещё не принято, так и сказано.

### Интерфейс абонента: виджет

Требования: виджет встраивается в личный кабинет; принимает ответ потоком; идентификатор абонента будет приходить из
SSO; на демонстрации не должно быть второго процесса.

| Критерий | Статичные HTML / JS / CSS (прототип) | React + TypeScript (целевой) | Vue или Angular |
|---|---|---|---|
| Сборка и Node.js на стенде | Не нужны | Нужны | Нужны |
| Проверка архитектуры | Проверяет то, что важно (контракт API, поток, подтверждение), больше ничего не добавляет | Та же проверка плюс лишний слой сборки | То же |
| Встраивание в личный кабинет | Не рассматривается: стенд отдельный | Указан в целевой схеме (`C4_Deployment.html`); совместимость со стеком ЛК не проверена — владелец стек не назвал | Зависит от стека ЛК, который неизвестен |
| Типы для контракта API | Нет | TypeScript позволяет описать контракт типами | Возможно в обоих |
| Итог | **Выбран для прототипа** | **Целевой выбор для MVP, предварительный** | Не сравнивались: выбор фреймворка зависит от стека ЛК |

Что не решено: фреймворк виджета на MVP подтверждается после того, как владелец назовёт стек личного кабинета.

### Протокол потока ответа: SSE

Требование: ответ абонента идёт потоком (ADR-100). Данные идут в одну сторону — от сервера к виджету.

| Критерий | SSE (поток событий по HTTP) | WebSocket | Периодический опрос (long polling) |
|---|---|---|---|
| Направление | Сервер → клиент — как нужно | Двустороннее — не нужно | Клиент опрашивает сервер |
| Прохождение через Ingress и прокси | Обычный HTTP | Требует поддержки смены протокола | Обычный HTTP |
| Задержка первого куска | Сразу | Сразу | До интервала опроса |
| Сложность | Текстовый формат, легко читать и тестировать | Выше: состояние соединения, переподключение | Ниже, но лишняя нагрузка запросами |
| Итог | **Выбран** | Отклонён | Отклонён |

Особенность: встроенный в браузер `EventSource` умеет только GET-запросы, а вопрос абонента отправляется POST-ом.
Поэтому виджет читает поток через `fetch` и разбирает события сам; на стороне сервера события формируются на
`StreamingResponse` (Starlette), а не отдельной библиотекой.

### Почему PostgreSQL и Redis

Требования к базе данных: транзакции и точная арифметика; общая база для нескольких сервисов; JSON; зрелый драйвер для
Python. (Материализованные представления входили в требования, пока профиль абонента предрасчитывался; с решением 25
сентября 2026 читать данные онлайн они не нужны и на выбор не влияют.)

| Критерий | PostgreSQL | MySQL / MariaDB | SQLite |
|---|---|---|---|
| Несколько сервисов и реплик работают с одной базой | Да | Да | Нет: один файл, одна запись за раз |
| JSON и сложные типы | JSONB | JSON, возможности скромнее | Ограниченно |
| Драйвер и инструменты в стеке проекта | psycopg 3 | Есть | Встроен |
| Итог | **Выбран** | Отклонён: нечего добавить к PostgreSQL | Отклонён: не подходит для нескольких сервисов |

Требования к хранилищу счётчиков и состоянию диалога: атомарный счётчик с окном времени; общее состояние для
нескольких реплик; быстрый доступ; отказ хранилища не должен пропускать запросы без учёта (ADR-400).

| Критерий | Redis | Memcached | PostgreSQL (таблица счётчиков) | Счётчики в памяти процесса |
|---|---|---|---|---|
| Атомарный счётчик с временем жизни | `INCR` + `EXPIRE` | `incr` и время жизни есть | Строка и блокировки; окно с истечением пишется вручную | Есть, но только в одном процессе |
| Общее состояние для нескольких реплик | Да | Да | Да | Нет |
| Структуры данных для сессии (хеши, списки) | Есть | Только ключ-значение | Есть | — |
| Сохранение при перезапуске | Настраивается | Нет | Да | Нет: лимиты обнуляются |
| Итог | **Выбран** | Отклонён: нет структур для сессии | Отклонён не по нагрузке: при пике до 0,66 вопроса в секунду на тираже в максимуме прогноза (ADR-500) Postgres справился бы; счётчик с окном пришлось бы писать вручную, а Redis уже нужен для сессий | Отклонён: теряет лимиты (fail-open) |

### Почему Qdrant, а не другая векторная база

Требования: фильтр по типу обращения одновременно с векторным поиском; переиндексация без остановки поиска; один
сервис в эксплуатации; работа в закрытом контуре.

**Какой объём заложен.** База знаний MVP — **несколько тысяч документов** (решение по проекту: 3 000–5 000). Документ
режется на части (до 300 знаков), каждая индексируется своим вектором. Сколько частей на документ — **допущение**:
10–20 (на реальных документах не измерялось). Отсюда **30 000–100 000 векторов**. Размерность целевой модели BGE-M3 —
1 024 числа по 4 байта: 123–410 МБ одних векторов, без индекса и payload. Замер на 100 000 таких векторов с индексом по типу: 477 МБ на диске, ОЗУ 527 МиБ (718 МиБ при загрузке). На прототипе для сравнения — 190 фрагментов
размерности 384 (20 пар FAQ и примеры классификатора). Замер по вариантам ниже сделан только для Qdrant (см. после таблицы);
pgvector, Milvus и OpenSearch не измерялись, их сравнение — по документации.

| Критерий | Qdrant | pgvector | Milvus | OpenSearch (векторный поиск) |
|---|---|---|---|---|
| Фильтр по payload вместе с поиском | Встроен, есть индексы по полям | Через SQL `WHERE`. На 30–100 тыс. векторов размерности 1 024 точный перебор читает сотни мегабайт на запрос (по оценке, не замер) — нужен приближённый индекс; фильтр вместе с ним в документации описан как источник неполной выдачи при выборочном фильтре (в новых версиях расширения смягчено) | Есть | Есть |
| Эксплуатация | Один сервис | Расширение к Postgres, отдельного сервиса нет | Набор компонентов, тяжелее | Кластер на JVM, тяжелее |
| Переключение индекса без остановки | Алиасы коллекций | Решается вручную | Алиасы | Алиасы индексов |
| Итог | **Выбран** | Не отклонён по качеству: рабочая альтернатива на нашем объёме, требует настройки индекса и проверки фильтра; не выбран из-за алиасов и разделения нагрузки | Не выбран: набор компонентов тяжелее без выигрыша на нашем объёме (по документации) | Не выбран: универсальный движок, векторный поиск — надстройка (по документации) |

**Замер Qdrant на верхней границе объёма.** 100 000 случайных векторов размерности 1 024, payload `inquiry_type` с индексом
(8 значений), Qdrant в docker на рабочей машине, запрос из Python по HTTP (REST через `qdrant-client`; см. «Замер протокола» ниже: у REST-пути разброс от 6 до 60 мс), топ-20, 300 запросов подряд: без фильтра
**P50 27,5 мс, P99 64,9 мс**; с фильтром по типу (1/8 точек) **P50 14,3 мс, P99 36,8 мс**. Оба значения укладываются в
бюджет поиска 100 мс. Оговорки: векторы случайные, а не эмбеддинги документов; одна машина, запросы не параллельные; в
конце загрузки около 2 000 векторов остались вне индекса (перебираются напрямую); задержка эмбеддера сюда не входит.

**Замер протокола: REST и gRPC (24 сентября 2026).** Docker compose на рабочей машине с Windows, запросы из контейнера backend, 100 000 точек размерности 384 (размерность e5-small), подряд, не параллельно, `qdrant-client` 1.19.1.
Без payload и с текстом в payload (≈250 знаков, как в боевом хранилище):

| Запрос | REST (`qdrant-client`) P50 / P99, мс | gRPC P50 / P99, мс |
|---|---|---|
| топ-20, без payload | 6,2 / 7,9 | 5,7 / 7,0 |
| топ-20, с payload | **60,1 / 68,7** | 8,4 / 11,2 |
| топ-100, без payload | **51,6 / 56,6** | 8,3 / 11,3 |
| топ-100, с payload | 15,5 / 40,1 | 12,6 / 19,5 |

Отдельно (без payload, топ-20): время поиска на самом сервере — 3,0 мс (поле `time` ответа); голый `httpx` по тому же HTTP — 5,3 мс. Выдача всех 100 000 точек (так работало хранилище: лимит — все точки коллекции): 1,3 с по REST и 3,4 с по gRPC.
На боевой коллекции прототипа (51 фрагмент, 190 точек, полный перебор с payload) наоборот: REST P50 13 мс, P99 17 мс; gRPC P50 26 мс, P99 38 мс — выдача всех точек по gRPC дороже. Выдача REST и gRPC на прототипе совпала в 60 из 60 запросов.

**Сравнение протоколов клиента Qdrant (итог).** По замеру выше; трафик в обоих случаях без TLS и без ключа API (ADR-400).

| Критерий | REST через `qdrant-client` (был) | gRPC (выбран) |
|---|---|---|
| Задержка, 100 000 точек, топ-20 с текстом | P50 60 мс, P99 69 мс | P50 8 мс, P99 11 мс |
| Разброс между сочетаниями лимита и текста | От 6 до 60 мс | 6–13 мс |
| Выдача большого числа точек (5 000, с текстом) | P50 80 мс | P50 258 мс |
| Прототип: полный перебор 190 точек | P50 13 мс | P50 26 мс |
| Порт | 6333 | 6334 (открыт в compose; в кластере нужны правила сети) |
| Зависимости | Уже есть | Уже есть (`grpcio` входит в `qdrant-client`) |
| Итог | Отклонён: непредсказуемый разброс; переключатель `QDRANT_PREFER_GRPC=false` оставлен | **Выбран (24 сентября 2026):** стабильная задержка на объёме MVP; на прототипе на ≈13 мс медленнее; замер повторить в кластере |

**Что из этого следует.** Довод «задержка HTTP — единицы миллисекунд» неверен для нашего кода: не сам протокол, а путь REST внутри `qdrant-client` даёт непредсказуемый разброс от ≈6 до ≈60 мс в зависимости от размера запроса и ответа (похоже на задержку TCP около 40 мс; причина
не доказана). gRPC стабилен: 6–13 мс во всех сочетаниях. Поэтому клиент переведён на gRPC (порт 6334, `qdrant_prefer_grpc`, переключатель `QDRANT_PREFER_GRPC=false` возвращает REST). На прототипе это
дороже (26 против 13 мс на полном переборе), на объёме MVP — предсказуемее, и P99 вдвое ниже. **Оговорки:** одна машина, случайные векторы, запросы не параллельные, платформа Windows и Docker; в кластере владельца на Linux цифры могут отличаться и должны быть повторены
(ADR-600, тест эмбеддингов).

**Лимит выдачи.** Раньше хранилище запрашивало у Qdrant все точки коллекции (точный перебор, оправданный на 190 точках). На объёме MVP это 1,3–3,4 с на запрос. Теперь: до 300 точек в коллекции — все точки (точный перебор), выше — `top_k × 10` точек
(`PARTS_HEADROOM`; значения выбраны по замеру ниже, на реальном корпусе не проверялись). Верх выдачи остаётся точным, потому что лучшая точка каждого фрагмента идёт первой; хвост из 20 кандидатов может оказаться короче, если несколько фрагментов заняли много первых точек.

**Значения допущений хранилища: сравнение (24 сентября 2026).** Два числа в `app/kb/qdrant_store.py` были допущениями; ниже их сравнение по замеру. Оговорки в конце.

**1. Граница точного перебора (`EXACT_SCAN_MAX_POINTS`).** До неё поиск берёт все точки коллекции, выше — ограничивает выдачу. Цена выдачи L точек с payload (текст ≈250 знаков), коллекция 10 000 точек, `qdrant-client`, P50 / P99, мс:

| L точек | REST | gRPC |
|---|---|---|
| 100 | 6,0 / 6,8 | 10,0 / 11,9 |
| 190 (прототип) | 7,7 / 9,3 | 14,1 / 19,1 |
| 300 | 8,8 / 10,1 | 18,7 / 28,2 |
| 500 | 12,1 / 14,5 | 28,8 / 36,6 |
| 1 000 | 19,8 / 64,7 | 53,5 / 99,4 |
| 2 000 | 36,4 / 55,6 | 105,9 / 149,5 |
| 5 000 | 80,4 / 132,2 | 258,3 / 318,1 |

Возврат точек стоит ≈0,05 мс на точку по gRPC и ≈0,016 мс по REST. **Выбрано 300** (было 500): по gRPC P50 ≈19 мс, то есть примерно пятая часть бюджета поиска 100 мс; 500 давало 29 мс (P99 37), 1 000 — 54 мс. Граница выбрана по условию «P50 не выше ≈20 мс» — это выбор автора, не расчёт.
Возможное улучшение (не делалось и не измерялось): запрашивать без payload и подгружать текст только для первых фрагментов, тогда цена выдачи почти не зависела бы от числа точек.

**2. Запас точек на фрагмент (`PARTS_HEADROOM`).** Выдача сворачивается до одной пары на фрагмент, а у фрагмента несколько точек; нужно запросить больше точек, чем фрагментов. Синтетический корпус: 50 000 точек размерности 384, тематические группы по 8 родственных фрагментов
(косинус между центрами ≈0,6, части фрагмента к центру ≈0,85), 40 запросов рядом с темой, 5, 10 и 20 точек на фрагмент, gRPC с payload. Эталон — выдача всех точек. «Вся группа» — все 8 родственных фрагментов в выдаче:

| Точек на фрагмент | H = 1 (20 точек) | H = 2 | H = 3 | H = 5 (100) | H = 10 (200) |
|---|---|---|---|---|---|
| 5 | вся группа 12 из 40 | 40 из 40 | 40 из 40 | 40 из 40 | 40 из 40 |
| 10 | 5 из 40 | 23 из 40 | 37 из 40 | 40 из 40 | 40 из 40 |
| 20 | 3 из 40 | 10 из 40 | 26 из 40 | **37 из 40** | 40 из 40 |
| P50 gRPC, мс | 4,7–6,7 | 5,7–6,4 | 6,5–6,8 | 7,4–7,5 | 9,6–10,2 |

Топ-3 совпал с полным перебором у 40 из 40 запросов при любых H и любом числе точек на фрагмент. **Выбрано 10** (было 5): при 20 точках на фрагмент (верхняя граница допущения «10–20 частей на документ») H = 5 терял часть группы у 3 запросов из 40, H = 10 — ни у одного; цена — около 2 мс.
Проверка шла на случайных векторах с заданной структурой близости, а не на настоящих эмбеддингах; на случайных векторах без групп сравнение хвоста выдачи бессмысленно (порядок шума не воспроизводится), поэтому такие результаты не приведены.

**Оговорки.** Синтетические данные; одна машина с Windows и Docker; запросы не параллельные; число точек на фрагмент и близость фрагментов реального корпуса не измерены; в кластере владельца замер надо повторить (ADR-600, тест эмбеддингов).

**Что это меняет и что нет.** Довод «pgvector не справляется с фильтрацией» в первой редакции раздела не был основан на
замере и убран. Qdrant выбран по другим причинам: переиндексация без остановки поиска через алиасы коллекций (уже
реализована: коллекции `kb_faq__v<время>`), отдельный сервис под векторную нагрузку, проверенное на прототипе подключение
(`VECTOR_STORE=qdrant`). Сравнение pgvector на том же объёме — замер, которого нет; при желании убрать отдельный сервис его
нужно сделать на выгрузке владельца.

Два частных решения:

- **Клиент по gRPC, а не по REST** (изменено 24 сентября 2026; прежний довод «задержка HTTP — единицы миллисекунд» замером не подтвердился, см. «Замер протокола» ниже). Порт 6334, переключатель `QDRANT_PREFER_GRPC`.
- **Переиндексация blue-green, а не на месте.** Обновление на месте оставляет интервалы с неполным индексом. Новая
  коллекция собирается рядом, алиас переключается атомарно; цена — двойное хранение на время сборки. Удалённые
  документы уходят при полной пересборке.

### Телеметрия: почему Postgres, а не ClickHouse

Требование: нарезка событий (токены, стоимость, исход вызова) по абоненту, типу, модели, не мешая основной базе.
**Решение: телеметрия остаётся в Postgres на всех этапах** (отдельная база `telemetry`, таблицы `usage_events` и
`quality_reports`) **с хранением подробных событий 12 месяцев и сводной таблицей по периодам**. ClickHouse из целевой схемы убран:
объём этого не требует, при условии хранения не дольше года.

**Объём — по числу абонентов.** Одно событие — один вызов модели (`usage_events`, `app/gateway/usage.py`). Спрос — минимальный и
максимальный сценарии `Capacity_Report.html` (не все абоненты приходят в чат; до модели доходит 30–70% вопросов).

| Контур | Вопросов в месяц | До модели в месяц | Событий в год |
|---|---:|---:|---:|
| MVP, 8 000 абонентов | 1 680–42 000 | 504–29 400 | 6 048–352 800 |
| Тираж, 90 000 абонентов | 18 900–472 500 | 5 670–330 750 | 68 040–3 969 000 |

**Замеры в Postgres** (docker на рабочей машине, настройки по умолчанию, синтетические данные с равномерным распределением; схема
`usage_events` с тремя индексами; агрегат — число вызовов, сумма токенов и P95 времени по месяцу × типу × модели):

| Строк в таблице | Размер | Агрегат по всей таблице | За 30 суток: расход по абонентам | Соответствует |
|---:|---:|---:|---:|---|
| 2 млн | 589 МБ (308 байт на строку) | 1,5 с | 0,35 с | ≈6 месяцам на тираже, максимум |
| 12 млн | 3 492 МБ (305 байт на строку) | **39 с** | **5,8 с** (за 31 сутки, 87 583 абонента) | ≈3 годам на тираже, максимум |

Время растёт быстрее числа строк (×26 при ×6 строк): таблица перестаёт помещаться в кэш. Для 4 млн строк (≈1 год на тираже, максимум)
по этим двум точкам получается ≈5 с — это интерполяция, не замер.

| Критерий | PostgreSQL (отдельная база `telemetry`) | ClickHouse | TimescaleDB | OpenSearch |
|---|---|---|---|---|
| Хватает ли на объёме проекта | **Да на MVP и в минимуме тиража; на тираже в максимуме — при хранении событий до 12 месяцев (≈4 млн строк). Агрегат по всей таблице на 12 млн строк — 39 с (замер)** | Хватает с огромным запасом | Хватает | Хватает |
| Сколько систем в стеке | Уже есть | Ещё одна СУБД со своей эксплуатацией и резервным копированием | Расширение существующей | Ещё одна система |
| Мешает ли основной базе | Нет: отдельная база; до 0,13 записи в секунду в среднем на тираже, максимум | Нет | Нет | Нет |
| Итог | **Выбран для прототипа, MVP и тиража** (с ограничением по сроку хранения) | Отклонён: вторая СУБД ради объёма, которого нет | Не нужен на этом объёме | Не нужен: тяжелее без выигрыша |

**Ограничение решения и когда вернуться к вопросу.** Довод «агрегаты за секунды» верен только до нескольких миллионов строк. Поэтому
принято: подробные события хранятся 12 месяцев (≈4 млн строк на тираже в максимуме), более старые сворачиваются в сводную
таблицу по дням, отчёты по всей истории строятся по ней, а не по событиям; таблица делится на секции по месяцам. Сводная таблица и
секционирование не реализованы и не измерялись. Если потребуется аналитика по подробным событиям дольше года либо агрегаты по
всей таблице станут дольше 10 с, ClickHouse рассматривается заново (порог 10 с — предложение, не расчёт). Переезд — смена DSN и
диалекта запросов записи, а не бизнес-логики (ADR-100), поэтому откладывать безопасно. Замер сделан на синтетических данных и не
заменяет проверку на реальных событиях после запуска.

### Данные абонента: читаем из систем в момент вопроса, кэш — после замера

**Требование (заказчик проекта, 25 сентября 2026): данные абонента нужны онлайн из систем.** Баланс, начисления, показания
счётчиков и статусы заявок меняются, и ответ по сводке с отставанием на период пересчёта может оказаться неверным. Прежний
план — заранее посчитанный профиль в материализованном представлении Postgres, который обновляет задание по расписанию, —
снят.

Прочие требования: не выдавать устаревшее как актуальное; не подвесить ответ на медленной внешней системе; нагрузка на
Биллинг и Диспетчерскую не должна расти сверх допустимой для владельца (лимиты их API неизвестны).

**Как сейчас (прототип).** Ни Postgres, ни Redis для данных абонента не используются. Пример данных (`ЛКК.csv`, `ЛСФЛ.csv`,
`счетчики.csv`, `начисления.csv`) читается при старте в словари в памяти процесса (`app/billing/source.py`); запрос
абонента ничего не разбирает. Источник данных стоит за интерфейсом, поэтому на MVP его заменяет клиент к настоящим API
(Биллинг — XML-RPC, Диспетчерская — REST), а вызывающий код меняется мало (оценка автора, не проверялось).

| Критерий | Слепок в памяти процесса (прототип) | **Чтение из систем на каждый вопрос (выбрано для MVP)** | Предрасчёт по расписанию в Postgres (был в плане, снят) | Чтение из систем + короткий кэш в Redis | Feature Store (Feast, Tecton) |
|---|---|---|---|---|---|
| Свежесть данных | Возраст слепка, данные не обновляются | Актуальны на момент вопроса | Отстают на период пересчёта | Отстают на срок жизни записи в кэше (минуты) | Отстают на период пересчёта |
| Задержка | Наносекунды | Время вызова внешнего API прибавляется к ответу; **не измерялась** (доступа к API нет) | Порядка миллисекунд (оценка) | Первый вызов в диалоге — как без кэша, повторный — миллисекунды (оценка) | Зависит от онлайн-хранилища |
| Нагрузка на Биллинг и Диспетчерскую | Разовая загрузка при старте | Пропорциональна числу вопросов: не более одного вызова каждой системы на вопрос (верхняя оценка) | Пересчёт по расписанию | Ниже, чем без кэша; доля повторов не измерена | Пересчёт по расписанию |
| Отказ внешней системы | Не зависит | Помощник говорит, что данных нет; по старым не отвечает | Отвечает по устаревшему | Как без кэша, но повторный вопрос в диалоге может обойтись | Отвечает по устаревшему |
| Что добавляет в стек | Ничего | Клиент к API, таймауты и повторы | Задание по расписанию, представление в Postgres | Слой кэша со сроком жизни | Отдельная платформа |
| Итог | **Прототип** | **Выбран для MVP** | Отклонён 25.09.2026: данные нужны онлайн | **Не берётся сейчас; включается после замера** (условие ниже) | Отклонён: избыточен и не решает свежесть |

**Решение: данные абонента читаются из Биллинга и Диспетчерской в момент вопроса.** Вызовы идут параллельно и только за
теми данными, которые нужны пути ответа (баланс, начисления, заявки). Таймаут и число повторов — настройки клиента.
Если система не ответила, помощник так и говорит: данных сейчас нет; устаревших значений не подставляет. Данные попадают к
модели только после обезличивателя (PII Filter), как и раньше (ADR-400). Задание по расписанию и представление Postgres для
данных абонента не разворачиваются. Профиль из личного кабинета (ФИО, лицевой счёт, адрес) приходит от Платформы ЛК при
открытии диалога (SSO) и этим решением не затрагивается.

**Кэш (Redis, срок жизни — минуты, ключ «абонент + вид данных») — только после замера.** Цель кэша — не выбрасывать
повторные вызовы в одном диалоге. Он включается, если замер покажет одно из двух: время вызова API не укладывается в
бюджет ответа либо лимиты API владельца ниже нагрузки. Что кэш допустим по свежести, решает владелец (для баланса он может
не подойти). Замер — ADR-600, «Замер чтения данных абонента из систем».

**Что не подтверждено.** Скорость, лимиты и доступность API Биллинга и Диспетчерской неизвестны, доступа к ним у проекта нет.
Доля вопросов, которым нужны данные абонента, не измерена. Довод «одно определение признаков для офлайн-оценки и онлайна»
отпадает: офлайн-оценка идёт по эталонному набору вопросов и таблицы профиля не читает. Сравнение с Feature Store сделано по
требованиям и документации, не по пробному внедрению.

### Почему индексация — задание по расписанию, а не Airflow

Требования: редкое, некритичное по задержке обновление базы знаний; ночное окно с жёсткой границей; изоляция от
обслуживания абонентов.

| Критерий | Планировщик по расписанию (K8s CronJob; Celery Beat по схеме развёртывания) | Apache Airflow | Cron внутри контейнера Backend |
|---|---|---|---|
| Состав | Примитив Kubernetes, в схеме развёртывания предусмотрен и для резервного копирования (кластера пока нет) | Отдельная платформа: планировщик, веб-сервер, база метаданных | Нет отдельного процесса |
| Подходит для одного периодического задания | Да | Избыточен | Да |
| Изоляция от обслуживания абонентов | Отдельный под с окном и ограничением времени | Отдельные воркеры | Нет: индексация конкурирует с ответами за процессор |
| Итог | **Выбран** | Отклонён: платформа ради одного задания | Отклонён: нет изоляции |

Прежний второй потребитель планировщика — предрасчёт профиля абонента — снят решением 25 сентября 2026 (данные читаются онлайн). Кроме индексации CronJob остаются у ночной проверки качества и резервного копирования.

### Индексация базы знаний: что реализовано и какой объём

**Реализовано (прототип).** Python-скрипт `scripts/reindex_kb.py` вызывает `app/kb/build.py`: разбор документов Word
библиотекой `python-docx` (`app/kb/documents.py`, фрагмент = раздел с путём заголовков, таблицы разворачиваются в строки) и
пар FAQ из `kb/faq_voronezh.json` → нарезка частей до 300 знаков → проверка на ПДн обезличивателем (Presidio; найденные
ПДн — ошибка сборки) → эмбеддинги `multilingual-e5-small` (sentence-transformers, CPU) → запись в новую коллекцию Qdrant
и атомарное переключение алиаса (blue-green). Запуск — сервис `reindex` в `docker compose`, одноразовый при подъёме стека.
Очереди задач и планировщика в коде нет (Celery в зависимостях проекта отсутствует): «Celery Beat» — название на схеме
развёртывания, а не реализованная технология. На MVP выбран планировщик по расписанию (Kubernetes CronJob), Celery Beat
рассматривался бы, только если понадобится очередь задач; решение не принято.

**Что в базе сейчас.** 27 источников: 20 пар FAQ и 7 документов Word (5 договоров и 2 регламента) → 51 фрагмент → 190
частей (векторов). Документы Word **синтетические** (свойство файла `СИНТЕТИКА`, собраны `scripts/generate_kb_docs.py`):
настоящих регламентов на прототипе нет.

**Какой объём планируется.** MVP — несколько тысяч документов (3 000–5 000), 30 000–100 000 частей (допущение о числе
частей на документ, 10–20, не измерялось). Реального источника документов и их формата (Word, PDF, сканы) владелец не
назвал: это открытый вопрос, от него зависит разбор.

**Хватит ли ночного окна 02:00–05:00 (3 часа).** Расчёт по замеру скорости эмбеддингов на CPU (64 фрагмента,
`scripts/measure_search_models.py`, 24 сентября 2026):

| Модель | Время на часть | 30 000 частей | 100 000 частей |
|---|---:|---:|---:|
| `multilingual-e5-small`, CPU | ≈27 мс | ≈14 мин | ≈46 мин |
| BGE-M3, CPU | ≈320–345 мс | ≈2,7–2,9 ч | ≈8,9–9,6 ч |
| BGE-M3, GPU | не измерялось | — | — |

Вывод: на CPU BGE-M3 в 3-часовое окно не укладывается уже на нижней границе объёма; для полного ночного реиндекса
нужен GPU, но скорость на GPU **не измерялась**, поэтому «окно хватит» — допущение. Замер скорости индексации на GPU и
решение между полным и инкрементальным (только изменённые документы) реиндексом входят в тест моделей эмбеддингов
(ADR-600). Время на разбор документов и проверку ПДн (spaCy) в расчёт не входит и не измерялось.

### Наблюдаемость: Grafana владельца и источники данных

**Факты со слов заказчика проекта (24 сентября 2026, документально не подтверждено): в реальном контуре работают Grafana, Zabbix (мониторинг) и Jaeger (трассировка); алерты не настроены.**
Версии Zabbix, Grafana и Jaeger, источники данных Grafana (вероятно, Zabbix), кто и как ими управляет — неизвестно. Своя Grafana не разворачивается: дашборды нашего сервиса поставляются как код
(JSON, импорт в Grafana владельца). Прежний выбор — собственная четвёрка Prometheus, Grafana, Loki, Alertmanager — снят: Grafana и система мониторинга у владельца уже есть.

Требования: развёртывание в закрытом контуре; метрики приложения и Kubernetes; логи без сырых ПДн; оповещения; без дублирования того, что у владельца уже есть.

| Вариант | Что мы поставляем | Оценка |
|---|---|---|
| A. Prometheus владельца | Порт `9100`, правило сбора, дашборды и правила как код | В контуре не назван: **снят**, пока владелец его не подтвердит |
| B. Grafana владельца + свой Prometheus | Один под Prometheus как источник для их Grafana (квантили задержки считаются `histogram_quantile`) | **Запасной**, на случай если Zabbix не справится с задержкой P99 (риск ниже); хранение и обновления на нас |
| **C. Zabbix владельца + Grafana владельца** | Порт `9100`; Zabbix забирает метрики (HTTP-агент и разбор формата Prometheus — по документации Zabbix, не проверялось); дашборды строятся на плагине Zabbix; алерты — триггеры и действия Zabbix | **Выбран как основной** (Zabbix уже в контуре); ничего нового не ставим |
| D. Своя четвёрка (Prometheus, Grafana, Loki, Alertmanager) — прежний выбор | Всё сами | Отклонён: дублирует Grafana и Zabbix владельца |
| E. ELK / OpenSearch | Тяжёлый полнотекстовый стек | Отклонён: тяжелее для основной задачи (по документации) |
| F. Облачный сервис мониторинга | — | Отклонён: данные уходят за периметр, 152-ФЗ |

**Главный технический риск варианта C — задержка P99.** Требования к ответу выражены в квантилях (P99), а наши метрики задержки — гистограммы `prometheus-client` (`assistant_ttft_seconds_bucket` и др.).
По моим сведениям (не проверялось), Zabbix разбирает формат Prometheus и считает скорость роста счётчиков, но расчёт квантиля по корзинам гистограммы штатно не предусмотрен: нужны вычисляемые элементы,
либо приложение экспортирует готовые квантили, либо вариант B. **Проверить на стенде владельца до начала работ по дашбордам.**

**Алерты не настроены.** 9 описанных алертов (от «Утечка ПДн в ответе» до «ИБ + деградация») сейчас принимать некому: нет настроенных каналов оповещения и получателей. Нужно определить: получателей (эксплуатация — отдел владельца;
по ПДн — ИБ), канал (почта, мессенджер, тикет), регламент реакции и эскалации, триггеры и действия Zabbix. Без этого алерты «Утечка ПДн в ответе» и «ИБ + деградация» на MVP не сработают. **Предложение автора (согласовать
с ИБ):** настроенный алерт об утечке ПДн — условие включения MVP в работу. Настроены ли алерты в Zabbix вообще или только для нашего сервиса — неизвестно.

**Jaeger.** Трассировка в контуре есть, **в нашем коде её нет** (библиотек OpenTelemetry и Jaeger в проекте нет — проверено поиском по коду). Возможная работа: инструментация OpenTelemetry (FastAPI, HTTP-клиенты, клиенты Qdrant,
Postgres, Redis) и отправка трасс по OTLP в Jaeger владельца; польза — разбор задержки ответа по стадиям (поиск, обезличивание, провайдер, проверка) при разборе P99 и TTFT. **В MVP не входит**, берётся по необходимости:
если гистограмм по стадиям не хватает для разбора задержек. Оценка автора 2–4 чел-дн, не подтверждена, в итог не добавлена. Условие: в атрибутах спанов нет ПДн (ADR-400). Принимает ли Jaeger владельца OTLP — неизвестно.

**Проверка на стенде владельца до выбора между C и B** (предложение автора, 1–2 чел-дн, нужен доступ к Zabbix и Grafana владельца; пороги и оценка на замере не основаны):

| № | Проверка | Как понять, что прошла |
|---|---|---|
| 1 | Zabbix забирает метрики с порта `9100` наших подов | Метрики видны в Zabbix; сетевые правила пропускают доступ |
| 2 | Расчёт P99 по гистограммам (`assistant_ttft_seconds_bucket`) | Значение в Zabbix совпадает с расчётом `histogram_quantile` на пробном Prometheus в пределах ширины корзины |
| 3 | Алерт из Zabbix доходит до получателя | Пробный триггер (например, по `guardrails_blocked_total`) срабатывает, оповещение приходит по каналу |
| 4 | Grafana владельца показывает данные Zabbix | Плагин подключён, панели строятся |
| 5 | Нагрузка на Zabbix | Число элементов данных (метрики × метки × поды) укладывается в лимиты владельца |

**Правило выбора:** C, если проходят проверки 1–4 (5 — ограничение); если не проходит 2 или 3 — B, а алерты идут через Grafana Alerting или триггеры Zabbix по метрикам Prometheus. Цена запасного варианта B —
под Prometheus (0,5 vCPU, 1 ГиБ, диск 10 ГиБ) и его обслуживание; она уже в верхней оценке узлов (ADR-500).

**Алерты: сравнение вариантов** (все пункты по документации, у владельца не проверялось; каналы оповещения не настроены):

| Вариант | Как работает | Когда подходит | Замечания |
|---|---|---|---|
| **Триггеры и действия Zabbix (выбран при C)** | Условие на метрику → оповещение по каналу, настроенному в Zabbix | Zabbix уже мониторит инфраструктуру, там же оповещения эксплуатации | Каналы и получатели надо настроить: сейчас не настроены; зависит от версии Zabbix |
| Grafana Alerting | Правила в Grafana по любому источнику, отправка через её каналы | Вариант B; если в Zabbix настройка невозможна | Ещё одно место с правилами; в Grafana владельца может быть отключена или закрыта правами |
| Alertmanager | Маршрутизация и подавление повторов для Prometheus | Только при своей связке Prometheus | Не разворачиваем: дублирует Zabbix и Grafana |

Общие требования к любому варианту: алерт «Утечка ПДн в ответе» приходит **сразу и без подавления повторов** получателю от ИБ; алерт о недоступности самого источника метрик (иначе тишина неотличима от исправной работы);
получатели, канал, регламент реакции и эскалация определены до включения MVP (предложение автора, согласовать с ИБ). Порог срабатывания каждого из 9 алертов задаётся по замеру на живых данных, а не заранее.

**Трассировка (Jaeger): сравнение вариантов**

| Вариант | Что даёт | Что стоит | Оценка |
|---|---|---|---|
| **1. Без трассировки, метрики по стадиям (по умолчанию для MVP)** | Разбор P99 и TTFT по стадиям гистограммами: сейчас есть проверка ответа и обезличивание (`guardrails_check_seconds`, `pii_sanitize_seconds`), **нет поиска и вызова провайдера** | Добавить две гистограммы (поиск в базе знаний, вызов провайдера): 0,5–1 чел-дн (оценка автора, в пределах оценки строки «Наблюдаемость») | Достаточно, пока стадий четыре и цепочка линейная |
| 2. OpenTelemetry → Jaeger владельца | Путь одного запроса через Backend, шлюз, Qdrant, Postgres, Redis, провайдера | 2–4 чел-дн (оценка автора, в итог не входит), нужен приём OTLP в Jaeger владельца (не проверено) | Берётся, если после варианта 1 задержку P99 разобрать не удаётся; в атрибутах спанов нет ПДн (ADR-400) |
| 3. Свой Jaeger | То же без зависимости от владельца | Ещё один компонент и хранилище | Отклонён: Jaeger в контуре есть |

**Вывод:** на MVP берётся вариант 1; вариант 2 — по условию.

**Логи.** Loki, сбор логов кластера или другое — неизвестно. Правило для любого варианта: в логах только отчёт об обезличивании, без значений ПДн (ADR-400).

**Чем заменено прежнее обоснование.** Zabbix был отклонён фразой «ориентирован на хосты»: она не проверялась и упрощала. Теперь Zabbix основной по факту: он уже в контуре.

**Для расчёта узлов (ADR-500)** Alertmanager и Grafana исключены; Prometheus (запасной источник для P99) и Loki (логи неизвестны) оставлены как верхняя оценка до ответа владельца.

**Вопросы владельцу:** версии Zabbix, Grafana, Jaeger; подключён ли Zabbix к Grafana и каким плагином; включён ли разбор формата Prometheus и можно ли считать квантили по корзинам; кто и как настраивает триггеры и оповещения,
на кого приходят алерты по ПДн; принимает ли Jaeger OTLP; где хранятся и как собираются логи; как принимаются дашборды (импорт JSON, provisioning из Git, права).

**Ограничения.** Сравнение по документации проектов; проверки на стенде владельца не было. Поставка дашбордов и триггеров как кода — работа команды (матрица, строка «Наблюдаемость»).

### Документы Этапа 2: PDF и хранилище файлов (решение не принято)

На прототипе бланк — HTML-лист на печать. На Этапе 2 нужны PDF и хранение готовых файлов со ссылкой ограниченного
срока. Библиотека PDF **пока не выбрана**; ниже кандидаты и критерии выбора.

| Кандидат | Вход | Тяжесть | Замечание |
|---|---|---|---|
| WeasyPrint | HTML и CSS — как текущие бланки | Python-пакет и системные библиотеки | Текущие шаблоны переносятся без переписывания |
| Headless Chromium | HTML и CSS | Браузер в образе, сотни мегабайт | Точная вёрстка, тяжёлый образ |
| ReportLab | Код на Python | Python-пакет | Шаблоны пришлось бы переписать |
| LibreOffice headless | Документы Word / ODT | Офис-пакет в образе | Нужны шаблоны Word; бланки в проекте — HTML |
| wkhtmltopdf | HTML | Отдельный бинарник | Проект архивирован, брать не следует |

Критерий выбора: принимать текущие HTML-шаблоны без переписывания. Под него подходят WeasyPrint и headless Chromium;
окончательный выбор — задача Этапа 2 (ADR-600).

Хранилище файлов: **MinIO (S3)** — предложение (та же технология, что уже применяется для резервного копирования;
ссылка с ограниченным сроком жизни делается средствами S3), против файловой системы на томе (нет ссылок с временем
жизни) и хранения в базе (смешивает файлы с данными). Решение предварительное.

### Шаблоны заявлений: отдельный контейнер, шаблоны из хранилища или файлового сервера

**Требование (заказчик проекта, 25 сентября 2026):** шаблоны документов для обращений (например, заявление о поверке) не должны жить в коде приложения. Их
надо тянуть из хранилища или с файлового сервера, отдельным контейнером.

**Как сейчас (прототип).** Семь образцов заданы в коде (`app/documents/templates.py`): текст с полями, вопросы абоненту, список приложений, правовое основание.
Состав полей сверен с ПП РФ №644/776/831 и публичными бланками; **настоящих утверждённых бланков владельца на прототипе нет**, каждый образец помечен
«ориентировочный, не официальная форма». Замена бланка сегодня — правка кода и выпуск версии приложения.

**Что не устраивает.** Официальная форма принадлежит водоканалу и меняется при смене правил; выпуск приложения ради замены бланка — неоправданно
долгий путь; проверить, что в бланк не попали персональные данные, негде.

| Вариант | Где лежит шаблон | Кто отдаёт | Что даёт | Что стоит |
|---|---|---|---|---|
| А. В коде (прототип) | Python-файл | Backend | Ничего лишнего, проверено тестами | Любая замена — выпуск приложения; владелец не может обновить бланк сам |
| Б. Файлы в образе или ConfigMap | Каталог рядом с приложением | Backend | Шаблоны отделены от кода | Замена всё равно через выпуск; ConfigMap ограничен по размеру (≈1 МБ, по документации Kubernetes) |
| В. **Отдельный контейнер `template-service` + хранилище MinIO (S3)** | Объекты в бакете шаблонов | Контейнер по API | MinIO уже в стеке (документы Этапа 2, копии); версии объектов и права на бакет; замена без выпуска приложения | Ещё один контейнер; нужен формат шаблона и описателя |
| Г. **Отдельный контейнер + файловый сервер владельца** (SMB/NFS/WebDAV) | Папка на сервере владельца | Контейнер по API | Владелец кладёт форму туда, где уже работает; ничего нового у нас | Доступ, протокол и учётная запись неизвестны; сеть между контурами; наша сторона не контролирует доступность |
| Д. В Postgres | Таблица | Backend или контейнер | Транзакции | Смешивает файлы форм с данными; ручная загрузка Word/PDF неудобна |

**Решение.** Вариант **В/Г — отдельный контейнер `template-service`** с подключаемым источником: MinIO (S3) или каталог файлового сервера, смонтированный
в контейнер (только чтение). Backend и Document Generator получают шаблон по внутреннему API; сами шаблоны не хранят. **Какой источник основной — не решено:** зависит от ответа
владельца (где лежат утверждённые формы, кто их обновляет, есть ли доступ к файловому серверу). Пока владелец не ответил, кандидатом считается MinIO: он уже
в стеке, версии и права — средствами S3 (по документации). Сравнение по документации и общим сведениям, стенда нет.

**Что делает контейнер.**
- Отдаёт по коду шаблона (`meter_verification`, `certificate` и т. д.) текст или файл формы и **описатель**: правовое основание, поля из Биллинга, поля, которые
  собираются в диалоге (готовые вопросы), приложения. Сегодня это поля класса `DocumentTemplate`.
- Кэширует шаблоны в памяти с проверкой изменения (ETag или время изменения) и не ходит в источник на каждый вопрос.
- При загрузке или обновлении проверяет: все поля шаблона известны системе; в тексте нет ПДн (то же обезличивающее средство, что проверяет базу знаний).
  Шаблон, не прошедший проверку, не подключается, прежняя версия продолжает отдаваться.
- Держит версию: при выдаче бланка версия записывается в след операции.

**При недоступности источника.** Контейнер отдаёт последнюю версию из кэша; если кэша нет (холодный старт при отказе) — Backend возвращается к встроенным образцам
с пометкой «ориентировочный образец», как на прототипе, и не выдаёт это за официальный бланк.

**Формат шаблона — не выбран.** Определяется тем, в каком виде владелец даст формы: Word, PDF-форма или HTML. Это связано с выбором библиотеки PDF (подраздел выше): критерий
«принимать текущие HTML-шаблоны без переписывания» относился к образцам в коде; если формы придут в Word, вариант LibreOffice headless становится основным кандидатом.
Описатель (поля, вопросы, приложения) в любом случае отдельный файл YAML или JSON рядом с шаблоном (формат записи — предложение автора).

**Безопасность.** Учётная запись контейнера в хранилище — только на чтение бакета; загрузку выполняет отдельная роль владельца или наша, но не сервис (предложение
автора). NetworkPolicy: вход только от Backend и Document Generator. В шаблонах нет персональных данных — это проверяется при загрузке. Целостность — контрольная
сумма объекта; сравнение с эталоном при обновлении.

**Что не подтверждено.** Формат форм, источник (MinIO или файловый сервер), протокол и доступ, кто утверждает и обновляет формы, необходимость приложений
(копии паспорта прибора учёта и т. п.) — всё это ответы владельца (ADR-600, «Ждут ответа владельцев систем»). Размер и нагрузка ничтожны (килобайты, чтение с кэшем), но не измерялись.
Оценка работ — ADR-600, «Сервис шаблонов заявлений (MVP)».

### Секреты в проде: GitLab CI variables и альтернативы

Целевой механизм секретов на MVP — **GitLab CI/CD variables, деплой через pipeline, Kubernetes Secret** (решение заказчика проекта, 24 сентября 2026: в реальном контуре уже используется GitLab; документально не подтверждено).
Прежний выбор — Vault Agent Injector — не был обоснован сравнением и отменён (ADR-400). Сравнение сделано **по документации проектов и общим сведениям, замеров и проверки на стенде владельца нет**.

| Критерий | **A. GitLab CI variables + pipeline → Kubernetes Secret (выбрано)** | B. Vault Agent Injector (sidecar) | C. External Secrets Operator или Vault CSI + хранилище | D. SOPS или Sealed Secrets (зашифрованные секреты в Git) | E. Kubernetes Secret вручную (`kubectl`) |
|---|---|---|---|---|---|
| Что нужно поставить нового | Ничего: GitLab и runner уже есть (со слов); job деплоя | Vault (отказоустойчивость, разблокировка, резервные копии, обновления) и инжектор в кластере | Оператор или провайдер и хранилище (Vault или другое) | Контроллер Sealed Secrets либо ключи SOPS и расшифровка в pipeline | Ничего |
| Где хранится значение | GitLab → Kubernetes Secret (etcd, base64) | Vault; в поде — файл в памяти, Kubernetes Secret не создаётся (по документации) | Хранилище → Kubernetes Secret | Git в зашифрованном виде → Kubernetes Secret | Только Kubernetes Secret |
| Кто может прочитать | Роли проекта GitLab с правом читать переменные и RBAC namespace | Политики Vault и RBAC | Политики хранилища и RBAC | Все с доступом к Git видят шифртекст; ключ — у держателя ключа | RBAC namespace |
| Аудит чтения | Нет; изменения — по журналу аудита GitLab (зависит от редакции) | Есть (аудит Vault, по документации) | Есть на стороне хранилища | Нет | Нет |
| Замена секрета | Изменить переменную, deploy, перезапуск подов | Без пересборки образа; перечитывание — по шаблону агента (по документации) | Синхронизация; для переменных окружения нужен перезапуск | Коммит и deploy | `kubectl` и перезапуск |
| Динамические секреты и аренда | Нет | Да | Зависит от хранилища | Нет | Нет |
| У владельца уже есть | Да (GitLab CI, со слов) | Неизвестно | Неизвестно | Неизвестно | Да, но нет воспроизводимости |
| Работы команды на MVP | 2–3 чел-дн (оценка автора) | Не оценивалась: установка и эксплуатация Vault с нуля — отдельная работа, в смету не входит | Не оценивалась | Не оценивалась | Около 0,5 чел-дн (оценка автора), но без повторяемости и журнала |
| Главный риск | Утечка через логи pipeline и права ролей GitLab; нет аудита чтения; шифрование etcd неизвестно | Сложность эксплуатации, sidecar на каждый под, лицензия Vault (по моим сведениям, с 2023 года BSL; не проверялось) | Две системы вместо одной | Единая точка отказа — ключ шифрования | Ручные ошибки, нет журнала |

**Почему A.** Ничего нового не ставится и не эксплуатируется, а требование «секреты вне образа и вне Git» выполняется; приложение не меняется. Цена — нет аудита чтения, замены без перезапуска и динамических секретов; для статичных ключей
внешних провайдеров это не критично. **Почему не B.** Выигрыш B (аудит чтения, аренда, замена без пересборки) сейчас никем не требуется, а платить пришлось бы новой системой, у владельца которой может не быть (неизвестно), и sidecar в каждом поде.
**Условия пересмотра** — в ADR-400. **Что не проверено:** доступность защиты переменных в версии GitLab владельца, роли и права на переменные, шифрование etcd, лимиты на число и размер переменных, отсутствие секретов в логах pipeline —
проверяется при подготовке деплоя (2–3 чел-дн выше).

## Общий принцип выбора на прототипе

Везде, где прототип использует не целевой компонент, а упрощённую замену (Qdrant→память,
Kubernetes→docker compose), причина одна и та же: **объём прототипа не оправдывает
эксплуатационную стоимость целевого инструмента**, а миграция — это смена конфигурации/DSN, не
переписывание бизнес-логики (см. ADR-100, таблицу «Целевые компоненты — упрощённая замена»).

