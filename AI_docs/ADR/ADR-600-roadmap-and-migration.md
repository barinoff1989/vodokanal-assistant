---
status: accepted
date: 2026-09-14
---

# ADR-600: План развития — переход прототип → MVP → тираж

## Статус

**Accepted** как сводка: группировка задач под переход между этапами, укрупнённые сроки и явные
допущения, без полного списка задач. Числа — оценка, не
согласованный план-график с исполнителем.

## Критерий перехода к MVP (stage-gating функционального Этапа 2)

Зафиксировано в контексте проекта и не меняется этим документом: Этап 2 не начинается до получения
метрик Этапа 1 по всем четырём условиям одновременно — **стабильные метрики качества, подтверждённый
бизнес-эффект, согласование ИБ, готовность интеграций**. Календарной даты у перехода нет — только эти
условия.

## Переход прототип → MVP

### Блокирует защиту бюджета (P0) — сделать в первую очередь

| Задача | Оценка | Допущение |
|---|---|---|
| Смета CAPEX/OPEX — умножить уже посчитанные количества (`Capacity_Report.html`, раздел 7) на цены из коммерческого предложения поставщика | 1–2 дня | Цены поставщика **пока не получены** (`Capacity_Report.html`: коммерческих предложений нет); календарное время зависит от поставщика, не от команды |
| Расчёт NFR (TTFT/P99) под конкретную GPU-конфигурацию | 2–3 дня | Формула уже выведена, нужна подстановка |
| Подтверждение допущений о нагрузке (A1–A3) статистикой ЛК и колл-центра | 1 неделя работы + время ответа владельца данных | Разброс стоимости токенов на MVP — ×82 между минимумом и максимумом (`Capacity_Report.html`); без этих данных смета и sizing остаются диапазоном, не числом |
| **Первый пересчёт сметы по данным:** подставить статистику ЛК, колл-центра и живых обращений (A1–A3, доля вопросов до модели, доля обращений с записью) в модель (`Tech_Stack_Matrix.xlsx`, лист «Прогноз стоимости»), пересчитать нагрузку, стоимость и sizing, обновить `Capacity_Report.html`, ADR-500 и матрицу | 3–5 чел-дн | Начинается после получения данных от владельца и анализа живых обращений; календарный срок определяет владелец данных |
| **Повторный пересчёт по пилоту:** длина запроса на базе из тысяч документов, тарифы провайдеров (в том числе GigaChat Pro), скорость моделей на GPU | 1–2 чел-дн | После пилота и получения тарифов |

**Итог блока: ≈2,5–3,5 недели (11,5–17 чел-дн) работы команды**, из них пересчёт сметы по данным — 4–7 чел-дн, календарно — дольше, если ответ от водоканала/поставщика
задерживается.

### Ждут ответа владельцев систем (часть P1) — не в контроле команды по срокам

Согласование кодов типов обращений, текста о качестве воды, четырёх процессов вне справочника
(заключение/расторжение договора, смена тарифа, сверка расчётов), настоящих бланков заявлений,
сигнатур внешних API (Биллинг, Диспетчерская, микросервис обращений) — все эти пункты **не требуют
разработки**, требуют ответа от водоканала. Разработка по каждому — от нескольких часов до 1–2 дней
на внедрение полученного ответа; календарный срок определяет не команда.

### Качество поиска и классификации (часть P1) — измеримая работа

Подтверждение разметки эталонного набора, сравнение e5-small/BGE-M3 на качестве (не только скорости),
замер офлайн-роли реранкера, калибровка судьи качества, устранение найденных ложных срабатываний
классификатора, расширение запасного классификатора `InquiryType` на оставшиеся девять
типов (требует размеченного корпуса, отдельное предусловие) — это уже сделанная в проекте
методология (замер → порог → проверка), применённая многократно на прототипе.

**Оценка: ≈7–10 недель (35–51 чел-дн)** сфокусированной работы, при условии что корпус для оставшихся девяти
типов `InquiryType` уже собран (само по себе — отдельная задача, зависит от готовности чат-регистра, не от
разработки). Состав:

| Работа | Оценка |
|---|---|
| Расширение запасного классификатора на девять типов, устранение ложных срабатываний, подбор порогов на выбранной модели эмбеддингов (подробно ниже) | 8–12 чел-дн (1,5–2,5 недели) |
| Тест нескольких моделей эмбеддингов для MVP на размеченном наборе (подробно ниже) | 10–14 чел-дн |
| Реранкер: замер офлайн-роли (2–3) и тест нескольких моделей (5–7) | 7–10 чел-дн |
| Судья: калибровка порогов (2–3) и тест моделей судьи (4–6) | 6–9 чел-дн |
| Полнота обезличивания на живых данных и выбор модели разбора языка | 4–6 чел-дн |

#### Расширение классификатора `InquiryType` — что сделано, что осталось, и почему

**Сделано (7 из 16 типов справочника).** Запасной классификатор (`app/agents/inquiry_type_fallback.py`)
и закрытый список регистрируемых типов (`REGISTRABLE`, `app/backend/registration.py:54`) покрывают:
`meter_verification` (поверка счётчика), `meter_sealing` (опломбировка), `meter_installation` (установка
прибора), `meter_unsealing` (снятие пломбы), `certificate` (справка), `inspection` (обследование),
`document_submission` (приём документов). Полный справочник 16 типов — `app/taxonomy.py` (`InquiryType`,
строки 62–87).

**Осталось (9 типов из того же справочника):** `refund`, `debt`, `payment`, `penalty`,
`accrual_recalculation`, `inquiry_cancellation`, `meter_reacceptance`, `meter_reacceptance_auto`, `other`
(последний — категория «прочее» по построению, не кандидат на отдельную классификацию, но входит в счёт
16 по тому же перечислению, что и остальные пятнадцать).

**Почему именно эти семь — решение по качеству данных, не по объёму работы.** Разбор
`fixtures/inquiries_voronezh.csv` по девяти оставшимся типам показал: реальные тексты по ним — по
существу споры («откуда задолженность, если я плачу», «почему сумма выросла втрое»), а не запросы на
регистрацию обращения. Обучать классификатор на них означало бы учить его смешивать спор с заявкой — ту
же ошибку, которую `AccountResponder._DISPUTE_MARKERS` отдельно отсекает на соседней оси (полное
обоснование — докстринг `app/agents/inquiry_type_fallback.py`, строки 1–19).

**Условие начала работы, а не срок по календарю.** Расширение происходит, когда появится размеченный
корпус вопросов-заявок (не жалоб) в форме чата по этим девяти типам — это отдельная задача, зависящая от
готовности чат-регистра, а не решение «не успели». Оценка 1,5–2,5 недели (8–12 чел-дн, см. таблицу выше) относится именно к работе
после появления такого корпуса, тем же уже отработанным приёмом (замер порога → замер разрыва → проверка
на ложных срабатываниях), что дал нынешние семь.

#### Тест моделей эмбеддингов для MVP

Модель для поиска и запасного классификатора (ADR-300, раздел 2) выбирается не по репутации, а по замеру на
размеченном наборе из живых обращений. Кандидаты (4–5): `multilingual-e5-small` (текущая, база сравнения),
`multilingual-e5-large`, BGE-M3, модели, дообученные на русском (`deepvk/USER-bge-m3`, `ai-forever/ru-en-RoSBERTa`).
Критерии: качество поиска (hit@1, hit@3, MRR), охват и точность запасного классификатора, время запроса на GPU (бюджет
поиска P99 менее 100 мс), размер и требования к GPU, лицензия.

| Работа | Оценка |
|---|---|
| Окружение для прогона на GPU; расширение скриптов замера (модель параметром, замер качества поиска и k-NN) | 1–2 чел-дн |
| Прогон 4–5 моделей: индексация корпуса, качество поиска, охват и точность k-NN, время запроса и индексации, размер | 4–5 чел-дн (≈1 на модель) |
| Подбор порогов поиска и k-NN отдельно под каждую модель (пороги не переносятся между моделями) | 3–4 чел-дн |
| Свод результатов, решение, запись в ADR-300, переиндексация выбранной модели | 2–3 чел-дн |

**Оценка: 10–14 чел-дн.** Пороги под каждую модель подбираются здесь один раз; в расширении классификатора порог подбирается только на модели-победителе, поэтому порядок работ — сначала тест эмбеддингов, затем расширение классификатора (иначе подбор порогов делался бы дважды). Размеченный набор берётся из анализа на живых данных (следующий раздел) и в оценку не
входит. Итог — выбор модели для MVP и проверка, годится ли она для тиража.

#### Тест моделей реранкера для MVP

Кандидаты (2–3): `BAAI/bge-reranker-v2-m3` (текущая), `jinaai/jina-reranker-v2-base-multilingual`,
`cross-encoder/mmarco-mMiniLMv2-L12-H384-v1`; при достаточном GPU — `BAAI/bge-reranker-v2-gemma` (ADR-300, раздел 3).
Критерии: качество (hit@1, MRR) поверх выбранной модели эмбеддингов, время на GPU в бюджете поиска, размер, лицензия.

| Работа | Оценка |
|---|---|
| Окружение на GPU, расширение скриптов замера (реранкер параметром); повторный замер базовой линии на текущем индексе — прежний замер сделан на корпусе из 20 фрагментов и устарел (ADR-300, раздел 3) | 1 чел-дн |
| Прогон 2–3 моделей на размеченном наборе, замер времени | 2–3 чел-дн |
| Подбор порога отдельно под каждую модель и под выбранные эмбеддинги | 1–2 чел-дн |
| Свод, решение, запись в ADR-300 | 1 чел-дн |

**Оценка: 5–7 чел-дн** сверх замера офлайн-роли (2–3 чел-дн). Замер делается после выбора модели эмбеддингов.

#### Тест моделей судьи качества для MVP

Кандидаты: Qwen2.5-7B (текущая), Qwen2.5-14B / 32B локально на GPU, YandexGPT, GigaChat Pro (судья — другой провайдер,
чем отвечающий; выбор зависит от решения об основном провайдере, ADR-300, раздел 4).

| Работа | Оценка |
|---|---|
| Ручная разметка выборки 100–200 живых ответов (обоснованность, релевантность), часть — двойная | 2–3 чел-дн |
| Прогон 3–4 моделей судьи, сравнение с разметкой (согласие, корреляция) | 1–2 чел-дн |
| Свод и решение о судье | 1 чел-дн |

**Оценка: 4–6 чел-дн** сверх калибровки порогов (2–3 чел-дн).

#### Полнота обезличивания на живых данных

Размечаются ПДн в живых обращениях (ФИО, адрес, телефон, лицевой счёт, почта); измеряются полнота и точность
распознавателей по видам; сравниваются `ru_core_news_md`, `sm`/`lg` и Natasha; правятся распознаватели (ADR-400).

| Работа | Оценка |
|---|---|
| Разметка ПДн в выборке живых обращений (из анализа на живых данных) | 2 чел-дн |
| Прогон моделей и распознавателей, полнота и точность по видам | 1–2 чел-дн |
| Правка распознавателей, выбор модели, запись в ADR-400 | 1–2 чел-дн |

**Оценка: 4–6 чел-дн.**

### Анализ типов обращений на живых данных и повторный выбор моделей (P1) — условие выбора моделей

Метка типа в выгрузке, на которой сделан прототип, поставлена оператором, а таблица построена на 136
текстах (`AI_docs/Inquiry_Types.xlsx`). Поэтому на MVP **до выбора моделей** проводится анализ всех типов
и тем на живых обращениях; по его итогам заново собирается таблица типов и тем и заново выбираются модели
эмбеддингов и запасного классификатора — для MVP и для тиража (решение зафиксировано в ADR-300).

| Работа | Оценка |
|---|---|
| Выгрузка живых обращений (не менее 100 на тип, около 1 500–2 000 текстов), обезличивание, загрузка | 2–3 чел-дн |
| Разметка эталонного набора и анализ всех 16 типов и 6 тем: слова, пересечения, «спор против заявки», тип-статус против типа-темы | 8–12 чел-дн |
| Пересборка таблицы типов и тем (слова, примеры k-NN, проработка, этап) | 3–4 чел-дн |
| Выбор моделей для MVP: тест нескольких моделей на размеченном наборе, пороги поиска и k-NN | 10–14 чел-дн (входит в блок «Качество поиска и классификации») |

**Оценка: 13–19 чел-дн (≈2,5–4 недели) дополнительно к блоку «Качество поиска и классификации»**; весь пакет
вместе с тестом моделей и расширением классификатора — 31–45 чел-дн. Предусловие вне разработки — выгрузка
живых обращений от владельца. Повторное решение по моделям для тиража — в таблице перехода MVP → тираж ниже.

### Проверка Guardrails и выбор провайдера на живых данных (P1) — условие включения MVP

Проверки Guardrails и выбор GigaChat Pro / YandexGPT сделаны без живых ответов и живых вопросов абонентов. На MVP до
включения в работу проводится проверка на ограниченном объёме живых вопросов (порядка 100–200); все запросы идут
через PII Filter (решение зафиксировано в ADR-300 и ADR-400).

| Работа | Оценка |
|---|---|
| Ограниченная выборка живых вопросов, пропуск через PII Filter, подготовка набора | 1–2 чел-дн |
| Guardrails на живых ответах: разметка нарушений (ПДн, тон, разглашение промпта), ложные срабатывания и пропуски, правка шаблонов | 4–6 чел-дн |
| Прогон YandexGPT и GigaChat Pro на той же выборке: качество (судья и ручная проверка), время до первого куска и полного ответа, доля блокировок, отказы и сбои, стоимость | 4–6 чел-дн |
| Оптимизация синхронной проверки: проверять новый кусок с небольшим перекрытием, а не весь накопленный текст (сейчас время растёт с длиной ответа: P95 ≈49 мс на 800 знаков, ≈120 мс на 2 000, ADR-400), замер на ответах 500–3 000 знаков | 1–2 чел-дн |
| Решение об основном и запасном провайдере, запись в ADR-300, настройка цепочек переключения | 1–2 чел-дн |

**Оценка: 11–18 чел-дн (≈2–3,5 недели)** дополнительно к остальным блокам. Настройка самого GigaChat (сертификаты НУЦ,
ключ, канал) в оценку не входит — она учтена в подключении провайдеров. Предусловия вне разработки: доступ к GigaChat в
контуре MVP и право отправлять обезличенные живые вопросы внешним провайдерам.

### Замер чтения профиля из Postgres (MVP)

Профиль абонента на MVP читается из материализованного представления Postgres без слоя Redis (ADR-200, «Кэш профиля
абонента»). Задержка чтения пока оценка. Замер проверяет, что это так.

| Работа | Оценка |
|---|---|
| Наполнить представление объёмом MVP (8 000 абонентов) и прогнать чтение под целевой нагрузкой с несколькими репликами Backend | 1 чел-дн |
| Свод (P50, P99 против бюджета Backend), решение: остаётся Postgres или возвращается Redis, запись в ADR-200 | 0,5–1 чел-дн |

**Оценка: 1,5–2 чел-дн.** В итог таблицы «Итог перехода прототип → MVP» не входит (как и остальные работы по инфраструктуре и кэшу профиля); учтена в `Tech_Stack_Matrix.xlsx`, строка «Кэш профиля абонента». Если замер покажет
превышение бюджета, добавляется работа по кэшу Redis (инвалидация после пересчёта) — вне этой оценки.

### Основная разработка функционального Этапа 2

То, что на прототипе — заглушка или временная замена (ADR-100): `Agent Orchestrator` (FSM уже
написан и покрыт тестами, но не в проде), реальная интеграция `Billing Adapter` и `Сервис работы с
обращениями` с настоящими API (после получения сигнатур от владельцев — см. блок выше), рендер
документов в PDF и хранение в S3/MinIO вместо HTML-каталога, проверка привязки `subscriber_id` к
сессии на каждом write-шаге (открытый P1 по безопасности, ADR-400), интеграция с настоящей SSO-сессией
Платформы ЛК вместо метаданных демо-стенда.

**Оценка: 4–6 недель** — самый неопределённый блок: зависит от того, когда владельцы систем
предоставят сигнатуры API (на прототипе доступа нет и не будет, переносится на MVP целиком) и от
готовности внешнего микросервиса обращений принимать реальные запросы.

### Переход агента на LangGraph — по триггеру (не входит в итог)

Если наступит хотя бы один триггер из ADR-200 («Оркестрация агента», точные условия и моменты проверки там же: модель выбирает следующий
шаг после вызова инструмента; шаг ждёт событие дольше 30 минут и должен продолжиться с того же шага; две и более одновременные
ветви со своими повторами и слиянием результатов), автомат переносится с собственной таблицы на LangGraph. Оценка блока «Основная
разработка Этапа 2» (4–6 недель) считает собственную таблицу; работы ниже — **дополнительные**, при наступлении триггера, и в итог
перехода к MVP не входят.

| Работа | Оценка |
|---|---|
| Перенос таблицы переходов в граф без изменения состава переходов; тесты, перебирающие все состояния, работают и на графе | 2–3 чел-дн |
| Подтверждение: `interrupt_before` либо заменяет проверку состояния, либо не применяется; проверка, что второго механизма подтверждения нет | 1–2 чел-дн |
| Контрольные точки: хранилище (Redis с сохранением или Postgres), сериализация состояния, срок жизни и удаление ПДн из сохранённого состояния | 2–3 чел-дн |
| Ветвление по выбору инструмента (если триггер 1): адаптеры Биллинга, обращений и Диспетчерской как инструменты, ограничение набора и числа шагов, идемпотентность и аудит вызовов | 3–5 чел-дн |
| Зависимость: образ, лицензии, отключение внешней телеметрии библиотеки (`langsmith`), согласование с ИБ | 1–2 чел-дн |
| Регрессия и проверка задержки | 1–2 чел-дн |

Работы выполняются **до написания кода сценария**, в котором сработал триггер. **Оценка: 10–17 чел-дн** (оценка автора, разбивки под неё нет). Если наступает только триггер 2 или 3, строка про инструменты не нужна:
10–17 сокращается примерно до 7–12. Оценка function-calling у провайдеров РФ (≈1 неделя, в плане перехода к тиражу) — предусловие для
решения по триггеру 1 и в эту оценку не входит.

### Документация и согласование ИБ

Полная сверка `C3/C4_L3_Backend.html` с кодом (частично сделана), уровни L3/L4/S/P/D
плана тестирования (нагрузочные, security-сценарии STRIDE), резюме для CTO. **Оценка:
1–1,5 недели.** Согласование с ИБ перечня триггеров self-hosted-перехода и общей архитектуры
безопасности (ADR-400) — календарный срок определяет служба ИБ водоканала, не команда.

### Итог перехода прототип → MVP

| Блок | Оценка разработки | Что вне контроля команды |
|---|---:|---|
| P0 (смета, NFR, допущения нагрузки, пересчёт по данным) | ≈2,5–3,5 недели (11,5–17 чел-дн) | Цены поставщика, статистика ЛК |
| Согласования с владельцами | 1–2 дня на пункт | Скорость ответа водоканала |
| Качество поиска/классификации | ≈7–10 недель (35–51 чел-дн) | Корпус для 9 типов `InquiryType` |
| Анализ типов на живых обращениях | 2,5–4 недели (13–19 чел-дн) | Выгрузка живых обращений от владельца |
| Проверка Guardrails и выбор провайдера на живых данных | 2–3,5 недели (11–18 чел-дн) | Доступ к GigaChat, право отправлять обезличенные вопросы провайдерам |
| Основная разработка Этапа 2 | 4–6 недель | Сигнатуры внешних API, готовность микросервиса обращений |
| Документация и ИБ | 1–1,5 недели | Согласование ИБ |
| **Итого разработка** | **≈20–30 недель** (≈4,5–7 месяцев) | Календарный срок может быть больше — пять блоков зависят от внешних сторон |

Стоимость перехода в деньгах — трудозатраты команды (не оценены в проекте: нет утверждённой ставки
специалиста) плюс инфраструктура MVP из ADR-500 (Kubernetes-кластер, GPU-пул под эмбеддинги/
реранжинг — сам GPU-сервер отдельной строкой сметы не оценён).

## Переход MVP → тираж

Наступает не по календарю, а по триггеру: рост нагрузки сверх экономичного порога внешнего
API, требование ИБ отказаться от внешних вызовов, либо деградация SLA провайдера. До триггера — работа
не начинается, это осознанное решение.

| Задача | Оценка | Примечание |
|---|---|---|
| Сравнение семейства self-hosted-модели (Qwen vs Llama vs открытые веса GigaChat) на Golden Set | 3–5 дней | Оснастка уже есть (`golden_set/`) |
| Оценка function-calling для роли агента Этапа 2 у провайдеров РФ | ≈1 неделя | Открытый вопрос |
| Формализация целевой инфраплатформы отдельным ADR (сейчас Kubernetes только подразумевается) | 2–3 дня | — |
| Закупка и ввод GPU-сервера (1× A100 80GB) | недели–месяцы | Не разработка — снабжение; лид-тайм у поставщика оборудования, не в проекте |
| Развёртывание self-hosted модели в контуре, интеграция с LLM Gateway | 1–2 недели | Смена конфигурации провайдера, не переписывание кода (единый шлюз) |
| Повторное решение по моделям для тиража по живой статистике MVP (проверка при нагрузке тиража, пороги, sizing) | 2–3 дня | После анализа на живых обращениях (ADR-300) |
| Пересчёт NFR под тираж (90 000 абонентов) | предварительно сделано | ADR-500, раздел «Тираж»: расчёт по допущениям A1–A4 без данных водоканала, скорость на GPU не измерялась |

Отдельно, вне таблицы и вне итога ниже: срок хранения подробных событий телеметрии 12 месяцев, сводная таблица по дням и секции по месяцам с замером на 4 млн строк — 2–3 чел-дн (оценка автора, нужна до накопления ≈4 млн строк; ADR-200).

**Итог: ≈3,5–5 недель активной разработки/оценки, плюс лид-тайм закупки оборудования (недели–месяцы,
вне контроля команды).** Стоимость самого перехода — ≈930 тыс.–2,4 млн ₽ на карту (ADR-500) плюс
сервер-хост и обслуживание, не оценённые отдельно.

## Общий вывод

Оба перехода не ограничены разработкой команды — в каждом есть блок, зависящий от внешней стороны
(водоканал, поставщик оборудования, служба ИБ), и именно он определяет реальный календарный срок.
Оценки разработки (4,5–7 месяцев на переход к MVP, 3,5–5 недель плюс закупка на переход к тиражу) — нижняя граница при
условии, что внешние блокеры не задерживают начало соответствующей работы.

## Источники

В пакете сдачи — [ADR-100](ADR-100-scope-and-limitations.md) (что уже сдано), [ADR-500](ADR-500-resources-and-cost.md)
(стоимость ресурсов по контурам).
