---
status: accepted
date: 2026-09-14
---

# ADR-017: Технологический стек по контурам развёртывания

## Статус

**Accepted** как сводка. Каждое решение ниже принято отдельным детальным ADR (`AI_docs/ADR/`) — здесь
только вывод и таблица «выбор против альтернатив»; полное обоснование, драйверы решения и открытые
вопросы — по ссылке на источник.

## Как читать таблицу

Колонка «Контур» показывает, где компонент реально разворачивается: **прототип** — уже работает,
**MVP** — целевая production-инфраструктура (разворачивается после stage-gating, ADR-016), **тираж** —
масштаб на порядок больше. Это ось развёртывания, а не функциональный этап (Этап 1/Этап 2 — про
возможности системы, см. ADR-016) — строка про FSM-оркестрацию ниже единственная, где важна именно
функция, а не контур, это отмечено отдельно.

## Backend и оркестрация

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Веб-фреймворк | Python / FastAPI | прототип и далее | Go, Node.js — сравнение ниже | раздел ниже |
| Оркестрация контейнеров | Kubernetes на своём железе (целевое) | MVP | Docker Compose на нескольких хостах (не переживает отказ узла); Docker Swarm (мёртвая экосистема, нет активного сообщества) | ADR-004 |
| Оркестрация на прототипе | `docker compose`, один хост | прототип | — (единственный разработчик, одна машина — K8s избыточен) | ADR-004, раздел «Отступление прототипа» |
| Оркестрация агента (функциональный Этап 2, любой контур) | Свой FSM (`app/fsm.py`) | написан на прототипе, включается с функционального Этапа 2 | LangGraph — кандидат, не выбран: усложнение ради возможностей, которые пока не нужны | ADR-010 |

### Почему Python, а не Go или Node.js — Backend / API Gateway

Команда полиглотная (Go Middle, Python Middle, Node.js Upper) — все три языка были реальными вариантами,
не гипотетическими. Решающее ограничение — правило 4.1 контекста («L2 — источник истины»): RAG-логика
(эмбеддинг запроса, поиск, сборка промпта) обязана жить **внутри** контейнера Backend, не выноситься в
отдельный сервис.

| Критерий | Python / FastAPI | Go | Node.js |
|---|---|---|---|
| Экосистема RAG/AI (эмбеддинги, PII-обезличивание, LLM-SDK) | Родная — LiteLLM, sentence-transformers, Presidio написаны на Python или имеют полноценные Python-биндинги | Зрелых библиотек почти нет — пришлось бы переписывать их или звать Python-подпроцесс | Тоньше, чем у Python: SDK для LLM-провайдеров есть, но нет инструментов PII-обезличивания и локального инференса эмбеддингов уровня Presidio/sentence-transformers |
| Совместимость с правилом 4.1 (RAG внутри одного контейнера) | Прямая — тулинг уже на этом языке | Нарушается или требует второго языка/процесса внутри контейнера | Та же проблема, что у Go |
| Производительность и конкурентность | Достаточно — нагрузка I/O-bound (вызовы LLM Gateway, Vector DB, Биллинга) | Объективно сильнее всего (goroutines), но выигрыш не используется — узкое место не здесь (см. ниже) | Сопоставимо с Python — тоже событийный цикл, тоже под I/O-bound |
| Contract-first / OpenAPI | FastAPI генерирует OpenAPI-схему из кода — совпадает с принципом проекта | Нужна отдельная библиотека и дисциплина | Нужна отдельная библиотека (например, NestJS + swagger) |
| Итог | **Выбран** | Отклонён | Отклонён |

**Go отклонён**, несмотря на объективное преимущество в конкурентности: этот выигрыш не используется,
потому что узкое место системы — сетевая задержка вызова внешнего LLM-провайдера (сотни миллисекунд —
секунды), а не накладные расходы веб-фреймворка (единицы миллисекунд). **Node.js отклонён** по той же
причине, что и Go — нехватка зрелого RAG/PII-инструментария; использование одного языка для фронтенда и
бэкенда не решает эту проблему, ограничение именно в инструментарии, а не в знании команды JavaScript.

LLM Gateway и ETL Worker на других языках даже не сравнивались — они с самого начала Python-only, потому
что весь нужный тулинг (LiteLLM, sentence-transformers) существует только в этой экосистеме.

## Хранилища

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Векторный поиск (целевое) | Qdrant, self-hosted, в Kubernetes | MVP | pgvector (не тянет фильтрацию по payload на нужном объёме); Milvus (тяжелее в эксплуатации при том же результате) | ADR-006 |
| Векторный поиск (прототип, по умолчанию) | Корпус в памяти процесса | прототип | Qdrant с первого дня — избыточен на 20 парах FAQ | ADR-006, «Отступление прототипа» |
| Векторный поиск (прототип, опционально) | Qdrant через `docker compose`, та же модель эмбеддингов | прототип | Реализовано и проверено (`app/kb/qdrant_store.py`) — переключение одной переменной, не полноценный целевой контур (нет payload-фильтра, blue-green, K8s) | ADR-006 |
| Состояние сессии, квоты | Redis | прототип и далее | In-memory без Redis (fail-open) — отклонено: теряет квоты при рестарте | ADR-008 |
| Телеметрия (целевое) | ClickHouse | MVP | — | `C3/C4_L3_LLM.drawio` |
| Телеметрия (прототип) | Postgres, те же таблицы (`usage_events`, `quality_reports`) | прототип | ClickHouse с первого дня — вторая СУБД ради объёма, который даёт один стенд | контекст, раздел 35.1 |
| Слепки внешних систем (прототип) | Postgres (три базы: Биллинг, ЛК, обращения) | прототип | — | `docker-compose.yml` |

## LLM-хостинг

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Стратегия хостинга генерации | Гибрид: управляемый API по умолчанию + единый шлюз (LiteLLM) + self-hosted по триггерам | прототип → MVP → тираж | Только SaaS весь жизненный цикл (риск вендор-лока и роста цены без права уйти); только self-hosted с начала (CAPEX на GPU до подтверждения ценности продукта) | ADR-002 |
| Шлюз к модели | LiteLLM (100+ адаптеров, единый OpenAI-совместимый интерфейс, retry/circuit breaker из коробки) | прототип и далее | Свой клиент на каждого провайдера — дублирование ради контроля, который LiteLLM уже даёт | ADR-002, раздел 8.1 |

## Секреты и конфигурация

| Слой | Выбор | Контур | Альтернативы (отклонены) | Источник |
|---|---|---|---|---|
| Секреты на разработке | `.env` (шаблон), опционально Vault dev-mode | прототип | GitHub Codespaces — опробован и отклонён: базовая машина (32 ГБ диска) не выдержала полного стека (Ollama + эмбеддинги + torch); локальная машина мощнее и уже рабочая | Журнал, разделы 100–101 |
| Секреты в проде (целевое) | Vault Agent Injector в Kubernetes | MVP | Хранение в CI/CD-переменных без Vault — секрет дублируется в двух местах вместо одного источника истины | контекст, раздел «секреты» |

## Общий принцип выбора на прототипе

Везде, где прототип использует не целевой компонент, а упрощённую замену (Qdrant→память,
ClickHouse→Postgres, Kubernetes→docker compose), причина одна и та же: **объём прототипа не оправдывает
эксплуатационную стоимость целевого инструмента**, а миграция — это смена конфигурации/DSN, не
переписывание бизнес-логики (см. ADR-016, таблицу «Целевые компоненты — упрощённая замена»).

## Источники для углубления

`AI_docs/ADR/ADR-002` (LLM-хостинг), `ADR-004` (оркестрация), `ADR-006` (векторная БД), `ADR-008`
(поведение при недоступности хранилища квот), `ADR-010` (FSM vs LangGraph).
