---
file_name: TODO.md
project: AI-помощник абонента водоканала
purpose: единственный список открытых задач проекта
supersedes: раздел 44.4 файла контекст_4.md (и через него — разделы 27, 33.3, 34.3, 38.2, 39.3, 40.3, 41.10, 42.6, 43.7)
updated: 2026-09-04
language: ru
---

# Открытые задачи

> **Это единственный список.** Разделы 27, 33.3, 34.3, 38.2, 39.3, 40.3, 41.10, 42.6, 43.7 и 44.4
> файла контекста — история, источником статуса не являются. Обновлять **только этот файл**.

**Правило ведения, выведенное из десяти неудачных попыток.** Закрытый пункт **удаляется отсюда**, а
не зачёркивается: к версии 4.30 зачёркнутых стало больше, чем живых, и список перестали читать
целиком — из-за чего два сделанных пункта (22 и 40) числились открытыми, а два закрытых код-шага (9
и часть 10) не были описаны нигде. Куда делся пункт, видно из журнала и `git log`; держать его труп
в списке незачем.

**Нумерация сквозная и не переиспользуется.** Номер, освободившийся при закрытии, новому пункту не
достаётся: на старые номера ссылается журнал.

---

## P0 — блокирует защиту бюджета и перевод любого ADR в Accepted

Ни один из трёх не двигался с версии 4.0. Пока они открыты, **все 13 ADR остаются Proposed**, то
есть вся архитектура проекта состоит из непринятых решений.

| № | Задача | Что разблокирует |
|---|---|---|
| 1 | **Смета CAPEX/OPEX.** Ни одной цены во всём проекте. Количества посчитаны в `Capacity_Report.html`, раздел 8 — осталось умножить на цены из КП по категориям раздела 28.4 | Все ADR. Строка «Экономика» матрицы сдачи |
| 2 | **Расчёт под NFR.** TTFT < 500 мс и P99 поиска < 100 мс не подкреплены расчётом под конкретную GPU и модель (формула раздела 28.2) | ADR-002; критерии приёмки в ТЗ |
| 3 | **Подтвердить допущения A1–A3** статистикой ЛК и колл-центра | Всё, что считается от нагрузки: сейчас разброс выводов 25-кратный, а расход токенов — единственная болезненно растущая статья (раздел 42.5) |

---

## P0.5 — до демонстрации

Поднято из P1 3 сентября 2026: это не «когда-нибудь», а то, что видно на экране во время защиты.

| № | Задача | Почему это ломает демонстрацию |
|---|---|---|
| 52 | **Внести публичные телефоны водоканала в исключения обезличивателя** — 8-903-411-86-76, 8-861-217-03-33, 8-800-200-36-46 опубликованы на сайте, а фильтр принимает их за персональные данные и вырезает из ответов (раздел 60.4 журнала) | В FAQ телефон стоит в половине ответов. Абонент получает «позвоните по ⟨ТЕЛЕФОН⟩» — ответ, который бесполезен ровно там, где он нужен |
| 35 | **Разобраться со временем первого запроса после перезапуска**: 21,6 с до первого куска против 1,9 с в прежнем замере при том же прогреве. Замерить по слагаемым, как в разделе 50.3 журнала, а не предполагать причину | Первый вопрос на защите — и есть первый запрос после перезапуска. Двадцать секунд молчания при заявленном DoD 500 мс |
| 53 | **Решить, заменять ли телефоны в текстах воронежскими.** После приведения к одному филиалу организация воронежская, а номера в ответах краснодарские (код 861) — сочетание несуществующее | Демонстрация показывает организацию, которой нет |

> **Сюда же по срочности относится пункт 62** (порог поиска не разделяет вопросы с ответом и без).
> Он оставлен в разделе «Качество поиска», чтобы не разводить один пункт по двум спискам, но на
> демонстрации проявится так же наглядно: вопрос, ответа на который в базе знаний нет, получит
> контекст и уверенный ответ по чужому фрагменту.

---

## P1 — до начала Этапа 2 и до MVP

### Ждут ответа владельцев систем

| № | Задача | Блокирует |
|---|---|---|
| 36 | **Получить реальные коды типов обращений.** Переданы только названия; пункт 2 `Data_Request.md` просил код и название. До них перечень живёт на синтетических кодах, назначенных нами и помеченных как синтетические | ADR-011 → Accepted |
| 37 | **Получить текст о качестве воды** — формулировку, источник (норматив либо внутренний документ), дату вступления в силу и владельца, вправе её менять | ADR-012 → Accepted; реализация третьего пути ответа |
| 38 | **Подтвердить, где ведутся четыре процесса, которых нет в справочнике обращений**: заключение и расторжение договора, смена тарифа, сверка расчётов. Предположение — реестр договоров, постановление и «Справка»/«Начисления»; на предположении разметку базы знаний строить нельзя | Разметка базы знаний |
| 39 | **Уточнить подтипы обращений.** Механизм найден — суффикс в названии типа («с авто» / «без авто»), — но значение суффиксов неизвестно | ADR-011 → Accepted |
| 41 | **Выяснить, что означают «с авто» и «без авто»** в паре «Повторная приемка узла учета воды в эксплуатацию». Тексты не проясняют; назначать коды по догадке нельзя | ADR-011 → Accepted |
| 46 | **Форма онлайн-доступа к БД отключений на MVP** — таблицы или выделенное представление. Представление снимает единственную применимую силу ADR-003 (схема не контракт) и даётся владельцем дешевле, чем API | ADR-013 → Accepted; MVP |
| 20 | **Сверить сигнатуры внешних API** (XML-RPC Биллинга, API микросервиса обращений, REST Диспетчерской) с владельцами систем — заглушки шага 9 построены на допущениях | Этап 2 |
| 19 | **Подтвердить хранилище готовых документов и срок жизни ссылки** (сейчас предположение, раздел 41.4) | Этап 2 |
| 8 | **Учётные данные GigaChat и корневые сертификаты НУЦ Минцифры** — только к MVP. Доступ к Yandex Cloud получен, прототип работает на настоящем YandexGPT | MVP |
| 12 | **Способ выгрузки источников 3 и 10** для корпуса дообучения (исключение из правила «только через API») | ADR-003 → Accepted |
| 21 | **Согласие на использование расшифровок звонков** (источник 10) либо явный отказ от источника | Этап 3 |

### Качество поиска

**Измерено 3 сентября 2026** на эталонном наборе из 66 вопросов (63 настоящих):
Recall@3 = **87%** (26 из 30), hit@1 = 23%, поиск 33,7 мс при бюджете 100 мс.
Оснастка — `golden_set/`, `scripts/build_golden_set.py`, `scripts/measure_retrieval.py`.

**Тот же замер вскрыл дыру крупнее той, ради которой затевался** — порог не разделяет вопросы
с ответом и без (пункт 62). Разметка набора наша и владельцем не подтверждена: числа показывают
направление, а не качество.

**Кросс-энкодер замерен 4 сентября 2026** (пункт 50 закрыт): тройки расходятся с векторными
**всегда**, hit@1 растёт с 23% до 70%, hit@3 не меняется. Разделяет он лучше косинуса в каждой точке
равной цены, но синхронно на процессоре невозможен — 20,8 с на вопрос.

| № | Задача | Блокирует |
|---|---|---|
| 62 | **Решить, что делать с порогом поиска.** Косинусная близость вопросы с ответом и без не разделяет: 0,828…0,901 против 0,753…0,876, распределения перекрываются. При действующем 0,80 контекст получают 13 из 15 вопросов, на которые корпус не отвечает. Четыре варианта, у всех цена измерена (журнал, разделы 65.3 и 66.3) — см. таблицу ниже | правило «нет контекста — нет генерации»; **до демонстрации** |
| 63 | **Подтвердить разметку эталонного набора у владельца данных.** Вопросы настоящие, соответствие «вопрос → фрагмент» проставлено нами. **Подорожало после замера кросс-энкодера:** три случая, где переранжирование «сломало» результат, оказались местами, где наша метка — соответствие по теме, а не по наличию ответа (журнал, раздел 66.2). От подтверждения зависит верность обоих чисел, 87% и 70% | ADR-006 → Accepted |
| 51 | **Сравнить качество e5-small и BGE-M3** на русскоязычной базе знаний. Оснастка готова и от модели не зависит: `scripts/measure_retrieval.py` берёт модель из настроек | MVP |
| 11 | **Замер F9**: кросс-энкодер против `quality_score` / центроидного сходства при дедупликации | ADR-007 → Accepted |
| 13 | **Измерить длительность полного реиндекса и дневной delta-объём** — сценарий прогона описан в `Test_Plan.html`, раздел 8.2 | ADR-005 → Accepted |

#### Четыре варианта пункта 62, с измеренной ценой каждого

| Вариант | Пропустит мимо (из 15) | Сохранит ответов (из 30) | Применим |
|---|---|---|---|
| Оставить порог 0,80 | 13 | 26 | сейчас; абонент получает уверенный ответ по чужому фрагменту |
| Поднять косинусный порог до нулевого пропуска (> 0,876) | 0 | **4** | сейчас; отказ на пять ответов из шести |
| Косинусный порог на два пропуска (0,86) | 2 | 11 | сейчас; середина, но обе цены заметны |
| **Отсекать оценкой кросс-энкодера** | **0** | **10** | **только MVP**: 20,8 с на вопрос на процессоре |

Последний вариант — единственный, где мера действительно различает наличие ответа, а не близость
темы. На MVP кросс-энкодер и так стоит в конвейере по ADR-007, то есть **отсев и переранжирование —
одна и та же работа**, и делать её дважды разными мерами не нужно. Но и он не решает задачу: при
нулевом пропуске две трети вопросов с ответом получают отказ. Смена меры передвигает цену, а не
отменяет её.

| 58 | **Решить, что вообще классифицируется по тексту.** Замер вскрыл потолок самих данных: метка выводится из текста в 28% случаев. Это не задача перебора ключевых слов, а вопрос о составе оси `inquiry_type` — часть типов, похоже, не определяется по обращению в принципе и должна выбираться абонентом или оператором | ADR-011; Этап 2 |

### Расхождения документов с кодом

| № | Задача | Блокирует |
|---|---|---|
| 60 | **Уровни L3, L4, S, P и D плана тестирования не представлены в репозитории.** Есть только L1 (`tests/unit`) и L2 (`tests/contract`). Нет 14 сквозных сценариев, нет security-тестов S1–S2 — а именно ими `Test_Plan.html` закрывает две угрозы STRIDE, — нет нагрузочных на Locust | DoD; отчёт по ИБ |
| 61 | **Завести реестр отклонений «прототип ↔ MVP».** Их девять, каждое обосновано по отдельности, но лежат в разных разделах журнала (44.5, 50.5, 57.5, 59.4, 62.5, 63.1): модель эмбеддингов, отсутствие переранжирования, статическая страница вместо React, своя таблица вместо LangGraph, Redis без сохранения на диск, график из файла вместо БД, база знаний с публичного сайта вместо документов Word, Биллинг из CSV вместо XML-RPC, пустая база знаний не роняет запуск. На защите вопрос «что доказано, а что нет» требует одной таблицы | Защита; ТЗ |
| 9 | **Дописать «Резюме для CTO» в `ADR/ADR-002-llm-hosting-strategy.md`** — обрывается на середине пункта (3), сам пункт грамматически сломан. Это первое, что читает утверждающий бюджет | P0 №1 |

### Остальное P1

| № | Задача | Блокирует |
|---|---|---|
| 54 | **На MVP пустая база знаний должна ронять запуск.** На прототипе правило консультанта применяется, только если поиск состоялся, — иначе ассистент молчал бы на любой вопрос. Осознанная дыра, видимая по предупреждению в журнале (раздел 62.5) | MVP |
| 45 | **Длительность короткого кэша графика отключений и порог, после которого последнее известное состояние перестаёт отдаваться даже с оговоркой** — осталось выразить числом | MVP |
| 47 | **Внести плановые отключения в каталог источников** (раздел 29) — источник существует, используется, в каталоге отсутствует | — |
| 43 | **Внести поле `type_docs` в каталог данных** — существует в выгрузке, используется, в проекте не описано; в `Data_Request.md` не запрашивалось | Этап 2 |
| 15 | **Пересчитать конфигурацию GPU** под фактическую λ при `batch = 1` (раздел 42.3: прежний sizing переразмерен примерно в 20 раз) | ADR-002 |
| 16 | **Пересчитать порог триггера №2 ADR-002** вместе со сметой и привязать к конкурентности. Сейчас триггер без числа и не срабатывает ни при каком условии, хотя дашборд продолжает считать накопление против 50 млн | ADR-002 |
| 17 | **Заменить «PII 100%» и «Uptime по SLA»** в DoD раздела 18 измеримыми формулировками. Первая заменяется формулировкой из `Test_Plan.html` (ноль пропусков на корпусе ≥ 200 запросов + прод-инвариант); вторая ссылается на SLA, которого в проекте нет — либо появляется число, либо пункт убирается | фаза 5 (ТЗ) |
| 18 | **Задать срок хранения диалогов** — иначе Postgres растёт неограниченно (раздел 42.5) | — |

---

## P2 — до более широкого распространения документации

| № | Задача |
|---|---|
| 23 | Перенести на L2 узлы «CRM колл-центра», «Реестр договоров», «Публичный сайт»; закрыть находки F3 (сквозная нумерация потоков Этапов 1 и 2) и F6 (нет визуального разделения этапов) |
| 24 | Экспорт PNG всех диаграмм — существующие устарели, CLI draw.io в этой среде зависает |
| 25 | Квантифицировать бизнес-цели раздела 2 (целевой % снижения повторных обращений и времени поиска ответа) |
| 26 | **Гигиена пакета сдачи.** Осталось: копии `контекст_4.md` в `dz4`/`dz5`/`dz6` на версии 4.3 с уже отменённой архитектурой доступа к Биллингу; посторонний `C3/test1.png` исключён из истории, но лежит на диске; `Data_Pipeline_Blueprint.html` в корне не числится ни в одном реестре артефактов |

---

## Артефакты сдачи — что осталось

| Артефакт | Статус | Фаза |
|---|---|---|
| Отчёт по ИБ | разрозненно: STRIDE (24.5), правило 4.2, Security Layer (35) — собрать в один документ | 3 |
| Доступы / целевой RBAC | описать на бумаге; проверить на реальных системах невозможно (закрытая сеть) | 3 |
| Экономика MVP + тираж | количества есть, цен нет — P0 №1 | 4 |
| ТЗ | нет отдельного файла; критерии приёмки берутся из `Test_Plan.html`, раздел 13 | 5 |
| Код | остались шаг 7 (сессии и состояние диалога), шаг 10 (судья качества ответа), шаг 11 (финал) | 6 |
| README + структура репозитория | после кода | 7 |
