---
file_name: TODO.md
project: AI-помощник абонента водоканала
purpose: единственный список открытых задач проекта
supersedes: раздел 44.4 файла контекст_4.md (и через него — разделы 27, 33.3, 34.3, 38.2, 39.3, 40.3, 41.10, 42.6, 43.7)
updated: 2026-09-11
language: ru
---

# Открытые задачи

> **Это единственный список.** Разделы 27, 33.3, 34.3, 38.2, 39.3, 40.3, 41.10, 42.6, 43.7 и 44.4
> файла контекста — история, источником статуса не являются. Обновлять **только этот файл**.

**Правило ведения, выведенное из десяти неудачных попыток.** Закрытый пункт **удаляется отсюда**, а
не зачёркивается: к версии 4.30 зачёркнутых стало больше, чем живых, и список перестали читать
целиком — из-за чего два сделанных пункта (22 и 40) числились открытыми, а два закрытых код-шага (9
и часть 10) не были описаны нигде. Куда делся пункт, видно из журнала и `git log`; держать его труп
в списке незачем.

**Нумерация сквозная и не переиспользуется.** Номер, освободившийся при закрытии, новому пункту не
достаётся: на старые номера ссылается журнал.

---

## P0 — блокирует защиту бюджета и перевод любого ADR в Accepted

Ни один из трёх не двигался с версии 4.0. Пока они открыты, **все 14 ADR (002–015) остаются
Proposed**, то есть вся архитектура проекта состоит из непринятых решений.

| № | Задача | Что разблокирует |
|---|---|---|
| 1 | **Смета CAPEX/OPEX.** Ни одной цены во всём проекте. Количества посчитаны в `Capacity_Report.html`, раздел 8 — осталось умножить на цены из КП по категориям раздела 28.4 | Все ADR. Строка «Экономика» матрицы сдачи |
| 2 | **Расчёт под NFR.** TTFT < 500 мс и P99 поиска < 100 мс не подкреплены расчётом под конкретную GPU и модель (формула раздела 28.2) | ADR-002; критерии приёмки в ТЗ |
| 3 | **Подтвердить допущения A1–A3** статистикой ЛК и колл-центра | Всё, что считается от нагрузки: сейчас разброс выводов 25-кратный, а расход токенов — единственная болезненно растущая статья (раздел 42.5) |

---

## P0.5 — до демонстрации

Поднято из P1 3 сентября 2026: это не «когда-нибудь», а то, что видно на экране во время защиты.

**Группа закрыта целиком 4 сентября 2026** — пункты 52 (публичные контакты вырезались из
ответов), 35 (двадцать шесть секунд ждал недоступный Redis), 53 (телефоны других филиалов),
65 (подъём сервиса виден и сокращён с 46 до 31 секунды) и 62 (порог поиска выбран решением
владельца, индексация переделана и перемерена — журнал, раздел 73).

Открытых пунктов «до демонстрации» не осталось.

---

## P1 — до начала Этапа 2 и до MVP

### Ждут ответа владельцев систем

| № | Задача | Блокирует |
|---|---|---|
| 36 | **Согласовать с владельцем назначенные нами коды типов обращений.** Решено 4 сентября 2026: реальных кодов у владельца нет, назначаем сами — синтетические коды стали действующим справочником, а не временной заменой. Осталось не «получить», а согласовать: принимающая система на Этапе 2 будет сверять именно их | ADR-011 → Accepted; до первой регистрации Этапа 2 |
| 37 | **Утвердить формулировку о качестве воды и назначить того, кто вправе её менять.** Решено 4 сентября 2026: текста от владельца не будет, написали сами. **Реализовано** (`app/regulated.py`), путь проверен сквозным прогоном; запись помечена `approved=False`, строгий режим загрузки не даёт ей уехать дальше прототипа. Текст намеренно **направляет, а не утверждает**: заявление «вода соответствует нормативам» — утверждение о безопасности водоснабжения, и делать его за владельца нельзя | ADR-012 → Accepted; **до MVP** |
| 38 | **Подтвердить, где ведутся четыре процесса, которых нет в справочнике обращений**: заключение и расторжение договора, смена тарифа, сверка расчётов. Предположение — реестр договоров, постановление и «Справка»/«Начисления»; на предположении разметку базы знаний строить нельзя | Разметка базы знаний |
| 39 | **Уточнить подтипы обращений.** Механизм найден — суффикс в названии типа («с авто» / «без авто»), — но значение суффиксов неизвестно | ADR-011 → Accepted |
| 41 | **Выяснить, что означают «с авто» и «без авто»** в паре «Повторная приемка узла учета воды в эксплуатацию». Тексты не проясняют; назначать коды по догадке нельзя | ADR-011 → Accepted |
| 46 | **Форма доступа к данным об отключениях на MVP** — таблицы, представление или API. Решено 4 сентября 2026: **на прототипе остаётся прямой доступ к файлу графика**, как и было; вопрос доступа на MVP владельцем не проработан и остаётся открытым. Представление предпочтительнее таблиц: оно снимает единственную применимую силу ADR-003 (схема не контракт) и даётся дешевле, чем API | ADR-013 → Accepted; MVP |
| 20 | **Сверить сигнатуры внешних API** (XML-RPC Биллинга, API микросервиса обращений, REST Диспетчерской) с владельцами систем. Решено 4 сентября 2026: **на прототипе доступа к сигнатурам нет и не будет** — заглушки шага 9 остаются на допущениях осознанно, и это ещё одно отклонение «прототип ↔ MVP» (пункт 61). Сверка переносится на MVP целиком | **MVP**, не прототип; Этап 2 |
| 19 | **Подтвердить хранилище готовых документов и срок жизни ссылки.** На прототипе реализован дубль (v4.53): `app/documents/artifact.py` — HTML для печати, каталог `.documents/`, TTL 24 ч, токен в пути. На MVP — S3/MinIO и presigned-URL с подтверждённым сроком; сейчас и то, и другое предположение (раздел 41.4). Смежно: настоящий рендер в PDF вместо HTML — тоже MVP | MVP; Этап 2 |
| 74 | **Решение владельца: какие числа ассистент вправе называть сам.** Ответчик `account` (раздел 79.1) отвечает на «сколько я должен» суммой из Биллинга, но молчит на «почему такой долг» — граница «факт / спор» проведена нами. На 136 обращениях чистого запроса факта почти нет, вопросы о долге все про «почему». Владелец может считать иначе — например, что сумму без объяснения давать вообще нельзя | `account` на MVP |
| 75 | **Получить настоящие бланки заявлений от владельца.** Сейчас семь ориентировочных образцов (`app/documents/templates.py`), структура — по ПП РФ № 644/776 и публичным формам, помечены «не официальная форма». Заодно закроет состав `collect`-полей: какие поля владелец собирает у абонента, а какие в его форме предзаполнены или не нужны. Часть A8 запроса данных (`Data_Request.md`) | `template` на MVP |
| 76 | **Подтвердить, где ведётся заключение договора (подключение).** Образец `connection` есть, а на регистрационной оси `inquiry_type` такого типа нет (ADR-011). Смежно с пунктом 38 — четыре процесса вне справочника обращений | ADR-011; разметка базы знаний |
| 8 | **Выбрать генерирующую модель MVP: GigaChat или YandexGPT.** Решено 4 сентября 2026: **на прототипе остаётся Яндекс**, GigaChat не подключаем. Живым путём проверен только он — генерация, поток, автопереключение при сбое; GigaChat не проверен ничем, и к нему нужны учётные данные и корневые сертификаты НУЦ Минцифры. К MVP: либо GigaChat проверяется и остаётся по ADR-007, либо основным становится Яндекс и правится таблица состава моделей | ADR-007 → Accepted; **MVP, не прототип** |
| 12 | **Способ выгрузки источников 3 и 10** для корпуса дообучения (исключение из правила «только через API») | ADR-003 → Accepted |
| 21 | **Согласие на использование расшифровок звонков** (источник 10) либо явный отказ от источника | Этап 3 |

### Качество поиска

**Измерено 3 сентября 2026** на эталонном наборе из 66 вопросов (63 настоящих):
Recall@3 = **87%** (26 из 30), hit@1 = 23%, поиск 33,7 мс при бюджете 100 мс.
Оснастка — `golden_set/`, `scripts/build_golden_set.py`, `scripts/measure_retrieval.py`.

**Тот же замер вскрыл дыру крупнее той, ради которой затевался** — порог не разделяет вопросы
с ответом и без (пункт 62). Разметка набора наша и владельцем не подтверждена: числа показывают
направление, а не качество.

**Кросс-энкодер замерен 4 сентября 2026** (пункт 50 закрыт): тройки расходятся с векторными
**всегда**, hit@1 растёт с 23% до 70%, hit@3 не меняется. Разделяет он лучше косинуса в каждой точке
равной цены, но синхронно на процессоре невозможен — 20,8 с на вопрос.

| № | Задача | Блокирует |
|---|---|---|
| 63 | **Подтвердить разметку эталонного набора у владельца данных.** Вопросы настоящие, соответствие «вопрос → фрагмент» проставлено нами. **Переразмечена 6 сентября 2026** под индекс из 51 фрагмента: после подключения документов правильным ответом стал раздел инструкции, а не только пара FAQ — 24 вопроса из 30, +37 ожидаемых фрагментов, размечено по содержанию, а не по выдаче поиска. **Подорожало после замера кросс-энкодера:** три случая, где переранжирование «сломало» результат, оказались местами, где наша метка — соответствие по теме, а не по наличию ответа (журнал, раздел 66.2). От подтверждения зависит верность всех чисел набора | ADR-006 → Accepted |
| 51 | **Сравнить качество e5-small и BGE-M3** на русскоязычной базе знаний. Замер мерил скорость и вес; качество не проверено ни у одной из трёх моделей. **Стало заметнее 4 сентября:** все числа поиска — попадание, MRR и сам порог 0,872 — сняты на паре «e5-small + резка 300 знаков». Порог есть свойство этой пары, а не константа проекта, и на MVP его придётся получать заново вместе с качеством | MVP |
| 11 | **Замер F9**: кросс-энкодер против `quality_score` / центроидного сходства при дедупликации | ADR-007 → Accepted |
| 13 | **Измерить длительность полного реиндекса и дневной delta-объём** — сценарий прогона описан в `Test_Plan.html`, раздел 8.2 | ADR-005 → Accepted |

#### Четыре варианта пункта 62, с измеренной ценой каждого

| Вариант | Пропустит мимо (из 15) | Сохранит ответов (из 30) | Применим |
|---|---|---|---|
| Оставить порог 0,80 | 13 | 26 | сейчас; абонент получает уверенный ответ по чужому фрагменту |
| Поднять косинусный порог до нулевого пропуска (> 0,876) | 0 | **4** | сейчас; отказ на пять ответов из шести |
| Косинусный порог на два пропуска (0,86) | 2 | 11 | сейчас; середина, но обе цены заметны |
| **Отсекать оценкой кросс-энкодера** | **0** | **10** | **только MVP**: 20,8 с на вопрос на процессоре |

Последний вариант — единственный, где мера действительно различает наличие ответа, а не близость
темы. На MVP кросс-энкодер и так стоит в конвейере по ADR-007, то есть **отсев и переранжирование —
одна и та же работа**, и делать её дважды разными мерами не нужно. Но и он не решает задачу: при
нулевом пропуске две трети вопросов с ответом получают отказ. Смена меры передвигает цену, а не
отменяет её.

| 64 | **Пересобрать признаки темы `water_quality` по наблюдениям.** Сейчас они предположение и помечены как предположение: в 136 настоящих обращениях вопросов о качестве воды **ни одного**, в корпусе FAQ темы тоже нет. Замер ложных срабатываний на этих данных — ноль, но это отсутствие наблюдений, а не подтверждение | ADR-012; когда появятся такие обращения |
| 66 | **Пересобрать признаки темы `tariff` по наблюдениям.** Пара к пункту 64. Сейчас предположение и помечены как предположение: в 136 настоящих обращениях вопроса о тарифе **ни одного**. Две находки по словарю оказались не тем — «почему сумма выросла с 233 до 895» (скачок вчетверо тарифом не объясняется) и служебная пометка оператора. Ноль ложных срабатываний здесь — отсутствие наблюдений, а не подтверждение | когда появятся такие обращения |
| 73 | **Замерить признаки тем `account` и `template` на корпусе обращений.** Тройка к пунктам 64/66. `ACCOUNT_PATTERNS`, `TEMPLATE_PATTERNS` и маркеры подвопросов в `app/billing/answer.py` собраны по языку вопроса, не по данным, и помечены `[ПРЕДПОЛОЖЕНИЕ]`: чистого запроса факта в 136 обращениях почти нет. Оснастка — по образцу `scripts/measure_classifier.py` | ADR-011; когда накопятся такие обращения |
| 78 | **Вопросы полей `collect` при заполнении бланка не проверены на живых абонентах.** Формулировки в `DocumentField` (раздел 81) фиксированные и написаны нами; понятны ли они абоненту и не путают ли — не измерено. Смежно с пунктом 75 | `template` на MVP |
| 69 | **Не индексировать служебные разделы документов.** Оба пропуска порога при 0,872 идут через разделы, которые описывают документ, а не отвечают на вопрос: «Область применения» инструкции по начислениям и «Ответ на несогласие с начислением» инструкции по общению. Отсев по роли раздела дешевле, чем поднятие порога, и не стоит ответов | качество поиска |
| 67 | **Внести в корпус три страницы сайта со знанием** — справочная служба, режим работы, электронная квитанция. Остальные 39 страниц раздела «Абонентам» — формы, прайсы и коммерческие услуги; страница вопросов-ответов на сайте ровно одна, и она уже в корпусе. Работа маленькая, но **эталонный набор не сдвинет и может слегка ухудшить**: чем больше кандидатов, тем больше шансов ложного совпадения. Делать с замером до и после (журнал, раздел 74.1) | — |
| 70 | **Согласовать с владельцем перечень типов, по которым ассистент предлагает подать обращение.** Сейчас их семь из шестнадцати — поверка, опломбировка, установка и снятие прибора учёта, справка, обследование, приём документов — и перечень **наш**, как и коды (пункт 36). Владелец может считать заявкой не то же самое | ADR-011; Этап 2 |
| 71 | **`Dev_Plan.html` снова разошёлся с кодом**: шаг 9 показан незакрытым, шаг 7 — частичным, хотя оба закрыты. Третий раз подряд расхождение находится не само, а при чтении плана — стоит завести проверку статусов против кода, а не править их вручную | планом пользуются как картой |
| 72 | **Назначить срок хранения обращений в таблице.** Срок жизни диалога назван (30 минут), а сама запись обращения живёт вечно: в ней те же слоты с ФИО и лицевым счётом. На прототипе это терпимо, на MVP срок назначает владелец | MVP; 152-ФЗ |
| 80 | **Назначить срок хранения `usage_events`.** В строках — `subscriber_id` и `session_id` (идентификаторы, не значения), но живут вечно. Пара к пункту 72; политика ротации нужна на MVP (раздел 42.5 уже требует её для Loki/ClickHouse) | MVP; 152-ФЗ |
| 81 | **На MVP заменить `UsageStore` клиентом ClickHouse и сделать запись асинхронной пачкой** (сейчас синхронный `INSERT` в Postgres после ответа, раздел 83.3). Тогда же считать стоимость в деньгах: `prompt/completion_tokens` × тариф провайдера — как только появится тариф (P0 №1) | MVP; смета |
| 82 | **Откалибровать судью — 7B слабо различает.** Базовая линия (журнал 84.8): faithfulness 0.913, answer_relevancy 0.570, pass_rate 3%. Судья подключён и к живому пути (журнал 89): строка на ответ в `quality_assessments`, `provisional=true`. Но Qwen-7B якорится — 22 из 30 ровно `answer_relevancy = 0.6`; и в ночном прогоне, и на живом пути число ориентир, не гейт. Нужно: судья MVP (YandexGPT против GigaChat) либо модель крупнее 7B; сверить с ручной разметкой Annotator (раздел 36.4). После этого снять `provisional` и включить пороги как критерий | код-шаг 10; DoD качества |
| 83 | **`classify_by_keywords` путает «хочу поверку» и «уже сделал поверку».** Оба содержат слово «поверка», и `Registrar.offer()` в обоих случаях предлагает один и тот же черновик `Обращение: Заказать поверку`. Живой пример: «Сделали поверку по счётчику ХВ 10.09.2026, внесите изменения по моему адресу» — абонент сообщает о выполненной работе и просит обновить запись, а черновик предлагает заказать её заново, обратный смысл. Судья на живом пути независимо оценил тот же ответ как нерелевантный (`faithfulness=0.0`, `answer_relevancy=0.5`, причина: «вопрос был о поверке счётчика, а не о нулевой квитанции» — совпадение по КБ тоже ложное, на грани порога 0.872). Нужно: либо сузить ключевые слова до намерения («хочу», «нужна», «закажите» — не факт «сделали», «прошли», «завершили»), либо не предлагать регистрацию при низкой `answer_relevancy` от судьи, либо завести отдельный путь «абонент сообщает о выполненной поверке» — сейчас такого сценария нет ни на одной диаграмме, не только в коде | ADR-011; качество Этапа 2 |
| 68 | **Собрать по обращениям список тем, на которые в корпусе нет ответа, и заказать владельцу объяснения.** Эталонный набор уже называет пятнадцать таких вопросов: возврат средств, расчёт водоотведения, отмена заявки, оплата госпошлины. Это единственный путь снять измеренный потолок поиска — больше половины настоящих вопросов получают отказ там, где ответ мог бы быть. **Сами обращения в векторную базу не кладутся** (контекст, раздел 75.7): медиана ответа оператора 38 знаков, обобщаемых записей 4 из 136, и это переписка по конкретным лицевым счетам | потолок качества поиска; корпус |
| 58 | **Решить, что вообще классифицируется по тексту.** Замер вскрыл потолок самих данных: метка выводится из текста в 28% случаев. Это не задача перебора ключевых слов, а вопрос о составе оси `inquiry_type` — часть типов, похоже, не определяется по обращению в принципе и должна выбираться абонентом или оператором | ADR-011; Этап 2 |

### Расхождения документов с кодом

| № | Задача | Блокирует |
|---|---|---|
| 60 | **Уровни L3, L4, S, P и D плана тестирования не представлены в репозитории.** Есть только L1 (`tests/unit`) и L2 (`tests/contract`). Нет 14 сквозных сценариев, нет security-тестов S1–S2 — а именно ими `Test_Plan.html` закрывает две угрозы STRIDE, — нет нагрузочных на Locust | DoD; отчёт по ИБ |
| 9 | **Дописать «Резюме для CTO» в `ADR/ADR-002-llm-hosting-strategy.md`** — обрывается на середине пункта (3), сам пункт грамматически сломан. Это первое, что читает утверждающий бюджет | P0 №1 |
| 77 | **Полная сверка `C3/C4_L3_Backend.html` с кодом.** В v4.53 диаграмма свёрнута 17 → 7 узлов (журнал 87): параметры поиска (e5-small, без реранжинга, порог 0.872) и роль кэша профиля («на прототипе чтение файлов при старте») подписаны прямо в боксах `Knowledge Base Search` и `Reference Data` с пометками «целевое / прототип»; таксономия — в ноту (`app/taxonomy.py`, две оси ADR-011). Осталось: свести общую ноту `n_fold` к нулю по мере того, как узлы получают прототипную реализацию | достоверность C3 |

### Остальное P1

| № | Задача | Блокирует |
|---|---|---|
| 54 | **На MVP пустая база знаний должна ронять запуск.** На прототипе правило консультанта применяется, только если поиск состоялся, — иначе ассистент молчал бы на любой вопрос. Осознанная дыра, видимая по предупреждению в журнале (раздел 62.5) | MVP |
| 45 | **Длительность короткого кэша графика отключений и порог, после которого последнее известное состояние перестаёт отдаваться даже с оговоркой** — осталось выразить числом | MVP |
| 47 | **Внести плановые отключения в каталог источников** (раздел 29) — источник существует, используется, в каталоге отсутствует | — |
| 43 | **Внести поле `type_docs` в каталог данных** — существует в выгрузке, используется, в проекте не описано; в `Data_Request.md` не запрашивалось | Этап 2 |
| 15 | **Пересчитать конфигурацию GPU** под фактическую λ при `batch = 1` (раздел 42.3: прежний sizing переразмерен примерно в 20 раз) | ADR-002 |
| 16 | **Пересчитать порог триггера №2 ADR-002** вместе со сметой и привязать к конкурентности. Сейчас триггер без числа и не срабатывает ни при каком условии, хотя дашборд продолжает считать накопление против 50 млн | ADR-002 |
| 79 | **Оценить function-calling у GigaChat / YandexGPT и потребность агента Этапа 2 в отдельной модели и ресурсах.** ADR-007 перечисляет пять ролей моделей, агентной (tool-calling / Form) среди них нет — неявно предполагается та же генеративная модель. Не проверено: надёжность function-calling у провайдеров РФ, нужна ли агенту модель крупнее RAG, не появляется ли self-hosted-GPU. Надбавка к токенам посчитана — `Capacity_Report.html`, раздел 8.5 (A6–A8): ~+20% на обоих масштабах, но при плохом function-calling может быть +40–60%. Смежно с ADR-010 (триггер 3) и пунктом 8 | ADR-007 → Accepted; смета MVP |
| 17 | **Заменить «PII 100%» и «Uptime по SLA»** в DoD раздела 18 измеримыми формулировками. Первая заменяется формулировкой из `Test_Plan.html` (ноль пропусков на корпусе ≥ 200 запросов + прод-инвариант); вторая ссылается на SLA, которого в проекте нет — либо появляется число, либо пункт убирается | фаза 5 (ТЗ) |

---

## P2 — до более широкого распространения документации

| № | Задача |
|---|---|
| 23 | Перенести на L2 узлы «CRM колл-центра», «Реестр договоров», «Публичный сайт»; закрыть находки F3 (сквозная нумерация потоков Этапов 1 и 2) и F6 (нет визуального разделения этапов) |
| 24 | Экспорт PNG всех диаграмм — существующие устарели, CLI draw.io в этой среде зависает. У C1 устаревший PNG удалён владельцем 5 сентября 2026; пересобрать его вместе с остальными |
| 25 | Квантифицировать бизнес-цели раздела 2 (целевой % снижения повторных обращений и времени поиска ответа) |
| 26 | **Гигиена пакета сдачи.** Осталось: копии `контекст_4.md` в `dz4`/`dz5`/`dz6` на версии 4.3 с уже отменённой архитектурой доступа к Биллингу; посторонний `C3/test1.png` исключён из истории, но лежит на диске; `Data_Pipeline_Blueprint.html` в корне не числится ни в одном реестре артефактов |

---

## Артефакты сдачи — что осталось

| Артефакт | Статус | Фаза |
|---|---|---|
| C1 — диаграмма контекста | доработана владельцем 5 сентября 2026: 6 внешних систем, десять подписанных потоков. Осталось — экспорт PNG (пункт 24) | сдача |
| Отчёт по ИБ | разрозненно: STRIDE (24.5), правило 4.2, Security Layer (35) — собрать в один документ | 3 |
| Доступы / целевой RBAC | описать на бумаге; проверить на реальных системах невозможно (закрытая сеть) | 3 |
| Экономика MVP + тираж | количества есть, цен нет — P0 №1 | 4 |
| ТЗ | нет отдельного файла; критерии приёмки берутся из `Test_Plan.html`, раздел 13 | 5 |
| Код | шаг 10: судья написан (`app/quality/`), базовая линия снята первым живым прогоном (faithfulness 0.91 / relevancy 0.57 / pass 3%, журнал 84.8) — остаётся калибровка судьи (пункт 82). Остаётся шаг 11 (финал). Шаг 7 закрыт вместе с состоянием диалога, шаг 9 — вместе с записью обращения | 6 |
| README + структура репозитория | после кода | 7 |
